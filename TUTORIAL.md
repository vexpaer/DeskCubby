# DeskCubby Android 0.16.0 使用教学

本文对应 DeskCubby Android **0.16.0** 与 Windows **0.7.0**。平台专属操作会明确标出；未标注的既有编号章节描述 Android。

Android 0.16.0 新增繁体中文/韩语/日语支持（首次启动先选择语言，设置 → 外观与语言可随时切换）；桌面小卡片加入可直接在桌面游玩的六个小游戏、音乐可视化、阅读、使用时间图表与合并的云端同步模块；PDF 阅读支持自由二维拖动并在双指缩放松手后保持大小位置一致；云端同步新增「撤回一次」。0.15.0 把 AI 设置改为独立子页结构：模型配置移到「AI 配置」子页，「AI 设置」页新增 AI 页面字体大小、回复框宽度与可恢复默认的 Agent 提示词；Agent 回复渲染 Markdown；导航页支持一列/两列/三列布局、长按进入布局更改模式（四点手柄排序、对勾退出），并可为每个模块设置名称、按钮底色与整体底色；PDF 双指缩放改为矩阵实时跟随、手势结束后重新加载。0.14.0 引入的 Agent Runtime、Agent Review/Undo 与导航页四点手柄能力继续保留，应用备份格式同步升至 v31（兼容导入 v1–v30）。Reader 私有状态保持 schema v7、Room 保持 v13。Windows 0.7.0 可导入 Android v1–v29 并统一导出 v29；桌面 PDF 改用 React-PDF/PDF.js，支持原稿显示、连续滚动、文字选择、目录、搜索、旋转与密码文档，并修复 TXT 配色。

DeskCubby 是一款**本地优先**的个人记录应用：日记和笔记以 Markdown 文件保存在你自己选择的文件夹里，小巧思、日期记录、诗词本、小游戏等结构化数据保存在本机数据库中。Windows 0.7.0 包含首页、日记/吃历/日常记录、笔记、小巧思、日期记录、诗词、阅读、RSS、AI、收藏夹、小游戏（含本地双人围棋）、统计、手机使用时间、健康、备份和设置；不制作 Android 内置浏览器与桌面小卡片。Windows 的手机使用时间与健康只显示、不采集也不上传。

开发者说明：Android Kotlin Plugin API 是供以后新功能使用的内部旁路架构，不是一个用户页面，也不会出现插件按钮或安装入口。当前生产插件集合为空，既有业务代码和页面调用链没有迁移；本教程里的可见变化来自 Android 业务实现，不是 Plugin API 自动注入。架构说明见 [android/plugin-api/README.md](android/plugin-api/README.md)。

## Windows 快速上手

1. 安装 NSIS 包，或直接运行便携 EXE。正式包必须通过 Tauri updater 私钥签名才能启用安全的应用内更新；Windows Authenticode 为可选发布者身份，因此没有代码签名证书的正式包仍可能显示“未知发布者”。
2. 打开 **设置 → 子页面设置 → 日记与媒体**，分别选择**日记目录**和**媒体目录**；需要自动备份时，再到 **设置 → 应用数据** 选择**自动备份目录**。可以直接选择已有的 Obsidian/DeskCubby 文件夹，Windows 会原地读写，不复制整个目录。
3. 从侧栏进入「备份」，点「选择并预览」导入 Android v1–v29 JSON；核对统计、勾选确认框后再点「确认导入」。旧格式会在内存中安全升级，Windows 统一导出 v29，单文件最大 64 MiB。
4. 从左侧栏进入「日记」开始记录；笔记、阅读、RSS、AI、收藏夹、小游戏、统计、手机使用时间和健康都有独立入口或可从「更多」打开。WebDAV/S3 位于 **设置 → 应用数据 → 云端同步**。

## Android 快速上手

1. **首次启动先选择语言**：第一次打开应用会显示语言选择页（简体中文、繁体中文、English、한국어、日本語），选择后立即生效；之后可在 **设置 → 外观与语言 → 软件语言** 随时切换。
2. **软件教学模式默认开启**：第一次进入每个页面会显示一层深色蒙版，介绍当前页面和关键操作；点外部或按返回键无法误关，必须点「知道了」。每个页面各确认一次，确认只保存在当前设备；可在 **设置 → 关于 → 页面教学** 关闭总开关或点「重新显示全部页面教学」。
2. 默认底部导航栏有五个页面：**首页、日记、小巧思、导航、设置**。笔记、浏览器、日期记录、诗词本、RSS、AI 聊天、收藏夹、阅读、小游戏、统计和小卡片默认收在「导航」页里；手机使用时间与健康可从“统计”进入，也可在设置中单独加入底栏或导航页。
3. 新用户可直接打开底栏 **日记**，在空状态点 **一键设置默认目录**。Android 会打开 SAF 系统文件夹选择器并定位到本机 Documents；你仍需确认一次读写授权，随后 DeskCubby 创建或复用 `Documents/deskcubby/diary` 和 `Documents/deskcubby/media`，校验成功后同时绑定。也可点 **手动选择目录**，前往 **设置 → 子页面设置 → 日记与媒体** 分别指定现有目录。
4. 目录就绪后，点日记页右下角「进入今日日记」即可写第一篇日记。不选日记目录无法写日记；不选媒体目录无法插图或使用主页饮食图片。要使用 Obsidian 笔记库，则进入 **导航 → 笔记 → 选择笔记库**；笔记媒体不使用日记媒体目录，每次上传都会重新选择笔记库内的目标文件夹。

## 目录

- [Windows 0.7.0 完整教学](#windows-070-完整教学)
- [1. 主页](#1-主页)
- [2. 日记（列表与回收站）](#2-日记列表与回收站)
- [3. 日记编辑器](#3-日记编辑器)
- [笔记页面（Android）](#笔记页面android)
- [4. 吃历（含滤镜、筛选与长图导出）](#4-吃历含滤镜筛选与长图导出)
- [5. 日常记录](#5-日常记录)
- [6. 小巧思](#6-小巧思)
- [7. 浏览器](#7-浏览器)
- [8. 日期记录](#8-日期记录)
- [9. 诗词本](#9-诗词本)
- [10. RSS 订阅](#10-rss-订阅)
- [11. DeskCubby Agent](#11-deskcubby-agent)
- [12. 收藏夹（加密保险库）](#12-收藏夹加密保险库)
- [13. 小游戏](#13-小游戏)
- [阅读页面（Android）](#阅读页面android)
- [统计中心（Android）](#统计中心android)
- [14. 手机使用时间](#14-手机使用时间)
- [15. 健康](#15-健康)
- [16. 导航页与底部导航栏](#16-导航页与底部导航栏)
- [桌面小卡片与同步小组件（Android）](#桌面小卡片与同步小组件android)
- [17. 设置](#17-设置)
  - [17.1 设置主页与通用操作](#171-设置主页与通用操作)
  - [17.2 外观与语言](#172-外观与语言)
  - [17.3 子页面设置](#173-子页面设置)
  - [17.4 手机使用时间设置](#174-手机使用时间设置)
  - [17.5 健康设置](#175-健康设置)
  - [17.6 主页设置](#176-主页设置)
  - [17.7 日记与媒体](#177-日记与媒体)
  - [17.8 浏览器设置](#178-浏览器设置)
  - [17.9 小巧思设置](#179-小巧思设置)
  - [17.10 诗词本设置](#1710-诗词本设置)
  - [17.11 RSS 订阅设置](#1711-rss-订阅设置)
  - [17.12 AI 配置](#1712-ai-配置)
  - [17.12.1 收藏夹设置](#17121-收藏夹设置)
  - [17.13 应用数据](#1713-应用数据)
  - [17.14 云端同步](#1714-云端同步)
  - [17.15 底部导航](#1715-底部导航)
  - [17.16 导航页设置](#1716-导航页设置)
  - [17.17 关于](#1717-关于)
- [常见问题](#常见问题)

---

## Windows 0.7.0 完整教学

### W1. 安装、便携启动与首次配置

Windows 版支持 Windows 10/11 x64。NSIS 安装包会把应用加入开始菜单；便携 EXE 可以直接运行，但应用数据库仍会保存在当前 Windows 用户的 `%LOCALAPPDATA%\com.deskcubby.windows\`，所以“便携”只表示无需安装，不表示数据与 EXE 放在一起。普通本地构建没有可信更新源，不会发起更新检查。正式 updater-enabled 包必须通过 Tauri updater 私钥签名；Authenticode 为可选增强，未配置证书的正式包仍可安全验证更新，但可能被 SmartScreen 标记为“未知发布者”。

应用不会在首次启动时强制复制或接管目录。进入设置后按需配置：

| 控件 | 操作 | 效果 |
| --- | --- | --- |
| 设置 → 子页面设置 → 日记与媒体 → 「选择文件夹」 | 单击后在系统文件夹选择器确认 | 分别设置日记目录和媒体目录；日记目录中的 Markdown 是正文真实来源，媒体目录用于图片和 `dc-media.json`。右上角点「保存」后生效。笔记库在「笔记」页单独选择。 |
| 设置 → 应用数据 → 自动备份目录 → 「选择文件夹」 | 单击后选择 | 设置可选备份目录；保存后自动备份使用 `dc.pending.json` / `dc.json` / `dc.previous.json` 轮换。 |
| 侧栏 → 备份 → 「选择并预览」 | 单击后选文件 | 选择 Android v1–v29 JSON，只生成严格校验后的预览，不立即替换数据；旧格式会安全补齐为 v29。 |
| 预览中的确认复选框与「确认导入」 | 核对统计后勾选并单击 | 先建立导入前恢复点，再以一个事务替换 Windows 管理的核心结构化数据。 |

选择已有目录不会复制、移动或批量改名原文件。Windows 另存本机路径，不会把 Android 备份中的 `content://` URI 当作路径使用。

### W2. 应用壳、侧栏与通用操作

左侧栏默认按「记录」「资料库」「订阅与智能」「娱乐与统计」「工具」五类分组，包含首页、日记、吃历、日常记录、笔记、小巧思、日期记录、诗词本、阅读、RSS、AI、收藏夹、小游戏、统计中心、手机使用时间、健康、更多和备份共 18 个主页面；底部设置入口固定显示。云同步与关于/更新位于设置层级。Android 内置浏览器和桌面小卡片不会出现。

- 单击分类标题可折叠/展开该分类；折叠后标题下面的页面按钮会一起隐藏并退出 `Tab` 键顺序，再次展开才恢复。折叠状态保存在当前电脑的 WebView 偏好中，不进入 Android v29 JSON。
- 宽屏侧栏只有中间的页面分类区滚动，窗口高度不足时仍可滚到最后一个页面；底部设置按钮不会被内容推走。宽屏可以整体收起侧栏，窄窗口点左上角菜单打开抽屉，选择页面后抽屉自动关闭。
- **设置 → 桌面导航** 可以控制全部 18 个主页面的显示、同类顺序及所属分类，也可新增分类、编辑中英文分类名、调整分类顺序或删除分类。删除分类只会把其中页面迁移到相邻分类，不会删除页面；最后一个分类不能删除。
- 隐藏页面仍可从「更多」或应用内相关入口打开。若保存配置时隐藏了当前页面，会自动回到第一个可见页面；即使全部主页面都隐藏，底部设置入口仍可用。

通用交互：

- 顶栏返回按钮返回上一级；如果设置草稿或日记正文尚未处理，会先弹出确认对话框。
- `Tab` / `Shift+Tab` 可移动焦点，`Enter` 或 `Space` 激活按钮；对话框可按 `Esc` 取消。
- 带拖动手柄的列表既可用鼠标拖动，也提供键盘“上移/下移”操作。
- 成功提示只在 Rust 后端完成数据库事务或文件落盘后出现。失败时内容保留在输入框中，可修正问题后重试。
- 语言、主题、明暗模式和字号来自「设置 → 外观与语言」，三套主题不会改变数据或文件格式。

### W3. 首页

首页按 **设置 → 子页面设置 → 主页** 中保存的顺序显示模块。可用项包括日历、今天、每日诗词、最近日记/小巧思、快速输入、日常记录、饮食图片、日期记录、连续记录、本月日记、总字数、随机旧日记、年度进度、笔记、小游戏快捷入口和记录概览；Android 的网站快捷入口在 Windows 被排除。还可设置用户名、双语问候、模块标题、卡片边框、饮食按钮图标和既有七个小游戏/变体的快捷入口。

| 模块 | 操作 | 效果 |
| --- | --- | --- |
| 每日诗词 | 单击卡片 | 打开完整诗词；可加入诗词本。网络失败时显示本地缓存或内置诗句。 |
| 最近日记 | 单击条目 | 打开对应 Markdown 日记。 |
| 最近小巧思 | 单击条目 | 进入小巧思页并定位到内容。 |
| 快速输入 | 输入后发送 | 新建小巧思；确认写入数据库后才清空。 |
| 日常记录 | 选择模板、替换 `xx`、发送 | 追加到今日日记；写盘失败不会清空。 |
| 饮食图片 | 选择餐别并导入图片 | 导入媒体目录并把相对文件名追加到今日日记；如已配置支持图片的 AI，可在吃历中明确发起热量估算。 |
| 随机旧日记 | 单击刷新/条目 | 换一篇随机旧日记或直接打开。 |
| 笔记 | 单击 | 打开笔记库；未选择目录时先进入选择状态。 |
| 小游戏 | 单击具体图标 | 直接打开设置中选择的 2048 变体、贪吃蛇、俄罗斯方块、扫雷或蜘蛛纸牌。围棋请从 **侧栏 → 小游戏** 进入；其棋局与战绩只保存在这台电脑，不进入 v29。 |
| 记录概览 | 单击统计项 | 查看日记、小巧思、日期记录和诗词数量，并可进入统计中心。 |

月度统计、总字数和最近日记均来自可重建索引；外部编辑后使用日记页「刷新」重新扫描即可更新。

### W4. 日记列表、创建与回收站

进入「侧栏 → 日记」后：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「刷新」 | 单击 | 重新扫描已选日记目录，按月份更新索引；外部新建/删除文件会在这里反映。 |
| 搜索框 | 输入标题、日期或正文关键词 | 筛选当前扫描结果，不修改文件。 |
| 「新建日记」 | 单击 | 输入日期/标题或文件名后创建 Markdown。重名、Windows 保留名和非法路径会被拒绝。 |
| 「今日日记」 | 单击 | 打开今天已有日记；不存在时按当前命名规则创建。 |
| 月份标题 | 单击 | 展开或折叠该月。 |
| 日记条目 | 单击 | 打开编辑器；打开时记录 SHA-256、大小和修改时间。 |
| 条目菜单 → 「重命名」 | 单击并输入新名称 | 安全重命名；目标已存在时拒绝覆盖。 |
| 条目菜单 → 「移入回收站」 | 单击并确认 | 软删除到专用回收站；复制/移动验证成功后才从列表移除。 |
| 「回收站」 | 单击 | 查看已删除日记；可「恢复」或「永久删除」。 |

恢复遇到同名文件时不会静默覆盖：请修改恢复名称或保留两份。永久删除不可恢复，应用会再次要求确认。

### W5. Markdown 编辑器与外部修改冲突

编辑器有「Markdown 源码」和「阅读预览」两种模式。源码使用 CodeMirror 6；预览渲染基础 CommonMark，但保存的仍是原始源码，因此未知 Obsidian 语法不会经过 AST 重写。预览会正常显示日记中以相对文件名引用的图片，也支持中文和空格文件名；React Markdown 产生的百分号编码仍须通过 Rust 媒体根目录协议和叶文件名校验，不会因此开放任意本机路径。图片缺失或无法解析时会结束加载并显示「图片不可用」。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「源码 / 预览」 | 单击切换 | 在编辑与阅读之间切换，不会自动改变正文。 |
| 「保存」或 `Ctrl+S` | 单击/快捷键 | 比较打开时 `FileVersion` 与磁盘当前版本；一致才安全写入并回读验证。 |
| 「插入图片」 | 单击并选择图片 | 处理 EXIF 方向，可按设置压缩到最长边 2560 px，然后写入媒体目录并插入相对 Markdown 文件名。 |
| 「日常记录」 | 单击 | 从模板生成一句文字，追加到当前编辑器或直接写盘。 |
| 返回 | 单击或系统返回 | 有未保存编辑时先询问保存、放弃或继续编辑。 |

如果 Obsidian 或其他编辑器在你打开日记后修改了同一文件，保存会显示冲突：

- 「重新加载」：放弃当前编辑草稿，读取磁盘最新内容；若文件已被外部删除，则接受删除并关闭当前编辑。
- 「覆盖」：明确用当前草稿覆盖磁盘版本；只有用户主动选择才执行。若原文件已被外部删除，会安全重建同名文件。
- 「另存副本」：保留磁盘原文件，把当前草稿保存为不冲突的新文件；原文件已被删除时也能直接保存草稿副本。

推荐先复制当前草稿再重新加载，或使用「另存副本」对比两份；应用不会自动决定哪一份更新。

### W6. 媒体、吃历与 PNG 长图

图片导入使用安全文件名，只写入已选媒体目录，Markdown 中始终使用相对文件名。应用兼容读取 `dc-media.json` v2 和旧名 `deskcubby-media.json`；可显示及编辑已有热量详情和地点，新图片可记录 EXIF 经纬度，但不做在线反向地理编码。配置支持图片的 AI 后，可由用户明确发起热量估算。

进入「侧栏 → 吃历」后：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 日期范围 | 选择开始/结束日期 | 首尾日期都包含；只筛选展示，不改日记。 |
| 餐别筛选 | 勾选早餐/午餐/下午茶/晚餐/水果/夜宵 | 只显示所选餐别，并遵循固定餐别顺序。 |
| 布局「2 / 3 / 智能」 | 单击 | 每行 2 张、3 张，或用 2/3 组合避免最后一行单独留空。 |
| 日期卡片「一列 / 两列」 | 单击 | 一列按原顺序铺满内容区；两列按日期列表中点分成左右两列，每一天及其全部餐食始终放在同一列，不会拆开。窄窗口自动回退为一列。 |
| 「滤镜」 | 开关并调整滑杆 | 非破坏调整亮度、对比度、饱和度、色温和色调；原图片不变。开关和五项参数会持久保存，离开吃历或重启应用后不会复原。 |
| 图片 | 单击 | 全屏查看；可缩放、平移，并查看说明、已有热量和地点。 |
| 「导入饮食图片」 | 选择日期、餐别和图片 | 安全导入媒体目录，并把相对文件名写入对应日记；导入本身不自动上传图片。 |
| 「估算热量」/详情中的「重新估算」 | 选择已配置的视觉/文本模型并确认 | 仅在用户确认后把选中图片或当天图片发给当前 AI 端点，结果写入 `dc-media.json` v2；失败不提交半套结果。 |
| 「导出 PNG」 | 单击、选择范围与保存位置 | 沿用当前筛选、布局、滤镜和说明设置生成长图；缺图显示占位。 |

滤镜、每行图片数量、智能换行、说明文字和日期卡片一/两列选择都写入 Windows 的受管设置并原子保存；默认日期卡片为一列。导出前会检查总像素和高度。范围过大时操作会安全失败，请缩短日期分批导出；失败不会产生一个被误报为成功的损坏文件。

### W7. 日常记录

进入「侧栏 → 日常记录」：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「新增模板」 | 单击 | 保存一整句模板，例如“今天跑了 xx 公里”。 |
| 模板菜单 → 编辑/删除 | 单击 | 修改模板或确认删除；模板只存 Windows 本机设置。 |
| 模板卡片 | 单击 | 把整句填入输入框；第一次定位到 `xx` 时自动选中，直接输入即可替换。 |
| 「写入当前日记」 | 单击 | 在已有编辑会话中追加；没有当前日记时按钮会提示先打开一篇。 |
| 「写入今日日记」 | 单击 | 打开或创建今日日记后追加，并刷新日记索引。 |

后端真正落盘后才显示成功并清空输入；遇到目录无权限、外部冲突或磁盘错误时保留原句。

### W8. 小巧思、分类与回收站

进入「侧栏 → 小巧思」：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 输入框 + 「添加」 | 输入后单击 | 新建小巧思；可同时选择分类。 |
| 搜索框 | 输入关键词 | 筛选当前页小巧思；不会修改内容或顺序。 |
| 分类栏 | 单击分类 | 查看全部或指定分类。 |
| 「新增分类」 | 单击 | 设置名称和颜色；分类名/ID 重复会提示。 |
| 分类菜单 → 编辑/删除/导出 | 单击 | 修改分类；删除前处理关联条目；导出该分类为 UTF-8 文本。 |
| 条目「置顶」「重点」 | 单击 | 分别控制列表顶部位置与重点视觉标记。 |
| 拖动手柄 | 鼠标拖动或键盘上移/下移 | 持久化同一列表中的顺序。 |
| 条目菜单 → 编辑/移入回收站 | 单击 | 修改内容，或软删除并保留恢复机会。 |
| 「回收站」 | 单击 | 可恢复或永久删除；永久删除会再次确认。 |

分类、颜色、置顶、重点和顺序都会进入 Windows 管理的 v29 导出字段。导入时会校验分类关联，孤立引用不会被静默接受。

### W9. 日期记录

进入「侧栏 → 日期记录」：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「新增日期」 | 单击 | 填写名称、图标和日期。 |
| 记录卡片 | 单击 | 打开编辑；保存后重新计算距今天数。 |
| 卡片菜单 → 删除 | 单击并确认 | 永久删除该日期记录。 |
| 排序/筛选 | 选择 | 按日期或当前提供的筛选查看，不改变记录本身。 |

日期以本地日历日保存，不从 Android 的系统统计或云端读取。

### W10. 诗词本与每日诗词

进入「侧栏 → 诗词本」可新增、编辑和删除正文/出处，建立带颜色的分类，把单篇诗词移入分类，并通过拖动手柄或键盘「上移/下移」调整分类与分类内顺序。删除分类时可选择把诗词移到「未分类」或连同分类内诗词删除，后者不可恢复。

「导入预设」列出与 Android 同源的 11 组离线初高中诗词，共 182 篇；选择后会按预设名称建立或复用分类，重复导入不会悄悄生成重复条目。首页「每日诗词」或诗词本中的每日诗词入口会请求受限 HTTPS API；请求有连接/读取超时和响应上限。

在线成功时更新本地缓存；失败时依次使用最近缓存和内置回退。打开详情后点「加入诗词本」会保存当前完整内容，重复条目会提示而不会悄悄新增多份。

### W11. 收藏夹

**完整路径**：侧栏 → 收藏夹。设置里的 **设置 → 子页面设置 → 收藏夹** 只提供安全说明和「打开收藏夹」入口。

第一次进入会看到「设置收藏夹密码」。密码至少包含 1 个 Unicode 字符，需再次输入确认，然后点「创建并解锁」。密码没有找回、重置或绕过渠道；忘记后无法恢复其中内容。

解锁后可进行以下操作：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「新增」/空状态中的「新增收藏」 | 单击 | 填写「正文」与可选「备注」，点「保存」后以密文写入 Windows SQLite。 |
| 「备注 / 日期」 | 单击切换 | 选择卡片附加显示备注或日期，不改变密文内容。 |
| 「复制正文」 | 单击 | 把解锁后的正文复制到系统剪贴板。 |
| 「在系统浏览器打开」 | 单击 | 仅对经过 Rust 校验的 HTTP(S) 链接显示；交给系统默认浏览器打开。 |
| 「编辑」 | 单击 | 修改正文或备注并重新加密保存。 |
| 「删除」 | 单击并确认 | 永久删除，不能从回收站恢复。 |
| 四点手柄 | 鼠标拖动；或按空格拾取、方向键移动、空格放下 | 调整顺序；也可使用卡片的「上移 / 下移」键盘按钮。按 `Esc` 取消键盘拖动。 |
| 「修改密码」 | 输入当前密码、新密码及确认后单击 | 全部条目成功解密并重新加密后才在一个事务中提交；中途损坏或并发变化不会部分改密。 |
| 「锁定」 | 单击 | 立即清除当前解锁会话，返回密码页。关闭应用后也需重新解锁。 |

收藏夹使用 PBKDF2-HMAC-SHA256（120,000 次）派生 AES-256-GCM 密钥，派生密钥只驻留在解锁会话内存中。这里描述的是 Windows 本机 Vault：正文、备注、密文、盐和校验元数据都不进入 Windows v29、自动备份、导入前恢复点或 WebDAV/S3 云同步。导入 Android v29 时也会清除其中的 Vault `active`/`pending`/`items`，不会拿 Android 密文覆盖 Windows Vault。

### W12. Android 手机使用时间的只读展示

进入 **侧栏 → 手机使用时间**。Windows 0.7.0 能显示按设备分组的 Android 规范历史，但它不是采集器：数据只能来自用户明确选择的兼容快照/只读链接，或从已启用的专用云 usage 对象下载；Windows 永不上传 usage，也不会调用 Windows 活动统计 API。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「导入快照」 | 选择兼容 Android 统计文件 | 把经过校验的多设备历史加密保存到 Windows 私有容器；之后与原文件脱离，原文件变化不会自动反映。 |
| 「链接只读文件」 | 选择兼容 Android 统计文件 | 保存只读来源并缓存最近一次有效快照。Windows 不写入、改名或删除源文件。 |
| 「刷新」 | 单击 | 仅对只读链接可用；重新校验源文件。源缺失、损坏、超限或读取中变化时保留上次有效快照，并显示「可能已过期 / 源文件已丢失 / 源文件无效」状态。 |
| 「7 天 / 30 天 / 90 天 / 全部」 | 单击 | 切换自然日范围。 |
| 「设备」选择框 | 选择单个设备或「所有设备」 | 按 Android 稳定设备 ID/名称查看单机或安全合并后的历史。 |
| 「应用」选择框 | 选择「全部应用」或包名 | 按选定包名过滤总览、趋势和排行；缺少可信名称/图标时回退为包名和通用图标。 |
| 总览卡 | 无（只读） | 显示总时长、日均、单日最高和近 7 日平均。 |
| 「每日趋势」与「应用排行」 | 无（只读） | 显示按日柱状图及当前范围内的包名排行；虚线柱表示当天仍可更新。 |

Windows 不申请系统使用情况权限、不调用 Windows 活动统计 API，也不采集这台电脑或手机的新活动。快照、只读链接路径及使用明细都不进入 v29、恢复点、自动备份或 WebDAV/S3 应用 JSON；专用云 usage 只允许下载和合并，不允许 Windows 上传。

### W12.1 健康（只读）

进入 **侧栏 → 健康**。页面只读取用户明确选择的兼容 Android 健康统计文件，不连接 Health Connect，也不调用 Windows 计步或健康 API。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「导入快照」 | 选择兼容文件 | 校验步数、距离、活动热量、日期、时区和 `OPEN`/`FINAL` 状态后保存到 DPAPI 私有缓存；以后不再读取原文件。 |
| 「链接只读文件」 | 选择兼容文件 | 保存只读来源和最近一次有效快照，绝不会修改、重命名或删除源文件。 |
| 「刷新」 | 单击 | 重新校验只读链接；源缺失、损坏、超限、重解析点或读取中变化时保留上次有效数据并标记过期。 |
| 「步数 / 距离 / 活动热量」 | 单击 | 切换总计、有效天平均、最高日和每日柱状图；无可信值显示「—」，不会伪造 0。 |
| 「7 天 / 30 天 / 90 天 / 全部」 | 单击 | 改变本地日历日范围，不修改源数据。 |

健康来源路径、明细与缓存不进入 v29、恢复点、自动备份或任何云同步。

### W12.2 笔记

进入 **侧栏 → 笔记**。第一次点「选择笔记库」并选择普通 Windows 文件夹；可以直接选择已有 Obsidian 仓库。笔记库与日记/媒体目录独立，Windows 不使用 Android `content://` 笔记 URI。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「选择/更换笔记库」 | 单击并选文件夹 | 保存受限根目录并列出其中的文件夹和 Markdown；更换根目录不会搬运旧文件。 |
| 「新建文件夹」/「新建笔记」 | 输入名称并确认 | 在当前文件夹创建；`..`、绝对路径、保留设备名、同名项和越界链接会被拒绝。 |
| 文件夹行 | 单击 | 进入文件夹；面包屑可返回任一级。 |
| 笔记行 | 单击 | 打开 CodeMirror 源码编辑器；「预览」显示 Markdown 与安全图片。 |
| 「插入图片」 | 选择图片 | 复制到当前笔记库内用户确认的位置并插入可移植相对链接；不会写入日记媒体目录。 |
| 「重命名」/「删除」 | 单击并确认 | 只操作当前笔记库内目标；删除不可从 DeskCubby 日记回收站恢复。 |
| 「保存」或 `Ctrl+S` | 单击/快捷键 | 比较 SHA-256 文件版本并回读验证；外部冲突时选择重新加载、明确覆盖或另存副本。 |

应用不会解析或改写陌生 Obsidian 语法。笔记正文/图片不进入 v29；跨设备请同步或复制整座笔记库。

### W12.3 阅读

进入 **侧栏 → 阅读**。点「打开 TXT / PDF」选择文件，文件会进入 Windows 本机受限书架。0.7.0 继续使用全高正文与单条紧凑控制栏，并将 PDF 换成 React-PDF 10.4.1（基于 PDF.js 5.4.296）：默认保留原稿，可连续滚动或单页翻页，并支持文字选择、链接、目录、整本搜索、旋转和密码文档。PDF worker、CMap、标准字体、ICC/WASM 均随应用离线打包，不依赖系统 PDF 查看器。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「打开 TXT / PDF」 | 选择 TXT/PDF | 校验扩展名、文件签名、大小、普通文件与重解析点边界后加入书架；重复选择同一原文件会打开既有记录，不复制正文。 |
| 阅读设置 → 书架布局 | 选择「紧凑列表」或「封面网格」 | 列表用较矮卡片提高密度；网格使用书本比例封面并可关闭封面下方的重复书名。 |
| 阅读设置 → 显示书架进度 | 开关 | 按 TXT 逻辑页或 PDF 实际页数显示进度条和百分比；文件内容变化后以后端重新测得的页数为准。 |
| 书籍卡片 | 单击 | 打开上次进度；TXT 按稳定逻辑页阅读，PDF 由 React-PDF/PDF.js 在应用内渲染。PDF 字节只通过含书籍 UUID 的受限只读协议按需读取，绝对路径不进入 React。 |
| 左上角返回箭头 | 单击 | 保存当前位置并返回书架；从书架移除与返回都不会删除原文件。 |
| 目录按钮 | 单击 | TXT 显示智能/自定义章节；PDF 显示文件内嵌目录。目录以浮层覆盖，不挤窄页面；无内嵌目录时会明确提示。选择条目跳页，`Esc` 关闭。 |
| 搜索按钮 / `Ctrl+F` | 输入并选择结果 | 搜索整本 TXT 或 PDF 文字层，显示结果序号并跳到上一/下一处；当前页文字层会标出匹配内容。扫描有查询长度和 5,000 条结果上限，不含文字层的扫描件不会伪造结果。 |
| 上一页/下一页、进度条、页码 | 单击、拖动或输入 | 更改并防抖保存位置。正文没有输入焦点时也可用 `←`/`PageUp`、`→`/`PageDown`/空格翻页，`Home`/`End` 到首尾。 |
| TXT 复制按钮 | 单击 | 把当前逻辑页写入剪贴板；失败时仍可手动选择文字后按 `Ctrl+C`。 |
| PDF `− / +` | 单击 | 以 10% 步长临时缩放当前 PDF；100% 表示适合当前阅读区宽度，设置中的「PDF 基准缩放」决定重新打开后的 50%–300% 默认值。 |
| PDF 旋转按钮 | 单击 | 当前会话顺时针旋转 90°；不写回或修改原文件。 |
| PDF 正文 | 拖动选择 / 单击链接 | 文字层可选择复制；批注中的外部链接以新窗口交给受限浏览器边界。密码 PDF 会弹出输入框，密码仅驻留本次打开会话且不保存。 |
| 专注模式按钮 | 单击 | 控制栏改为半透明悬浮，不占正文或 PDF 高度；鼠标移入或键盘聚焦时恢复清晰。再次单击或按 `Esc` 退出。可在阅读设置中开启「默认进入专注模式」。 |
| 阅读设置 → 颜色与页面 | 选择背景、自动/自定义文字色 | 白色、纸张、羊皮纸、护眼绿、夜间或自定义背景都会即时显示在预览中；对比度低于 4.5:1 时阅读区自动使用安全文字色并保留用户选值，防止文字与背景融为一体。 |
| 阅读设置 → PDF 显示 | 选择原稿/阅读配色、连续/单页、缩放与页间距 | 「保留原稿」是默认值，保留图片和彩色图表；「阅读配色」才把渲染副本按明暗映射成当前背景/文字双色。连续模式只保留当前页附近画布，长 PDF 不会一次渲染所有页面。 |
| 阅读设置 → TXT 排版 | 调整字体、字号、正文宽度、自然/两端对齐、行距、段距、首行缩进、字间距和页面留白 | 字体可选衬线、无衬线或等宽；每个段落明确继承阅读文字色，不再被全局次要文字样式覆盖。正文宽度 520–1280 px。 |
| 阅读设置 → 章节识别 | 选择智能/自定义模式、标题长度与整行正则 | 智能规则支持常见中英文章节；自定义 Rust 正则必须合法，保存失败不会覆盖原设置。 |
| 设置按钮、右上角「保存」/「恢复默认」 | 单击 | 书籍打开时设置以侧边浮层覆盖阅读区，底层内容停止键盘交互；所有控件先编辑本地草稿并即时更新颜色预览，恢复默认只改草稿，保存成功后才重开当前书并应用。带未保存更改关闭或离开会先确认。 |
| 书架卡片右上角删除 | 单击并确认 | 只从 Windows 私有书架移除记录与位置，不删除用户选择的 TXT/PDF。 |

阅读偏好保存在应用私有 `reader/reader-state-v1.json`；0.7.0 将内部 schema 升至 v4，并继续读取 v1/v2/v3。新增字段缺失时默认使用 PDF 原稿色、连续滚动、18 px 页间距、36 px 页面留白及无额外缩进/字距，不清空既有书架、路径、指纹、位置或累计时长。Reader IPC 升至 v3；PDF 地址仍显式固定为 `assetUrl`，前端仍只接受 `reader:` 与 `http://reader.localhost` 两种 UUID 受限只读形式。

书架、路径、书名、正文、显示偏好和阅读时长不进入 Android v29；只有不含 URI 的书籍指纹位置可以通过 v29 或可选阅读云对象合并。Windows 与 Android 使用相同指纹：`SHA-256("DeskCubby.ReaderBook.v1" + NUL + "TXT|PDF" + NUL + 完整文件字节)`。TXT 以段落位置作为跨端权威并映射到当前设备分页；PDF 使用页位置。导入内容完全相同的书后，会应用账本中的较新位置；仅书名相同而内容不同不会误命中。

### W12.4 RSS 订阅

进入 **侧栏 → RSS**。点「新增订阅」填写名称与公网 HTTPS Feed 地址；Windows 默认拒绝 HTTP、私网/回环解析、跨源不安全重定向和 XML DOCTYPE。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「新增订阅」/订阅卡「编辑」 | 填写名称、URL 并保存 | 校验并保存本机订阅配置；秘密和正文不会出现在错误信息。 |
| 「刷新全部」/订阅卡「刷新」 | 单击 | 在并发、连接/读取超时、重定向、条目数与响应体上限内读取 RSS 2.0/Atom。 |
| 订阅卡 | 单击 | 筛选该源文章；再次选择「全部」查看合并列表。 |
| 文章行 | 单击 | 经 Rust 校验后交给系统默认浏览器打开原始 HTTPS 链接。 |
| 「删除订阅」 | 单击并确认 | 删除订阅配置；不会向远端发送删除请求。 |

文章列表只保留在当前运行期；订阅配置可按 v29 兼容字段往返，但 Windows 不执行 Android 内置浏览器。

### W12.5 AI 聊天与热量模型

先到 **设置 → 子页面设置 → AI 配置** 新建 OpenAI-compatible 模型：填写名称、HTTPS Base URL、模型 ID、普通文本/视觉用途和 API Key。Key 依照产品格式是明文字段，会进入 Windows 设置与 v29；界面显示完整 Key，请把备份视为敏感文件。HTTP 只可在风险确认后用于可信本地服务。

进入 **侧栏 → AI**：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 模型选择 | 选择已配置聊天模型 | 决定下一条消息使用的端点；未配置时跳转 AI 设置。 |
| 「新对话」/历史列表 | 单击 | 建立新会话或打开本机历史；AI 会话不进入 v29。 |
| 图片/日记/小巧思上下文 | 选择后确认 | 生成当前会话的冻结快照；仅在点「发送」后随消息发往所选服务。 |
| 输入框 + 「发送」 | 输入并单击 | 通过受限非流式 `chat/completions` 请求发送；请求体/错误/日志不回显 Key。 |
| 「停止复用上下文」 | 新建对话 | 上下文会随同一会话后续请求复用；需要停止时创建新会话。 |
| 对话菜单「重命名/删除」 | 单击并确认 | 修改或永久删除 Windows 本机会话。 |

吃历中的 AI 热量是估算，不是医学或营养测量。图片以受限 data URL 发送，服务商和模型必须支持相应多模态消息格式。

### W12.6 小游戏与统计中心

进入 **侧栏 → 小游戏**，选择 4×4、5×5、6×6 2048、贪吃蛇、俄罗斯方块、自定义扫雷、单花色蜘蛛纸牌或本地双人围棋。页面会显示该游戏的键盘提示；按钮和键盘操作都经过同一引擎并在动作后保存存档。

- 2048：方向键移动；支持新局与单步撤回，三个棋盘尺寸分别保存最高分/存档。统计中的“总操作次数”包含未能改变棋盘的方向输入；“有效移动”只统计棋盘发生变化的操作，旧“失败次数”不再新增或展示。
- 贪吃蛇：方向键控制，不能直接反向；暂停/继续和重新开始都有按钮。
- 俄罗斯方块：方向键移动/软降，旋转与硬降使用页面提示键；新局会清空当前局。
- 扫雷：鼠标打开/插旗，双击数字可尝试展开周围；新局可选尺寸/雷数。
- 蜘蛛纸牌：点击/键盘选择并移动合法牌列，发牌与新局均会在不可恢复操作前确认。
- 围棋：新局可选 9/13/19 路；鼠标单击交叉点落子，也可用方向键/WASD 移动选择框、`Enter`/空格落子、`P` 停一手。引擎执行提子、禁自杀和简单劫；双方连续两次停着后结束。页面显示黑白双方提子数和最后一手，但不自动按地域或面积判断胜负，新局前会确认丢弃当前棋局。

进入 **侧栏 → 统计中心** 查看日记篇数/字数/连续记录、手机使用时间、健康、阅读、游戏时长及小游戏特色战绩；数据缺失显示未知，不会补成 0。既有七个游戏/变体的存档、最高分和特色统计按 v29 字段验证，2048 `moveAttempts` 可在两端往返；围棋显示最高提子以及落子、提子、停着、完成棋局，不计算胜率。围棋存档/统计/时长使用 Windows 私有表并排除 v29、恢复点、自动备份和应用 JSON 云同步；其他游戏累计时长也仍为 Windows 本机私有数据。

### W13. 设置、草稿与目录

进入「侧栏 → 设置」。设置主页分为「外观与语言」「子页面设置」「应用数据」「桌面导航」「关于」。可编辑子页先修改本地草稿；点右上角「保存」或按 `Ctrl+S` 才写入。点「恢复默认」只把本页默认值放入草稿，仍需保存；带未保存草稿离开时会要求保存、继续编辑或放弃。

主要路径：

| 完整路径 | 内容 |
| --- | --- |
| 设置 → 外观与语言 | 中文/英文、Material/Liquid Glass/Organic Future、主/副色、跟随系统/浅色/深色、字号、紧凑模式、全局背景图片/可见度/模糊和教学总开关。 |
| 设置 → 子页面设置 → 主页 | 用户名、双语问候、模块显隐/顺序/标题、卡片边框、饮食按钮图标和小游戏快捷入口；Windows 不提供网站模块。 |
| 设置 → 子页面设置 → 日记与媒体 | 选择/更换日记与媒体目录，并设置文件名、Markdown 模板、H1–H6 标题字号、图片尺寸/压缩和 EXIF 位置选项。 |
| 设置 → 子页面设置 → 小巧思 | 内容显示方式、重点颜色和编辑区高度。 |
| 设置 → 子页面设置 → 诗词本与吃历 | 诗词字号、行距、对齐、出处/装饰、七言换行，以及吃历图片高度、每行数量、说明和智能换行。Android 字体 URI 只兼容保留，不作为 Windows 字体路径。 |
| 设置 → 子页面设置 → 收藏夹 | 设置收藏卡片最小行高，并进入收藏夹；密码与密文不进入普通设置。 |
| 设置 → 子页面设置 → RSS 订阅 | 管理每个订阅的文章数量和摘要显示偏好，并进入 RSS 页。 |
| 设置 → 子页面设置 → AI 配置 | 新建/编辑/复制/删除 OpenAI-compatible 模型、系统提示词与完整 API Key。 |
| 设置 → 子页面设置 → 手机使用时间 / 健康 | 查看“只显示、不采集也不上传”的边界并进入对应页面。 |
| 设置 → 应用数据 | 选择自动备份目录；从「打开备份管理」进入 v29/恢复点，从「数据占用」查看存储说明，从「打开云端同步」进入 WebDAV/S3。 |
| 设置 → 桌面导航 | 管理全部 18 个主页面的显隐、同类顺序与所属分类；新建分类、编辑中英文名、调整分类顺序或删除分类。最后一个分类不能删除；删除其他分类时其中页面自动迁移到相邻分类。右上角保存后生效。 |
| 设置 → 关于 | 查看版本、GitHub、应用教学和更新状态。 |

更换目录不会自动搬运旧文件。需要迁移时请先在应用外复制完整目录，再选择新目录并刷新；保留媒体文件名和 `dc-media.json`。

### W14. v29 导入、导出与自动备份

从侧栏进入「备份」，也可通过 **设置 → 应用数据 → 打开备份管理** 进入：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「选择并预览」 | 单击并选择文件 | 接受不超过 64 MiB 的 Android v1–v29；严格校验后显示原版本、导出时间和结构化记录统计，旧格式在内存中补齐为 v29。尚不修改数据。 |
| 确认复选框 + 「确认导入」 | 核对后勾选并单击 | 先生成导入前恢复点，再事务替换 Windows 管理的设置、小巧思/分类、日期记录、诗词分类/诗词和小游戏结构化数据；失败完整回滚。 |
| 「选择位置并导出」 | 单击 | 生成 Android 可读的 v29 JSON，把 Windows 管理字段合并进已清理的兼容影子。 |
| 「立即运行」 | 单击 | 在已配置自动备份目录中执行 `dc.pending.json` → 回读校验 → `dc.json`，并把旧 current 轮换为 `dc.previous.json`。 |
| 恢复点列表 → 「恢复」 | 选择并确认 | 恢复核心结构化数据；恢复前会再建立当前状态的恢复点，失败不部分替换。 |

自动备份目录在 **设置 → 应用数据** 选择并保存。应用启动约 30 秒后检查一次，此后约每 5 分钟检查；内容没有变化时跳过轮换。自动备份与云同步是两套独立机制。

导入会检查数组数量、字符串长度、枚举、重复 ID、关联关系及 v29 专属结构。完整兼容数据先递归清除云凭据/DPAPI 载荷、两端 Vault、usage/health 明细和来源路径，再用当前 Windows 用户的 DPAPI 加密保存。Android 内置浏览器、桌面小卡片和未知字段仍可往返但不会执行；RSS/AI 普通配置由 Windows 页面管理，AI Key 按产品格式保持明文。

v29 新增部分会按以下规则处理：

- `CUSTOM/customTheme` 的全部字段先严格校验，再保存在 DPAPI 兼容影子中。Windows 当前使用其中的 `baseStyle` 映射到现有 Material/Liquid Glass/Organic Future 渲染；如果用户未主动改风格，导出时仍保持 `CUSTOM` 和全部合法参数。
- 根级 `readerProgress` 最多 500 条，只含指纹、TXT/PDF 类型、位置、总页数与更新时间。它会与 Windows 私有阅读账本按最新记录合并，不含书名、路径、URI、封面或正文。
- 桌面信息卡设计 `desktopWidgetConfigs` 会严格校验并按 Android 0.13.0 语义往返：v28 及更早导入时给每项补齐 `showName`/`backgroundOpacityPercent`/`showIcon`/`textAlignment`/`textScalePercent` 默认值，并把旧 `cloud_sync` 主页模块改写为 `cloud_sync_now`；`homeModuleId` 接受与 Android 同源的 21 个合法值。
- 2048 `moveAttempts` 正常导入、保存和导出；旧 `losses` 仍可读取和无损往返，但 Windows 不再新增或展示失败次数。
- 围棋不加入 v29 的既有七游戏白名单。Windows 的围棋存档、最高提子、特色统计与游玩时长位于独立私有表；导入 v29 不触碰这些表，导出、导入前恢复点、自动备份和应用 JSON 云同步也不会读取它们。

Android 导入的 `cloudSyncConfigs` 默认仍由兼容影子持有。只有用户在 Windows 新建、编辑、复制或删除云配置后，Windows 才接管这一非秘密字段并在后续导出中覆盖它；无论是否接管，影子、手动导出、自动备份、恢复点和应用 JSON 云上传前都会再次清理云凭据、两端 Vault、usage/health 明细和来源路径。Android 的 `diaryTreeUri`、`mediaTreeUri`、`notesTreeUri`、`poetryFontUri` 始终是不透明值，不会被 Windows 路径覆盖。

兼容影子不能跨 Windows 账户直接解密。正常跨设备迁移应导出 JSON；导出文件本身仍是未加密普通 JSON，可能保留 Android 原有的明文 AI API Key，请勿发到公开或共享位置。

### W15. WebDAV / S3 云同步

**完整路径**：设置 → 应用数据 → 打开云端同步。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「新增配置」 | 单击 | 选择 WebDAV 或 S3 兼容服务，填写配置名称、服务地址、远端目录和服务专属字段。 |
| 「启用此配置」 | 勾选 | 决定该配置能否手动或后台执行。 |
| 「日记 / 媒体 / 手机使用时间 / 应用 JSON / 阅读进度」 | 至少勾选一项 | 选择同步内容。应用 JSON 可能包含明文 AI API Key，远端对象没有端到端加密，界面会显示敏感提示；手机使用时间只读取 Android `usage/v1` 对象。阅读进度使用唯一对象 `reading/v1/progress.json`，新建配置默认勾选，旧配置不会被升级静默开启。 |
| 「仅上传 / 双向」 | 单击 | 仅上传不拉取；双向在双方均变化时保留冲突副本，不静默覆盖。 |
| 「允许 HTTP（仅可信局域网）」 | 勾选并在风险对话框确认 | 默认只接受 HTTPS；HTTP 可能暴露凭据和私人数据，仅用于你信任的局域网服务。 |
| 「本机凭据 → 保留 / 替换（填写） / 清除」 | 选择后保存配置 | WebDAV 密码或 S3 Access Key、Secret Key、可选 Session Token 由 Rust 使用当前用户 DPAPI 加密；前端只看到是否已配置。修改账号绑定字段后必须明确替换或清除旧凭据。 |
| 配置卡片「立即同步」 | 单击 | 只运行这一项；配置必须已启用并具有凭据。 |
| 顶栏「全部立即同步 / 取消同步」 | 单击 | 依次运行全部可用配置，或请求取消正在执行的同步。 |
| 「强制上传」 | 单击并在对话框再次确认 | 让同路径差异以本机扫描内容为准，可依次处理多个已启用且可用的端点；远端写入仍要求目标匹配本轮扫描版本。远端独有项目保留不删。 |
| 「强制下载」 | 单击并在对话框再次确认 | 只在恰好一个云端配置已启用且可用时开放，让同路径差异以该来源为准；本机写入仍要求文件匹配本轮扫描 SHA-256。并发本机修改会保留并产生冲突副本，本机独有项目保留不删。 |
| 配置卡片编辑 / 复制 / 删除 | 单击 | 复制会生成停用配置且不复制凭据；删除同时清除本机凭据，但不删除远端文件。 |
| 「云同步总开关」+ 右侧「保存」 | 修改并保存 | 只控制后台调度，手动「立即同步」不受影响。开启且存在可用配置时，启动约 2 分钟后尝试一次，之后按已保存间隔运行（默认 6 小时）。 |
| 「待确认的远端应用 JSON → 预览并恢复」 | 单击、核对统计后确认 | 后台只把远端 JSON 暂存到本机；用户确认前不会覆盖结构化数据。恢复不替换日记或媒体文件。 |

WebDAV 远端目录需预先存在并提供单个强 ETag；S3 兼容服务若返回未加引号、弱、多个或缺失 ETag，Windows 会先验证其条件请求语义，再用条件回读和内容哈希绑定验证器，任何一步不可靠都会失败关闭。普通同步与两种强制模式都限制单对象 64 MiB、单次 512 MiB、10,000 个对象和 10 分钟，不传播删除，也不适合作为整盘镜像；强制模式只改变双方同路径内容的取舍，不放宽条件写、扫描快照、HTTPS、路径或限额边界。强制下载取得的 `json/dc.json` 也只进入待确认区，必须预览后手动恢复。Windows 只下载并合并 Android `usage/v1` 设备对象，绝不上传；「仅上传」会跳过这些对象。usage/health 明细和两端 Vault 不进入应用 JSON，健康也没有独立云同步路径。

阅读进度对象是 v1、最大 512 KiB/500 条。双向模式按 `fingerprint + type` 和 `updatedAt` 合并，同一时间取更靠后的合法位置；并发时先合并本机账本，后续上传合并后的快照使各设备收敛。仅上传模式只上传本机合并账本。对象不含书名、路径、URI、封面、正文或阅读时长，但文件 SHA-256 仍可能被用于识别已知书籍；HTTPS 只保护传输，远端没有端到端加密。

### W16. 关于与应用更新

**完整路径**：设置 → 关于。页面显示当前 Windows 版本，并提供「GitHub 仓库」「应用教学」和「应用更新」。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「GitHub 仓库」/「应用教学」 | 单击 | 链接先由 Rust 白名单校验，再交给系统浏览器。 |
| 「启动时自动检查」 | 开关 | 仅 updater-enabled 生产包可用。开启后启动约 60 秒首次检查；上次尝试时间持久化，此后跨重启也不会在 24 小时内重复自动检查。自动检查只提示。 |
| 「检查更新」 | 单击 | 立即读取受信 HTTPS 更新清单；没有更新时显示「已是最新版本」。手动检查不依赖自动检查开关。 |
| 「下载、验证并安装」 | 检测到新版本后单击 | 安装前重新检查精确版本并验证 Tauri updater 签名；成功后安装并重启。请先保存日记与设置草稿。 |

检入的普通本地构建没有生产更新源和公钥，页面会显示「此构建没有配置可信更新源或更新公钥」，开关不可用，也不会发起 updater 网络请求。正式发布始终要求 Tauri updater 私钥、非空私钥密码、内嵌公钥、HTTPS endpoint 及可验证的安装包 `.sig`；缺任一项都会失败关闭。Authenticode 为可选发布者身份：未配置时脚本确认 EXE 为 `NotSigned` 后继续，SmartScreen 可能显示“未知发布者”；未来配置 PFX 或自定义签名命令时仍会自动签名并验证证书链、可信时间戳及可选 signer subject。Windows 0.1.0 没有 updater 插件，必须先手动安装第一个 updater-enabled 版本。

版本 tag 固定为 `windows-vX.Y.Z`，0.5.0 对应 `windows-v0.5.0`。GitHub Actions 构建后创建 Draft Release，并精确上传 `DeskCubby-0.5.0-windows-x64-portable.exe`、`DeskCubby-0.5.0-windows-x64-setup.exe`、`DeskCubby-0.5.0-windows-x64-setup.exe.sig`、`SHA256SUMS.txt`、`latest.json` 五个资产；人工复核发布后，才把已验证 `latest.json` 提升到独立 `windows-stable` 更新通道。

### W17. Windows 0.7.0 功能边界

- 不提供 Android 内置浏览器和桌面小卡片；这些字段可在清理私有值后的 DPAPI 兼容影子中往返，但 Windows 不展示或执行。
- 两端都提供本地双人围棋，但棋局与战绩不通过 v29 交换。Windows 围棋的存档、最高提子、特色统计和游玩时长还从恢复点、自动备份和应用 JSON 云同步结构性排除，只保存在当前 Windows 用户的 SQLite 私有表中。
- 手机使用时间与健康只显示、不采集。usage 只接受用户显式选择的来源或下载 Android 专用云对象，Windows 绝不上传；health 不进入任何云路径。
- Windows 收藏夹是本机独立保险库；导入 Android v29 时也清除 Android Vault `active`/`pending`/`items`。两端 Vault、usage/health 明细、来源路径和云凭据都不进入兼容影子、v29、恢复点、自动备份或应用 JSON 云上传。
- 不读取或共享 Android Room 数据库；结构化记录只通过 v1–v29 JSON 交换并统一导出 v29。日记、媒体、笔记库、阅读书架与正文不在应用 JSON 中，跨电脑迁移时需另行同步或复制相应目录；只有 URI-free 书籍指纹进度可以通过 v29 或独立云对象合并。
- Windows 业务联网包括每日诗词、RSS、用户配置的 AI、WebDAV/S3，以及生产包中配置的 updater；各自使用独立受限 Rust 网络边界。普通本地构建与核心记录功能仍可离线使用。

---

## 1. 主页

**如何到达**：应用启动后默认进入，或点击底部导航栏「首页」标签。页面是一个可上下滑动的模块列表；显示哪些模块、模块顺序、是否显示模块标题和卡片边框，都由 设置 → 子页面设置 → 主页 中的模块配置决定。

### 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 问候语标题 | 无交互（纯展示） | 默认内置 24 套简洁、中性的中英句式，按本地日期稳定轮换：同一天重新进入仍是同一句，连续 24 天后才循环。顶栏会随文本增高并完整换行显示，不再附加固定的 “DeskCubby” 副标题。前往 设置 → 子页面设置 → 主页 → 主页问候，可修改用户名、复制 `{name}` 占位符，也可新增、修改或删除问候语；模板中的 `{name}` 会替换为用户名。若删除全部自定义问候，主页显示「今日概览 / Today's overview」。页面底部会以提示条（Snackbar）弹出各类操作结果。 |

### 展示类模块（日历、天气、今天、连续记录、本月日记、日记总字数、年度进度）

| 模块/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「日历」本月月历 | 无交互（纯展示） | 显示当月完整日历（星期行"一"到"日"），今天的日期以主题色高亮。日期不可点击。 |
| 「天气」太阳图标 + "离线模式" / "暂无上次天气缓存" | 无交互（纯展示） | 当前版本只显示离线占位文案，不联网取天气，没有任何按钮。 |
| 「今天」日期文字 | 无交互（纯展示） | 按当前应用语言格式显示今天的完整日期（如"2026年7月26日 星期日"）。 |
| 「连续记录」"N 天"大字 | 无交互（纯展示） | 显示连续写日记的天数：从今天（若今天没写则从昨天）起向前连续有日记的天数。 |
| 「本月日记」"N 篇"大字 | 无交互（纯展示） | 统计本月（按日记日期）已写的日记篇数。 |
| 「日记总字数」数字 | 无交互（纯展示） | 显示全部已索引日记的字数总和。 |
| 「年度进度」进度文字 + 进度条 | 无交互（纯展示） | 显示今年已过天数占全年的百分比（自动区分闰年 366 天），如"56% · 第 207 / 365 天"。 |

### 「每日诗词」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 诗句正文与出处文字区域 | 单击 | 弹出以当前**诗词名称**为标题的对话框，显示全诗内容与出处（含朝代），不再用「完整诗词」作固定标题。若尚未取得全诗，先显示当前单句并提示"完整内容会在下次刷新诗词时获取。"。无网络或未刷新时显示内置的兜底诗句。 |
| 刷新图标按钮（换一句） | 单击 | 按日期与当天刷新次数轮换三个 HTTPS 来源：今日诗词 `v2.jinrishici.com`、Hitokoto 诗词分类 `v1.hitokoto.cn/?c=i`、古诗词·一言 `api.gushi.ci/all.json`。每个来源都采用有限次数请求并避开当天已展示内容；全部在线请求失败或重复时，继续从内置 182 篇诗库选择，所以不会再弹出“暂无未看过的新诗词”。应用当天再次启动也不会自行换掉当前诗句。 |
| 发送图标按钮（加入诗词本） | 单击 | 把当前诗（优先保存全诗，没有全诗则保存单句）连同出处保存进诗词本，成功提示"已加入诗词本"，失败提示"诗词保存失败"。 |
| 诗词详情对话框内「加入诗词本」 | 单击 | 同样保存到诗词本，并关闭对话框。 |
| 诗词详情对话框内「关闭」 | 单击（或点对话框外部） | 关闭对话框，不做保存。 |

### 「日期记录」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「添加目标日期」按钮（仅当还没有任何日期记录时显示） | 单击 | 跳转到日期记录页面新建目标日期。 |
| 日期记录条目（图标 + 倒计时/累计文字 + 日期，最多 4 条：最近 2 条未来日期 + 最近过去的 2 条） | 单击 | 跳转到日期记录页面。文字随日期自动变化：未来"还有 N 天到 ××"、过去"距离 ×× 已经过去 N 天"、当天"今天就是 ××"。未设置图标的条目显示默认 🎯。 |
| 「查看全部」按钮 | 单击 | 跳转到日期记录页面查看全部记录。 |

### 「最近日记」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 最近 3 篇日记条目（文件名 + 日期） | 单击 | 打开对应日记进入阅读/编辑。没有任何日记时显示"还没有日记"文字（不可点击）。 |

### 「最近小巧思」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 最近 3 条小巧思文字（单行省略显示） | 无交互（纯展示） | 预览最近记录的 3 条小巧思内容。 |
| 「查看全部」按钮 | 单击 | 跳转到小巧思页面。 |

### 「快速输入」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 输入框（占位文字"记一条小巧思"） | 输入文字 | 输入要保存的小巧思内容；内容为空时发送按钮呈灰色不可用。 |
| 圆形发送按钮（纸飞机图标） | 单击 | 把输入内容保存为一条"未分类"小巧思；成功后清空输入框并自动收起键盘。提交过程中按钮暂时禁用防止重复发送。 |
| 圆形发送按钮（纸飞机图标） | **长按** | 弹出「选择分类并发送」对话框：列表包含"未分类"和已创建的各分类（带颜色圆点），点选任意一项即按该分类保存并发送；点「取消」或对话框外部放弃发送。前提：输入框内容非空，否则长按无反应。 |

### 「日常记录」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「添加」按钮（仅当还没有任何日常事件模板时显示） | 单击 | 跳转到日常记录页面创建事件模板。 |
| 每条事件的多行输入框（预填模板文字，如"喝水 xx 杯"，xx 以主题色下划线高亮，最多 100 字，显示 2–6 行） | 单击 xx 处 / 输入 / 回车 | 点击 xx 占位符会自动整体选中；回车会插入真实换行，适合一次记录多件事。发送中输入框禁用。 |
| 每条事件右侧的发送按钮（实心圆形，纸飞机图标） | 单击 | 把完整多行内容原样追加写入今日日记，成功后提示"已添加到今日日记：……"并把输入框恢复为模板原文；失败提示具体错误且保留输入。发送中按钮转圈并禁用。前提：需先在设置中选择日记目录；内容为空时按钮禁用。 |
| 右下角「查看全部」/「管理日常事件」按钮 | 单击 | 跳转到日常记录页面。主页最多展示 4 条模板：超过 4 条时按钮文字为"查看全部"，否则为"管理日常事件"。 |

### 「饮食图片」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 6 个餐别按钮：早餐 / 午餐 / 下午茶 / 晚餐 / 水果 / 夜宵（设置中开启"图标模式"后显示可自定义表情，如 🥪🍱🍹🍜🍊🍤） | **单击** | 直接调起相机拍照，拍完自动存入媒体目录并插入今日日记，成功提示"××图片已加入今日日记"。前提：需先在设置中同时选择日记目录和媒体目录，否则提示"请先在设置中选择日记目录和媒体目录"；相机打不开时提示"无法打开相机"。上传期间所有餐别按钮禁用。 |
| 同一组餐别按钮 | **长按** | 打开系统照片选择器，选择一张图片后自动按同样流程加入今日日记；选择器打不开时提示"无法打开照片选择器"。前提同上。 |
| 模块底部提示 | 无 | 不再显示“单击选图，长按拍照”等操作提示；上传期间按钮禁用，热量估算仍在后台按原设置执行，失败不影响图片已保存。 |

### 「随机旧日记」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 随机一篇日记的文件名按钮 | 单击 | 打开这篇随机抽取的旧日记回顾；抽取结果在日记列表变化时才会更换。没有日记时显示"还没有可回顾的日记"（不可点击）。 |

### 「网站快捷入口」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 带地球图标的网址胶囊按钮 | 单击 | 跳转到应用内的浏览器页面并打开该主页网址。显示的网址可在设置中修改。 |

### 「笔记」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「打开」 | 单击 | 进入 Obsidian 兼容的 Markdown 笔记页；尚未选择笔记库时会先显示「选择笔记库」。笔记库与日记目录互不替代。 |

### 「小游戏」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 已启用的游戏胶囊按钮 | 单击 | 绕过小游戏列表，直接进入对应游戏；可显示 2048 · 4×4、2048 · 5×5、2048 · 6×6、贪吃蛇、俄罗斯方块、扫雷、蜘蛛纸牌和围棋中的任意组合，返回后仍回到正常应用导航。 |
| 「前往主页设置选择要显示的小游戏」提示 | 单击 | 仅在八个快捷入口全部关闭时显示；进入 **设置 → 子页面设置 → 主页** 重新选择。 |

### 「记录概览」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 日记 / 小巧思 / 日期数字 | 无（只读） | 显示当前已加载的日记篇数、小巧思条数和日期记录数。 |
| 「查看统计」 | 单击 | 打开统一统计中心，继续查看日记、使用时间、健康、阅读与小游戏统计。 |

### 「立即同步」与「强制上传/下载」模块

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 两张卡的状态文字与进度条 | 查看 | 两张卡共享“云端同步尚未开启”“没有已启用的同步服务”“已就绪”“已加入队列”“正在同步 x/y”、最近消息或安全错误；运行时显示进度条，也显示上次完成时间和待确认应用 JSON 数量。不会显示服务地址、路径、正文或凭据。 |
| 「立即同步」卡中的按钮 | 单击 | 把安全合并加入与设置页、系统桌面同步组件共用的串行 WorkManager 队列。需先在 **设置 → 应用数据 → 云端同步** 开启总开关并启用至少一个服务；运行中或动作刚加入队列时按钮暂不可用。 |
| 「强制上传/下载」卡中的「强制上传」 | 单击并确认 | 先显示“本机优先”的影响说明；确认后排队强制上传。同路径差异以本机扫描版本为准，保留远端独有项目，不传播删除。WebDAV 仍严格执行远端条件版本；S3 条件头是最佳努力并保留内容 SHA-256 校验。 |
| 「强制上传/下载」卡中的「强制下载」 | 单击并确认 | 先显示“云端优先”的影响说明。只有恰好一个已启用云端来源时可用；确认后排队，保留本机独有项目且不传播删除。本机在扫描后又发生修改时仍按同步冲突流程保留；多个来源时直接提示需要唯一来源。 |

这是应用首页中的两个普通模块，不是 Android 系统桌面小组件。可在 **设置 → 子页面设置 → 主页** 中分别排序、移除、重新添加或关闭标题。0.13.0 会把旧 `cloud_sync` 在原位置展开为“立即同步”再“强制上传/下载”；若用户此前已移除旧模块，则不会重新添加。v31 备份导入沿用同一迁移规则。

### 小技巧

- 快速输入的发送按钮支持**长按**：长按弹出「选择分类并发送」对话框，可以在保存小巧思的同时直接指定分类；单击只会存为"未分类"。
- 饮食图片的每个餐别按钮都有双重手势：**单击直接拍照，长按打开相册选图**；模块不再显示额外的操作提示语。
- 日常记录输入框里的 xx 是可替换占位符（带主题色下划线）：直接点一下 xx 就会整体选中，打字即可替换成数字，不必手动拖选。
- 每日诗词的正文区域本身可以点击，会弹出全诗对话框。今日诗词与内置诗库通常能提供全文；只返回单句的来源会保留单句，不会拿另一首诗的正文替换。
- 「日期记录」模块里的每一行条目都可以整行点击跳转到日期记录页，不只是底部的「查看全部」按钮。
- 主页显示哪些模块、模块顺序、是否显示模块标题和卡片边框，都在设置的主页模块配置里调整；「小游戏快捷入口」还可独立选择模块内的八个游戏按钮，日常记录模块在主页最多只显示前 4 条模板。围棋快捷入口只保存在 Android 本机，不进入 v31。
- 首次安装时主页默认预置 10 个模块：今天日期、每日诗词、快速输入、饮食图片、年度进度、笔记、小游戏、记录概览、立即同步、强制上传/下载；本章介绍的其余模块（日历、天气、日期记录、最近日记等）需到 **设置 → 子页面设置 → 主页** 的「添加模块」里手动添加。两个同步模块之后仍可分别移除、排序或关闭标题。
- 饮食图片和日常记录都会直接写入"今日日记"文件：饮食图片需要先选好日记目录和媒体目录，日常记录至少需要日记目录；热量估算另需在日记设置中配置图片识别模型和文字模型。

---

## 2. 日记（列表与回收站）

**如何到达**：底部导航栏点击「日记」进入日记列表页。列表页顶栏的垃圾桶图标打开日记回收站对话框；日历图标进入吃历页（见第 4 章）。

### 列表页 · 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 刷新 | 单击 | 重新扫描日记目录，刷新按月份分组的日记列表。 |
| 新建 | 单击 | 弹出"新建日记"对话框，输入标题后确定；文件自动命名为"当天日期 标题.md"（如 2026-07-26 标题.md），重名自动加 (2) 序号，按设置中的 Markdown 模板生成内容并直接进入编辑器。前提：已在设置中选择日记目录。 |
| 日记回收站 | 单击 | 刷新并打开"日记回收站"对话框，列出被软删除的日记（按删除时间倒序）。 |
| 吃历 | 单击 | 跳转到吃历页面，按日期汇总日记中标注了餐别的照片。 |

### 列表页 · 空状态与悬浮按钮

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 一键设置默认目录 | 单击 | 未选择日记目录时显示“设置日记目录”空状态。系统 SAF 文件夹选择器会定位到本机 Documents；确认一次读写访问后，应用创建或复用 `Documents/deskcubby/diary` 与 `Documents/deskcubby/media`，验证两个子目录均可访问后才同时保存。创建中按钮显示“正在创建…”，取消系统选择器不会改变设置。 |
| 手动选择目录 | 单击 | 从同一空状态进入 **设置 → 子页面设置 → 日记与媒体**，分别选择已有的日记和媒体目录。 |
| 进入今日日记（空状态按钮） | 单击 | 列表为空且无错误时显示"这里还没有日记"，此按钮直接创建/打开今日日记。 |
| 重试 | 单击 | 读取失败显示"无法读取日记"时出现，重新扫描目录。 |
| 进入今日日记（右下角悬浮按钮） | 单击 | 打开今天的日记：当天文件已存在则直接打开，不存在则按设置的文件名模式（默认"yyyy-MM-dd 日记"）和 Markdown 模板自动创建后打开。前提：已选择日记目录。 |

### 列表页 · 月份分组列表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 月份行（显示月份和篇数） | 单击 | 展开/收起该月的日记；同一时间只展开一个月份，点开新月份会自动收起之前的月份，再点已展开的月份可收起。 |
| 日记条目卡片（显示文件名和字数） | 单击 | 打开该日记进入编辑器。 |
| 日记条目卡片 | **长按** | 弹出该条目的操作对话框（重命名 / 删除）。 |
| 更多（条目右侧竖三点图标） | 单击 | 与长按等效，弹出该条目的操作对话框。 |

### 列表页 · 条目操作对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 重命名 | 单击 | 打开"重命名文件"输入对话框，确定后改名（自动补 .md 后缀）；目录中已有同名日记会失败并以底部提示条报错，成功后提示"已重命名为 xxx"。 |
| 删除 | 单击 | 打开删除确认对话框："文件将安全复制到日记目录内的回收站，校验成功后才删除原文件。"按"移入回收站"执行软删除（备份校验成功才删原件），成功后提示"已移入日记回收站"；"取消"关闭。 |
| 确定 / 取消（重命名、新建对话框通用） | 单击 | 确定仅在输入非空时生效并自动去除首尾空格；取消关闭对话框。 |

### 日记回收站对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 恢复 | 单击 | 把该日记从回收站还原回日记目录；若目录中已有同名文件，自动改名为"原名 (恢复 2).md"，成功后提示"日记已恢复"。 |
| 永久删除（垃圾桶图标） | 单击 | 弹出"永久删除？"二次确认（提示"xxx 将无法恢复。"）；确认后彻底删除文件并返回回收站列表，取消也返回回收站列表。 |
| 完成 | 单击 | 关闭回收站对话框；回收站为空时对话框显示"回收站为空"。 |

### 小技巧

- 长按日记条目会弹出重命名/删除菜单，与点条目右侧的"更多"竖三点图标效果完全相同。
- 月份行同一时间只展开一个月：点开新月份自动收起旧月份，再点已展开的月份可收起。
- 删除是软删除：文件先复制到日记目录内的隐藏文件夹 `.DeskCubby Trash` 并校验成功后才删原件；从回收站恢复时若重名会自动改为"原名 (恢复 2).md"；回收站内"永久删除"不可恢复。

---

## 3. 日记编辑器

**如何到达**：在日记列表点击某篇日记，或点击"进入今日日记"，或从主页"最近日记"/"随机旧日记"模块打开。

### 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回（左上角箭头） | 单击 | 先立即保存再返回列表页；系统返回手势/返回键行为相同。 |
| 标题下方状态栏（只读文字） | 无（仅显示） | 实时显示保存状态："已保存 / 未保存 / 正在保存… / 发现外部修改"，以及当前模式"Markdown 源码 / 阅读预览"。 |
| 预览 / 源码（书本或代码图标） | 单击 | 在 Markdown 源码编辑模式和阅读预览模式之间切换；源码模式下图标提示"预览"，预览模式下提示"源码"。 |
| 保存 | 单击 | 立即手动保存（有未保存修改时写盘）；平时停止输入约 1.2 秒也会自动保存，保存中顶部显示进度条。 |

### 正文（Markdown 源码模式）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 正文输入框（等宽字体，空白时显示"开始记录…"） | 输入文字 | 直接编辑 Markdown 源码；普通文字使用完整编辑宽度，不再为每一行统一预留媒体按钮空间。内容变化后约 1.2 秒自动保存，光标移动时页面自动滚动保持光标可见。 |
| 四点拖动手柄（图片 Markdown 行右侧） | **按住垂直拖动** | 只在独占一行的图片 Markdown 行尾部局部覆盖；上下拖动整行图片 Markdown 调整其在文中的位置。拖动时该行半透明跟随手指，一条主色横线指示插入位置，松手后完成移动。无障碍模式下提供"上移 / 下移"操作。 |
| 删除媒体（图片 Markdown 行右侧垃圾桶） | 单击 | 只覆盖对应媒体行的右侧，不压缩其他文本；打开删除确认框，确认后删除媒体目录中的真实文件，并移除当前日记中指向同一文件的全部 Markdown 图片引用。 |

### 正文（阅读预览模式）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| Markdown 正文 | 上下滑动 / 点击安全链接 | 按 CommonMark 保留 H1–H6 标题、粗体、斜体、列表、引用、代码和链接等排版，不再把所有格式压成同一字号纯文本。只允许 `http`、`https` 与 `mailto` 链接交给系统处理；标题字号可在 **设置 → 子页面设置 → 日记与媒体 → Markdown 阅读预览** 分别调整。 |
| 照片下方的地点行（定位图标 + 地名/经纬度） | 无（只读） | 如果媒体目录的 `dc-media.json` 已记录该照片的拍摄地点，会在照片下方显示，最多两行；没有地点元数据时整行不出现。旧名 `deskcubby-media.json` 仍会兼容读取。要为新导入照片记录地点，需先在 设置 → 子页面设置 → 日记与媒体 打开「记录照片拍摄地点」，且照片本身含可读 EXIF 位置；已有元数据即使后来关闭开关仍会显示。 |
| 图片下方说明文字按钮 | 单击 | 打开"修改图片说明"对话框编辑该图片的说明（写回 Markdown 的 `![说明]` 部分）；说明为空时按钮显示"点击添加图片说明"。 |
| 删除媒体（照片旁垃圾桶） | 单击 | 与源码模式相同，先显示不可恢复确认，再删除当前日记内该媒体的全部引用和媒体目录中的文件。 |

### 底部工具栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 上传媒体 | 单击 | 打开系统图片选择器，选中后图片复制进媒体目录并在正文末尾追加一行图片 Markdown。前提：需先在设置中选择媒体目录，否则报"请先在设置中选择媒体目录"。 |
| 上传媒体 | **长按** | 弹出餐别菜单：早餐 / 午餐 / 下午茶 / 晚餐 / 水果 / 夜宵，选后再挑图片；带餐别的图片会出现在吃历中，且若设置里开启了图片压缩会压缩为 JPG。若同时开启热量估算，会自动调用 AI 估算，并把结果写入媒体目录的 `dc-media.json`，不会改写图片说明（前提：已在日记设置中选择图片识别模型和文字模型）。 |
| 日常记录 | 单击 | 跳转到日常记录页面（可从那里把记录追加到当前日记）。 |

### 弹窗

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| "文件已在外部修改"（冲突对话框） | 单击按钮 | 当磁盘内容与打开时不同（如被 Obsidian 修改）时弹出且不可点外部关闭，此时自动保存暂停。"加载磁盘版本"放弃当前编辑改用磁盘内容；"强制覆盖"用当前编辑内容覆盖磁盘。 |
| 「删除这个媒体文件？」 | 单击「删除」或「取消」 | 「删除」会移除当前日记中该文件的全部引用和媒体文件，且没有回收站；删除前会检查日记 SHA-256，并同步清理可写的 `dc-media.json` 对应元数据。若媒体本来已缺失，则只清理悬空引用；若文件删除失败，应用会恢复日记引用并报告失败。 |
| 操作失败对话框 | 单击"知道了" | 关闭错误提示（读写失败、媒体目录未选等错误在此显示）。 |

### 小技巧

- 编辑器无需手动保存：停止输入约 1.2 秒自动保存，按返回键、返回按钮或离开页面时也会自动保存。
- "上传媒体"按钮有隐藏长按功能：单击是普通插图，**长按才能选餐别**（早餐/午餐/下午茶/晚餐/水果/夜宵）——只有带餐别的照片才会进入吃历。
- 源码模式里图片 Markdown 行右侧的四点手柄**按住即可拖动**（无需长按触发），拖动时的主题色横线就是松手后的插入位置。
- 源码和预览中的垃圾桶都会删除**真实媒体文件**而不只是隐藏图片；确认前请先核对文件名。当前日记中同一文件出现多次时会一次清除全部引用。
- 预览模式下图片说明是可点的按钮，说明为空时显示"点击添加图片说明"。
- 阅读预览中的地点是媒体侧车 JSON 的只读显示，不会写回 Markdown；旧照片没有地点时可继续正常预览。
- H1–H6 的字号是阅读显示偏好，不会改写 Markdown 源码；笔记页面共用同一组字号。
- 文件被外部工具（如 Obsidian）修改后会弹冲突对话框且自动保存暂停，必须选择"加载磁盘版本"或"强制覆盖"之一。
- 插入图片前必须先在设置中选择媒体目录，否则会提示"请先在设置中选择媒体目录"。

---

## 笔记页面（Android）

**如何到达**：底部导航「导航」→「笔记」。笔记默认收纳在导航页；也可在 **设置 → 底部导航** 把它加入底栏，或在主页启用「笔记」模块后点「打开」。首次进入点「选择文件夹」，选择一个 Obsidian 仓库或普通文件夹作为笔记库。

笔记与日记的定位不同：它没有吃历，也不按日期或月份分组；应用直接浏览笔记库里的真实目录层级。笔记正文和媒体不会复制到 DeskCubby 私有目录，非 Markdown 文件也不会被删除或改名。

### 笔记库与文件夹浏览

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「选择笔记库 / 选择文件夹」 | 单击后在系统文件夹选择器确认 | 持久授权并原地打开 Obsidian 仓库或普通目录。以后顶栏的文件夹图标可切换笔记库；切换不会搬运旧库内容。 |
| 面包屑胶囊 | 单击任一级 | 直接回到该级文件夹；左上角返回箭头和系统返回先回上一级，位于根目录时才离开页面。 |
| 文件夹卡片 | 单击 | 进入子文件夹。列表始终先列文件夹，再列 `.md` 文件，各组按不区分大小写的正常名称顺序显示，而不是按日期排序。 |
| Markdown 笔记卡片 | 单击 | 打开笔记编辑器；列表显示文件名（隐藏末尾 `.md`）和大小。非 Markdown 文件留在磁盘上，但不出现在此列表。 |
| 顶栏「新建文件夹」 | 单击并输入名称 | 在当前目录创建子文件夹；重名、路径分隔符、`.` / `..` 和 Windows 保留设备名会被拒绝。 |
| 顶栏「新建笔记」 | 单击并输入名称 | 在当前目录创建 UTF-8 Markdown 文件并立即打开；没有输入 `.md` 时会自动补上。 |
| 条目右侧菜单 →「重命名」 | 单击并输入名称 | 由当前 SAF 文档提供方重命名文件夹或笔记；不会静默覆盖同名目标。 |
| 条目右侧菜单 →「删除」 | 单击并再次确认 | 删除所选笔记；删除文件夹会连同其中全部内容交给存储服务删除。笔记页没有回收站，此操作不可撤回。 |

### 笔记编辑器

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| Markdown 源码输入区 | 输入 | 原样编辑 UTF-8 Markdown；停止输入约 900 ms 后自动保存，顶栏会显示「未保存 / 正在保存 / 已保存」。单个笔记读取上限为 4 MiB。 |
| 顶栏「预览 / 源码」 | 单击 | 切换共享阅读预览与源码。预览支持标题、粗体、斜体、列表、引用、代码、安全链接、标准 Markdown 图片和 Obsidian `![[Wiki 嵌入]]`；H1–H6 字号与日记设置共用。 |
| 顶栏「保存」 | 单击 | 立即保存当前草稿；返回页面时也会发起保存。写入前比较 SHA-256 文件版本并在写后回读验证。 |
| 底部「上传媒体」 | 单击后依次选择图片和目标文件夹 | **每次上传都必须重新选择存储位置**。第二次系统选择器中只能选择当前笔记库内部的文件夹；选择库外目录会拒绝。复制并回读 SHA-256 成功后，应用在正文末尾插入从当前笔记到媒体文件的可移植相对 Markdown 链接。 |

如果 Obsidian 或其他应用在本次编辑期间修改同一笔记，自动保存会暂停并弹出「文件已在外部修改」：

- 「加载磁盘版本」：放弃当前草稿，读取外部最新内容。
- 「明确覆盖」：用户明确同意后，以当前草稿覆盖外部版本。
- 「另存 DeskCubby 冲突副本」：保留外部原文件，把草稿保存为新的冲突副本，适合稍后在 Obsidian 中对比合并。

笔记库 URI、Markdown 标题字号和主页入口会进入 v31 设置备份，但笔记正文、文件夹和媒体字节不会进入 JSON。跨设备恢复后如果新设备没有原 URI 的持久访问权，需重新在笔记页选择仓库。日记媒体目录不会被笔记上传复用，这样一个仓库可按自己的附件规则管理媒体。

---

## 4. 吃历（含滤镜、筛选与长图导出）

**如何到达**：底部导航「日记」→ 日记列表顶栏最右侧「吃历」日历图标进入吃历页；在吃历页顶栏**长按**「照片滤镜」魔棒图标进入「吃历滤镜」设置页。首次进入时会对日记目录和媒体目录各做一次 SAF 子项元数据快照；URI、名称、修改时间和大小未变化的日记继续复用可重建图片引用缓存。从吃历滤镜或热量进度页返回会立即显示已加载结果，不再重复扫描；应用内保存/重命名/删除/恢复日记、删除媒体引用或从首页新增饮食照片后，下次进入会自动刷新。

### 吃历页 · 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回（左上角箭头） | 单击 | 返回日记列表页。 |
| 估算所有未计算热量（计算器图标） | 单击 | 弹出「计算热量」对话框；确认后按日期把所有尚无热量数据的饮食照片加入队列。不同日期依次处理；同一天最多 3 张图片并行识别，全部识别成功后只调用一次文字模型统一计算。任务直接使用已加载的吃历列表，不会为了定位同一张照片反复扫描日记。前提：需在 设置 → 子页面设置 → 日记与媒体 →「AI 热量估算」中开启『上传饮食图片后自动估算』，并在 AI 配置中添加文字模型和图片模型；未满足时此按钮不显示。 |
| 估算所有未计算热量（计算器图标） | **长按** | 进入「热量估算进度」页；即使当前没有任务，也可查看空状态。任务运行时计算器按钮会高亮，不再用吃历顶部持续移动但无法判断进度的细进度条表示状态。 |
| 导出长图（下载图标） | 单击 | 打开「导出吃历长图」对话框，选择以天为单位、首尾均包含的开始/结束日期，然后通过系统文件选择器保存 PNG。没有可见照片时按钮置灰；正在导出时按钮禁用且页面顶部显示进度条。详见下方「导出吃历长图」。 |
| 筛选餐别（漏斗图标） | 单击 | 弹出「筛选餐别」勾选对话框（见下）。当有筛选生效（未全选）时该图标变为主题色高亮。 |
| 照片滤镜（魔棒图标） | 单击 | 开/关照片滤镜：开启后全部吃历照片（含放大查看）实时套用已保存的滤镜效果，按钮出现圆形底色高亮；再次单击关闭。 |
| 照片滤镜（魔棒图标） | **长按** | 进入「吃历滤镜」设置页，可调整亮度、对比度、饱和度、色温、色调。 |
| 刷新 | 单击 | 强制绕过并重建图片引用缓存，重新读取日记与媒体目录。外部编辑后若文档提供方没有正确更新修改时间，可用此按钮取得最新内容。加载中且已有内容时顶部显示细进度条。 |

### 热量估算进度页

**如何到达**：底部导航「日记」→ 日记列表顶栏「吃历」→ **长按**顶栏计算器图标。

| 区域/控件 | 操作 | 效果 |
| --- | --- | --- |
| 顶部总进度 | 查看 | 显示本轮已结束天数 / 总天数；一天只有在所有选中照片完成并成功保存，或明确失败后，才计入已结束。若某天失败，后续日期仍继续。 |
| 「正在处理」卡片 | 查看 / 单击 | 显示具体日期与当前阶段。图片阶段显示正在并行处理几张、已完成几张；随后显示『统一计算当天热量』和『正在保存当天结果』。图片识别阶段进度条按完成张数推进，统一计算/保存阶段使用活动进度条；单击可打开全部并发图片请求及全日文字请求的实时详情。 |
| 「排队」卡片 | 查看 | 显示等待的日期、队列位置和图片数量。某一天正在计算时，可返回吃历并点其他日期的重新计算；新日期会出现在这里。同一日期已在运行或队列中时不会重复加入第二份任务。 |
| 「本轮结果」 | 查看 | 按最近完成顺序显示已完成/失败日期；失败标签会明确指出发生在图片模型、文字模型还是当天保存阶段。 |
| 清除已完成记录（右上角清扫图标） | 单击 | 只清除本轮已完成或失败的显示记录，不取消正在处理或排队的日期，也不删除已保存热量。 |
| 返回 | 单击或系统返回 | 可离开进度页、吃历，或切换应用内其他主页面；只要 DeskCubby 进程仍在，队列继续运行。它不是系统持久后台任务，强制停止应用或系统回收进程后不保证继续。 |

单击「正在处理」后出现的模型详情会为每个并发图片请求分别标出图片、模型名称、阶段和实时用时，并另列一张「全日统一计算」文字模型卡；计时使用单调时钟，不受系统时间修改影响。「思考」区域只显示服务端实际流式返回的 `reasoning_content`、`reasoning`、`analysis` 或 `<think>` 内容，默认可折叠/展开；「回复」区域持续追加可见回复。若兼容端点忽略流式参数并一次性返回普通 JSON，详情会在完整响应到达后更新，不会伪造模型未提供的思考。

每个日期最多并行运行 3 个图片模型请求；图片更多时后续请求等待空位。全部识别成功后，应用把同一天所有识别结果和仅供参考的日期备注合并成一个有界 JSON，只调用一次文字模型，并要求按 `photoIndex` 返回每张图片结果。文字模型可结合全部图片判断同餐重复角度，并把重复图片记为 0 kJ，避免日总量重复。最后对 `dc-media.json` **统一保存一次**；任一图片识别、全日计算、目录检查或侧车写入失败时，当天结果都不会部分提交，队列继续下一个日期。

### 吃历页 · 日期分组与照片列表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 日期行（如 2026-07-26） | 查看 | 每天一组照片；当日至少一张照片有热量数据时，日期后以主题色显示『· 总热量 kJ』。有手工总量时显示手工值，否则显示当天照片估算小计。 |
| 日期后的『· N kJ』 | 单击 | 打开这一天的「热量详情」。详情始终使用完整日期数据，即使当前只筛选了早餐等部分餐别也不会裁掉其他餐次。 |
| 重新计算某日期热量（日期行右侧计算器图标） | 单击 | 弹出确认框，点「重新计算」会把当天全部照片作为**强制重算**任务加入队列：最多 3 张并行识别，再由一次文字模型请求统一计算，完成后更新食物明细并清除手工总量；详情里已保存的备注只发送给文字模型。正在处理别的日期时仍可点击，任务会在进度页显示排队；同一天已在运行时不会重复计算或重复保存。前提同顶栏热量按钮：需开启 AI 热量估算，否则该图标不显示。 |
| 照片卡片 | 单击 | 全屏放大查看该照片。卡片图片按裁切方式填充，最大高度由 设置 → 子页面设置 → 日记与媒体 → 吃历显示 →「图片高度上限」滑条控制（80–320dp）；卡片下方的餐别/说明文字由「显示餐别文字」开关控制，说明为空时回退显示餐别名。 |
| 照片行 | 横向滑动 | 当「图片自动换行」关闭时，每天的照片排成一行，可左右滑动浏览；开启换行后照片按「每行图片数量」（2 / 3 / 2+3 自动）折行铺满宽度，不再横向滑动。『2+3 自动』会混排每行 2 或 3 张，让最后一行不留空位。 |
| 整页列表 | 上下滑动 | 按日期滚动浏览全部饮食照片。 |
| 空状态「还没有饮食照片」 | 单击「刷新」按钮 | 日记中标注了餐别（早餐、午餐、下午茶、晚餐、水果、夜宵）的照片会出现在吃历；没有时显示此空状态，点「刷新」重新扫描。读取失败时显示「无法读取吃历」和「重试」按钮。 |

### 吃历页 · 「热量详情」对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「总热量」卡片 | 单击 | 切换为数字输入框，可为多人聚餐、同一份饭拍了多次等情况输入手工总量（0–1,000,000 kJ）。手工值旁会保留估算小计作参考。 |
| 「恢复估算小计」 | 单击 | 清除手工覆盖值，重新使用各照片估算热量之和；仍需点「保存」才持久化。 |
| 「备注」输入框 | 输入 | 最多 4,000 字符，只在这一天的热量详情里显示，不写进日记 Markdown。适合说明「两人分享」「午餐 1/2 是同一份饭」等估算背景。 |
| 早餐 1 / 午餐 1 / 午餐 2… | 查看 | 每张照片按餐别和当天顺序编号，显示照片总 kJ；新估算还会逐项列出食物名称、分量、单位和 kJ。全日统一计算确认是同一餐的重复角度时，该重复图片可以显示 0 kJ；没有有效结果时只显示「估算失败」。 |
| 单张照片右侧计算器 | 单击并确认 | 只把这一张图片加入强制重算队列。新的逐项结果会替换该图旧结果，但保留当天手工总量和备注；适合只修正一张识别失败的照片。 |
| 「保存」 | 单击 | 持久化手工总量（或恢复小计）和备注，成功后关闭详情。侧车写入失败时不会把损坏/超限的旧文件当空数据覆盖。 |
| 「重新计算」 | 单击 | 保留当前输入的备注，把当天所有照片加入强制重算队列；轮到该日期后并行识别、统一计算、更新全部食物明细并清除手工总量。备注只加入当天唯一一次文字模型请求，不发送给任何图片识别模型。它与每张照片旁只重算单图的计算器不同。 |

热量侧车仍位于媒体目录 `dc-media.json`。0.8.0 继续写入兼容旧格式的 v2：文件名键统一小写，未知字段尽量往返保留，并限制文件大小、条目数、日期数、食物数、字符串和能量范围；更新采用 previous/pending 与回读校验。AI 结果始终只是估算，不是医学或营养测量值。

### 吃历页 · 「筛选餐别」对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 餐别行（早餐 / 午餐 / 下午茶 / 晚餐 / 水果 / 夜宵） | 单击整行或点击勾选框 | 切换该餐别照片的显示/隐藏；列表立即按勾选结果过滤。对话框内提示『全部取消时不显示任何照片。』，全部取消后列表区显示『当前筛选下没有照片』。 |
| 全部显示 | 单击 | 一键重新勾选全部六个餐别（清除筛选），对话框保持打开。 |
| 完成 | 单击 | 关闭对话框，保留当前筛选。注意：筛选只在本次停留吃历页期间有效，退出页面后自动恢复全部显示（不持久保存）。 |

### 吃历页 · 「导出吃历长图」对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 开始日期 | 单击 | 打开系统风格的日期选择器；选中的日期会包含在导出范围内。默认是当前筛选结果中最早的一天。 |
| 结束日期 | 单击 | 打开日期选择器；该日期同样包含在导出范围内。默认是当前筛选结果中最晚的一天。开始日期晚于结束日期时会显示错误且不能继续。 |
| 当前已选择 N 个餐别 | 无（只读） | 提醒本次导出会沿用吃历顶栏的当前餐别筛选；如需只导出早餐等类别，请先关闭对话框、调整漏斗筛选后再打开导出。 |
| 选择保存位置 | 单击 | 先检查所选范围是否有匹配照片，以及生成图片是否超过安全高度/像素上限；通过后打开 Android「新建文件」界面，默认名为 `DeskCubby-meals-开始日期_to_结束日期.png`。选择目标后生成 PNG、写入并回读校验，成功提示「已导出 N 天、M 张照片」。 |
| 取消 | 单击 | 关闭范围对话框，不生成文件。系统保存界面中取消也不会导出。 |

导出图会沿用当前照片滤镜、每行图片数量、图片高度上限和说明显示开关，并在图头标出日期范围与餐别。无法读取的单张照片会显示占位块，不会让整次导出无提示中断；范围过大或内存不足时会提示缩短日期范围，可分批导出。

### 吃历页 · 全屏图片查看器

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 图片 | **双指缩放（捏合）** | 在 1 倍到 6 倍之间自由缩放；放大后可用手指拖动平移查看细节（平移范围自动限制在图片边界内）。 |
| 图片 | **双击** | 未放大时一键放大到 2.5 倍；已放大时恢复原始大小并回到居中位置。 |
| 黑色背景/图片 | 单击 | 关闭查看器返回列表。 |
| 关闭（右上角 X） | 单击 | 关闭查看器返回列表。 |
| 底部说明条 | 查看 | 以『说明 · 热量kJ · 地点』格式显示：图片说明（为空时显示餐别名）、热量（若已估算）、拍摄地点（若媒体元数据中记录了地点名或经纬度）。地点信息只在放大查看时显示。若照片滤镜已开启，放大图同样套用滤镜效果。 |

### 吃历滤镜页 · 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回（左上角箭头） | 单击（或系统返回手势） | 若无改动直接返回吃历页；若有未保存的调整，弹出「设置尚未保存」对话框：『返回会丢失刚才的滤镜调整。』，可选「保存」（保存并返回）、「继续编辑」（留在本页）、「放弃」（不保存直接返回）。 |
| 保存（软盘图标 + 文字） | 单击 | 保存全部滤镜参数并自动返回吃历页；保存后同一效果应用到全部吃历照片。仅当有未保存改动时可点击，否则置灰。 |

### 吃历滤镜页 · 「统一照片滤镜」与「实时预览」卡片

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 统一照片滤镜 开关 | 单击切换 | 滤镜总开关（与吃历顶栏魔棒按钮单击是同一开关）。说明文字：『只改变吃历中的显示效果，不会修改原始图片。』改动需点「保存」才生效。 |
| 预览图 | 查看 | 实时显示当前滤镜效果；右上角显示『滤镜已开启 / 滤镜已关闭』。当前版本预览使用内置彩色渐变+黑白灰条示例图（非用户照片）。下方提示：调整立即显示在预览中，保存后同一效果应用到全部吃历照片。 |

### 吃历滤镜页 · 「调整」卡片

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 重置 | 单击 | 把五个滑条一键恢复默认值（全部归零/100%）；仅当存在可见调整时可点击。注意重置后仍需点顶栏「保存」才会写入。 |
| 亮度 滑条 | 拖动 | -100% ~ +100%，按 5% 步进吸附，右侧实时显示带正负号的百分比。 |
| 对比度 滑条 | 拖动 | 0% ~ 200%（默认 100%），5% 步进。 |
| 饱和度 滑条 | 拖动 | 0% ~ 200%（默认 100%），5% 步进；调到 0% 即黑白效果。 |
| 色温 滑条 | 拖动 | -100% ~ +100%，5% 步进；左端「冷」（偏蓝）、右端「暖」（偏黄）。 |
| 色调 滑条 | 拖动 | -100% ~ +100%，5% 步进；左端「绿色」、右端「洋红」。 |

### 小技巧

- 最容易被忽略：吃历顶栏的魔棒「照片滤镜」按钮是双功能按钮——**单击开/关滤镜，长按才进入「吃历滤镜」调整页**。
- 全屏看图支持双击快速放大到 2.5 倍、再双击还原；双指捏合可缩放 1~6 倍，放大后单指拖动平移；单击任意处即可关闭。
- 拍摄地点信息不在列表卡片上显示，只有点开照片放大后才会出现在底部说明条。
- 「筛选餐别」的勾选不会被保存：退出吃历页再进来会恢复全部显示；筛选生效期间漏斗图标会变成主题色作为提醒。
- 导出长图会沿用**当前**餐别筛选与照片显示设置；日期范围按自然日计算且包含首尾两天。若提示图片过高，请缩短范围分批导出。
- 顶栏「估算所有未计算热量」和日期行的重算按钮平时是隐藏的：必须先到 设置 → 子页面设置 → 日记与媒体 →「AI 热量估算」打开『上传饮食图片后自动估算』，而这个开关又要求先在 AI 配置里同时配好文字模型和图片模型。
- **长按顶栏计算器**查看精确进度；单击只负责把缺失照片按日期加入队列。日期行的计算器是强制重算，会覆盖当天已有的热量结果；处理别的日期时照样可以继续排队。
- 日期后的总热量文字可以点击；多人聚餐或重复拍摄时可在详情中改总量并写备注，之后重算会把备注交给文字模型辅助判断。
- 照片滤镜只改显示效果，永远不会修改原始图片文件。
- 吃历的排版都在 设置 → 子页面设置 → 日记与媒体 →「吃历显示」里控制：图片高度上限、显示餐别文字开关、图片自动换行开关，以及换行后的每行图片数量。
- 照片说明为空时，卡片和放大图会自动显示餐别名作为兜底文字。

---

## 5. 日常记录

**如何到达**：有两条入口，**写入目标不同**：

1. 首页"日常记录"模块 → 点底部"管理日常事件"（模板不超过 4 条时）或"查看全部"（超过 4 条时）；模块为空时点"添加"。**此入口进入后，发送按钮把记录写入今日日记。**
2. 日记编辑器底部工具栏的"日常记录"按钮。**此入口进入后，发送按钮把记录写入当前打开的那篇日记。**

### 顶栏与空状态

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回（左上角箭头图标） | 单击 | 返回上一页（首页或日记编辑器）。 |
| "还没有日常事件"提示卡片 | 无（纯展示） | 没有任何模板时显示，附说明："添加一句常用记录；需要替换的位置可以写成 xx。" |

### 每条日常事件卡片

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 事件多行输入框（预填模板文字，如"喝水 xx 杯"） | 单击 / 输入 / 回车 | 可直接编辑完整多行内容。句中的 xx（不区分大小写，XX/Xx 同样识别）以主题色加下划线高亮；点到 xx 会整体选中。内容上限 100 字符，显示 2–6 行，超出后在输入框内滚动；此处修改只影响本次发送。 |
| 软键盘回车键 | 单击 | 插入换行，不会自动发送；需要提交时点卡片右侧纸飞机按钮。 |
| 发送按钮（圆形纸飞机图标） | 单击 | 去掉内容首尾空白后，把中间换行保持为真实换行追加到日记末尾。从首页入口进入时写入今日日记；从日记编辑器入口进入时写入当前日记。写入或冲突检查失败会保留输入并提示，成功后才恢复模板原文。发送期间按钮变为加载圈，重复点击被忽略；输入框为空白时按钮禁用。 |
| 编辑（铅笔图标） | 单击 | 打开"编辑日常事件"对话框，修改该模板文字。该卡片正在发送时禁用。 |
| 删除（垃圾桶图标） | 单击 | 弹出"删除日常事件？"确认对话框（不会直接删除）。该卡片正在发送时禁用。 |

### 列表底部与对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| "新增日常事件"按钮（带 + 图标，通栏） | 单击 | 打开"新增日常事件"对话框。当另一次模板增/改/删操作尚未完成时按钮禁用。 |
| "事件文字"多行输入框（占位示例"例如：喝水 xx 杯"） | 输入 / 回车 | 填写模板内容，支持真实换行，上限 100 字符，显示 3–8 行。对话框内附提示："提示：xx 是可替换符号，记录时点击下划线处即可直接改写。" |
| 保存（新增/编辑对话框） | 单击 | 保存模板并关闭对话框，底部提示"已添加日常事件"或"已保存日常事件"。文字为空白时按钮禁用；若提交内容全为空白则提示"请输入日常事件名称"。 |
| 取消（新增/编辑对话框） | 单击（或点击对话框外部区域） | 关闭对话框，不保存任何修改。 |
| 删除（"删除日常事件？"对话框） | 单击 | 永久删除该模板（正文警告此操作无法撤销），成功后提示"已删除日常事件"。另一次模板操作进行中时禁用。 |
| 取消（"删除日常事件？"对话框） | 单击（或点击对话框外部区域） | 关闭对话框，不删除。 |

### 小技巧

- 点击句中带下划线的 xx 会自动选中整个占位符，直接打字即可替换，不需要先删除——这是本页最容易被忽略的交互。
- xx 不区分大小写，XX、Xx 同样会被高亮并支持点选替换；一句里可以写多个 xx。
- 回车用于输入换行；发送始终使用卡片右侧的纸飞机按钮，避免多行内容被误提交。
- 在卡片输入框里改写文字只影响本次发送，不会改动模板；发送成功后输入框自动恢复为模板原文。要永久修改模板请用铅笔"编辑"按钮。
- 两个入口写入目标不同：从首页进入写入"今日日记"（文件不存在时自动创建）；从日记编辑器进入写入"当前打开的那篇日记"。
- 每条记录作为一段纯文本追加到日记文件末尾，内容中的换行保持不变。
- 发送期间纸飞机按钮变为加载圈，此时对同一事件的重复点击会被忽略，防止重复写入。
- 本页面没有长按、滑动、拖动、双击或双指缩放手势；所有操作均为单击和文本输入。

---

## 6. 小巧思

**如何到达**：底部导航「小巧思」（闪电图标）；也可从首页的小巧思入口进入。回收站需在小巧思页顶栏点击回收站图标进入。每次本次页面首次载入「全部」、未分类或任一具体分类时，会自动显示列表最下面的最新内容，不再停在最上方最老条目；之后的手动滚动不会被反复抢回底部。

### 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 标题（当前分类名） | 无交互（仅显示） | 显示当前筛选的分类：「全部」「未分类」或具体分类名，过长时省略号截断。 |
| 一行 / 完整（文字按钮） | 单击 | 位于右上角「查看分类」按钮左侧；在「一行」省略显示和「完整」多行显示之间立即切换，并直接保存为下次打开时的偏好。按钮文字显示当前模式，无需再进设置页。 |
| 查看分类（三横线菜单图标） | 单击 | 打开左侧「小巧思分类」抽屉；正在编辑的条目会被取消编辑。 |
| 回收站图标 | 单击 | 进入「小巧思回收站」页面。 |

### 列表（小巧思条目）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 条目卡片 | 单击 | 进入编辑模式：该条内容载入底部输入框，输入框上方出现「正在编辑一条小巧思」提示与「取消」按钮；修改后点发送保存。 |
| 条目卡片 | **长按** | 弹出操作菜单，显示该条的分类、创建时间（及更新时间，若改过），并提供：置顶/取消置顶、标记重点/取消重点、切换分类、复制、分享这段文字、移入回收站、关闭。 |
| 删除（垃圾桶图标，每条右侧） | 单击 | 直接将该条移入回收站（**无二次确认**，可去回收站恢复）；若该条正在编辑，同时退出编辑并清空输入框。 |
| 四点拖动手柄（每条最右侧） | **按住上下拖动** | 拖动排序：拖动中条目半透明跟随手指，蓝色横线指示插入位置，松手落位。仅当列表多于 1 条时可用（只剩 1 条时手柄变灰）；开始拖动会自动收起键盘。在「全部」页是全局排序，在「未分类」或某分类页内只在该组内部排序。 |
| 置顶图钉图标 / 分类色点（条目左侧） | 无交互（仅显示） | 图钉表示已置顶；彩色圆点表示所属分类的颜色。标记为重点的条目整卡以高亮色显示。 |
| 空状态提示 | 无交互（仅显示） | 「全部」页为空时显示「记录此刻的想法／在下方快速写一条小巧思。」；某分类为空时显示「这个分类还是空的／可以在下方输入，长按发送按钮选择分类。」 |

### 长按条目弹出的操作菜单

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 置顶 / 取消置顶 | 单击 | 置顶把该条移到列表最前；取消置顶把它移到列表**末尾**。 |
| 标记重点 / 取消重点 | 单击 | 切换重点状态；重点条目整卡以高亮颜色显示（颜色深浅自动适配黑/白文字）。 |
| 切换分类 | 单击 | 打开「切换分类」弹窗，把该条归入其他分类或未分类。 |
| 复制 | 单击 | 将该条文字复制到系统剪贴板。 |
| 分享这段文字 | 单击 | 调起系统分享面板（标题「分享小巧思」）；若设备上没有可分享的应用，提示「没有可用的分享应用」。 |
| 移入回收站 | 单击 | 软删除该条，进入回收站，可随时恢复。 |
| 关闭 | 单击 | 关闭菜单，不做任何更改。 |

### 「切换分类」弹窗

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 未分类 | 单击 | 把该条设为未分类。 |
| 分类列表项（色点+名称） | 单击 | 把该条归入所选分类；当前所属分类右侧带对勾标记「当前分类」。若该条正在编辑，会同时退出编辑。 |
| 新增分类… | 单击 | 打开「新增分类」编辑弹窗，保存后立即创建该分类并把这条小巧思归入其中。 |
| 取消 | 单击 | 关闭弹窗，不改动分类。 |

### 底部输入区

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 输入框（提示语「此刻在想什么？」） | 单击输入 | 多行输入，随内容增高，最大高度可在设置中调整；键盘只在点击输入框时弹出。 |
| 发送按钮（纸飞机圆钮） | 单击 | 保存内容：新建时归入当前所在分类页对应的分类（在「全部」或「未分类」页发送则为未分类）；编辑模式下则更新原条目。内容为空时按钮灰色不可用。发送后清空输入框、收起键盘，新条目加到列表末尾并自动滚动定位到它。 |
| 发送按钮 | **长按** | 弹出「发送到分类」弹窗：选择「未分类」或任一分类后立即发送到该分类，并同时把列表切换到该分类页。前提：输入框有内容（按钮启用时长按才生效）。 |
| 取消（编辑模式下，「正在编辑一条小巧思」提示右侧） | 单击 | 退出编辑模式并清空输入框，原条目不变。 |

### 分类抽屉（左侧滑出，标题「小巧思分类」）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 全部 | 单击 | 显示所有小巧思，右侧数字为总条数；选择后抽屉自动关闭。 |
| 未分类 | 单击 | 只显示未归入任何分类的条目，右侧显示数量。 |
| 分类项（色点+名称+数量） | 单击 | 筛选出该分类下的小巧思。当前选中项有底色高亮。所选分类会被记住（重新打开小巧思页回到「全部」还是「上次访问」由设置决定）。 |
| 分类项 | **长按** | 打开「编辑分类」弹窗（改名、改色、导出、删除）。 |
| 新增分类 | 单击 | 打开「新增分类」弹窗创建新分类。 |

### 「新增分类 / 编辑分类」弹窗

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 分类名称输入框 | 单击输入 | 单行，最多 40 字；与现有分类重名时提示「已有同名分类」且无法保存（不区分大小写）。 |
| 分类颜色色板（9 个预置圆点） | 单击 | 选中该颜色，选中项带粗边框和对勾；「有机未来」视觉风格下预置色为另一套大地色系。 |
| 自定义颜色（调色板图标圆钮） | 单击 | 打开「选择颜色」取色器；确定后自定义色会作为新色块出现在色板中并被选中。 |
| 保存 | 单击 | 创建或更新分类；名称为空或重名时按钮禁用。 |
| 导出（仅编辑分类时显示） | 单击 | 把该分类下全部小巧思文本（以空行分隔）通过系统分享面板导出；分类为空时提示「该分类还没有小巧思」。 |
| 删除（仅编辑分类时显示，红色） | 单击 | 弹出「删除分类？」二次确认：分类下所有小巧思会保留并归入「未分类」。点「删除分类」确认，点「取消」放弃。删除分类不会删除任何小巧思。 |
| 取消 | 单击 | 关闭弹窗，不保存改动。 |

### 「选择颜色」取色器弹窗

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 颜色预览块 | 无交互（仅显示） | 实时显示当前颜色及其 #RRGGBB 十六进制值。 |
| 色相滑条 | 拖动 | 调整色相（0–360）。 |
| 饱和度滑条 | 拖动 | 调整饱和度（0–1）。 |
| 明度滑条 | 拖动 | 调整明度（0–1）。 |
| 十六进制输入框 | 单击输入 | 直接输入 #RRGGBB 色值，滑条同步跳转；格式不对时提示「格式应为 #RRGGBB」。不支持透明度（保存时强制不透明）。 |
| 确定 / 取消 | 单击 | 确定应用所选颜色；取消放弃。 |

### 小巧思回收站页面

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回（左上角箭头） | 单击 | 返回小巧思列表页。 |
| 恢复（每条右侧图标） | 单击 | 把该条恢复到列表：原本置顶的回到列表最前，其余排到列表末尾。 |
| 永久删除（每条右侧图标） | 单击 | 弹出「永久删除？／此操作无法恢复。」确认框；点「永久删除」彻底删除，点「取消」放弃。 |
| 空状态提示 | 无交互（仅显示） | 回收站为空时显示「回收站为空／删除的小巧思会暂时保存在这里。」 |

### 小技巧

- 长按发送按钮可直接选择「发送到分类」——发送的同时切换到该分类页，比先切分类再发送更快。
- 右上角分类按钮左边的「一行/完整」可以随时切换条目展开方式；它与 设置 → 子页面设置 → 小巧思 →「内容显示」是同一项设置。
- 分类抽屉里**长按某个分类**才能进入「编辑分类」（改名、换色、导出、删除）；单击只是筛选，编辑入口容易被忽略。
- 长按列表条目可查看创建/更新时间，并使用置顶、标记重点、切换分类、复制、分享、移入回收站等操作。
- 排序只能通过条目最右侧的四点手柄按住拖动，直接拖动卡片本身无效；列表只有一条时手柄禁用。
- 「置顶」会把条目移到列表最前，「取消置顶」会把它移到列表末尾（不是回到原位置）。
- 在「未分类」或某个分类页内拖动排序，只调整该组内部的相对顺序，不影响其他分类的条目。
- 「切换分类」弹窗底部有「新增分类…」，可以边归类边建新分类，保存后自动把当前条目归入新分类。
- 删除分类不会丢内容：该分类下的小巧思全部保留并归入「未分类」。
- 点条目右侧垃圾桶是直接移入回收站、没有确认弹窗；误删可去回收站点「恢复」找回，只有回收站里的「永久删除」才需要二次确认且不可恢复。
- 取色器里除了三根滑条，还可以直接在十六进制输入框粘贴 #RRGGBB 色值。
- 条目行高、单行/全文显示模式、重点高亮颜色、输入框最大高度，以及重新打开小巧思页时回到「全部」还是「上次访问」的分类，都在应用设置中调整。
- 初次进入某个分类会定位到最下面的新内容；发送新小巧思后仍会精确滚动到刚新增的条目。

---

## 7. 浏览器

**如何到达**：默认不直接显示在底部栏，而是放在「导航」页里：点底部「导航」→ 点「浏览器」卡片进入。可在 设置 → 底部导航 中改为直接显示在底部栏。进入后若上次浏览过网页，会自动恢复上次访问的网址；否则加载设置中的浏览器主页（默认 https://www.google.com）。

### 顶栏 · 地址栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 地址栏输入框 | 单击 | 聚焦地址栏；首次获得焦点时自动全选已有文本，方便直接整体替换输入新网址。 |
| 地址栏输入框 | 输入文字 | 编辑当前标签页的地址草稿（仅草稿，不会立即加载）。注意：没有搜索引擎功能，必须输入网址；不带协议前缀的输入（如 example.com）提交时会自动补全为 https://example.com。 |
| 键盘"前往（Go）"键 | 单击（软键盘确认键） | 提交地址并在当前标签页加载。地址留空提交则回到空白起始页（书签页）。 |
| 地址栏区域 | **横向滑动（左右滑）** | 在多个标签页之间切换：地址栏是一个横向翻页器，每个标签页对应一页，滑动停稳后自动切换到对应标签页的网页。只有一个标签时滑动无效果。 |

### 顶栏 · 标签页按钮（显示数字的按钮）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 标签数字按钮（显示当前标签页数量） | 单击 | 打开标签页下拉菜单，列出所有已打开的网页。 |
| 新建网页 | 单击（菜单项） | 新建一个空白标签页并切换过去。最多同时打开 8 个网页；已满时该项变灰并显示"最多打开 8 个网页"。 |
| 标签页列表项（序号 + 标题 + 网址） | 单击 | 切换到该标签页。当前标签页右侧带对勾标记；标题为空时显示"网页 N"。 |
| 关闭网页 N（每个标签项右侧的 ✕ 按钮） | 单击 | 关闭该标签页。关闭当前标签时自动切到相邻标签；关闭最后一个仅剩的标签时自动新建一个空白标签页（浏览器始终至少保留一个标签）。 |

### 顶栏 · 浏览器菜单（☰ 按钮）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 浏览器菜单（☰ 图标按钮） | 单击 | 打开浏览器功能下拉菜单。 |
| 打开输入的网址 | 单击（菜单项） | 加载地址栏当前输入的内容，等同于按键盘"前往"键。 |
| 主页 | 单击（菜单项） | 在当前标签页加载浏览器主页。主页网址可在设置中修改。 |
| 后退 / 前进 | 单击（菜单项） | 当前网页后退/前进一步。前提：当前标签有可后退/前进的历史，否则该项变灰。 |
| 刷新 | 单击（菜单项） | 重新加载当前网页。前提：当前标签的网页视图已创建。 |
| 收藏当前网页 / 取消收藏 | 单击（菜单项） | 把当前网页加入或移出收藏夹（按当前是否已收藏切换文案与书签图标）。空白起始页不可收藏（该项变灰）。收藏后的网页出现在收藏夹对话框和空白起始页的"书签"列表中。 |
| 页内查找 | 单击（菜单项） | 打开"网页内查找"对话框，在当前网页内搜索文字。 |
| 浏览历史 | 单击（菜单项） | 打开浏览历史对话框，可点击历史记录重新打开、或清空全部历史。 |
| 收藏夹 | 单击（菜单项） | 打开收藏夹对话框，可点击收藏项重新打开。 |
| 网页主题：跟随系统 / 亮色 / 暗色 | 单击（菜单项，循环切换） | 按 跟随系统 → 亮色 → 暗色 → 跟随系统 循环切换网页深浅色主题。注意：切换导致深浅色变化时，当前打开的网页会自动重新加载。 |
| 电脑模式：已开启 / 已关闭 | 单击（菜单项，开关） | 切换电脑版网页模式（使用桌面浏览器标识请求网页，并按桌面宽度排版）。切换后当前网页自动重新加载。该设置对所有标签页生效并会被记住。 |
| 用外部浏览器打开 | 单击（菜单项） | 调用系统默认浏览器打开当前网址。空白起始页不可用（变灰）；设备上没有可处理的浏览器时提示"没有可用的外部浏览器"。 |

### 空白起始页与网页浏览区

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 线性进度条（顶栏下方） | 无交互（自动显示） | 网页加载过程中按百分比推进，加载完成后自动消失。 |
| 书签列表项（标题 + 网址，空白起始页） | 单击 | 在当前标签页打开该收藏的网页。无收藏时显示"暂无书签"（有机未来风格下显示"收藏夹还是空的"及提示文字）。 |
| 网页内容 | **双指缩放** | 放大/缩小网页（已启用内建缩放，屏幕上不显示缩放按钮）。 |
| 系统返回键 / 返回手势 | 单击 / 侧滑 | 当前网页可后退时，返回操作让网页后退一步，而不是退出浏览器页面；不可后退时恢复系统默认返回行为。 |
| 网页内的非 http/https 链接 | 单击 | 尝试跳转到对应的外部应用处理；http/https 链接则留在应用内加载。 |
| 网页内的文件上传控件 | 单击 | 弹出系统文件选择器，选择文件后回传给网页；取消选择则上传取消。 |
| 网页内的下载链接 | 单击 | 通过系统下载管理器下载到设备公共"下载（Downloads）"文件夹，文件名中的非法字符自动替换为下划线；成功提示"已加入下载队列"，失败提示"下载失败"，下载完成后有系统通知。 |
| 重新加载（网页渲染进程崩溃页） | 单击 | 提示"网页渲染进程已停止"后重新加载该标签页的网页；此时该页的前进/后退历史会被重置。 |

### 对话框（页内查找 / 浏览历史 / 收藏夹）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 查找输入框 | 输入文字 | 边输入边在当前网页中实时查找并高亮所有匹配项。 |
| 上一个 / 下一个 | 单击 | 跳到上一处/下一处匹配。 |
| 完成 | 单击 | 关闭查找对话框并清除网页上的所有高亮标记（点对话框外部关闭同样清除高亮）。 |
| 历史记录项（标题 + 网址） | 单击 | 在当前标签页打开该网址并关闭对话框。无记录时显示"暂无记录"。 |
| 清空（浏览历史对话框） | 单击 | 立即清空全部浏览历史。**没有二次确认，清空后不可恢复。** |
| 收藏项（标题 + 网址） | 单击 | 在当前标签页打开该网址并关闭对话框。无收藏时显示"暂无记录"。 |
| 关闭 | 单击 | 关闭对话框。收藏夹对话框没有"清空"按钮；取消单条收藏需先打开该网页，再从浏览器菜单选"取消收藏"。 |

### 小技巧

- 隐藏手势：在顶部地址栏上**左右横滑**即可在标签页之间切换，无需打开标签数字按钮的菜单。
- 地址栏首次点击会自动全选已有网址，直接输入即可整体替换。
- 地址栏没有搜索功能：输入的内容一律当作网址处理，不带 http(s):// 前缀时自动补 https://；地址留空按"前往"会回到空白起始页。
- 最多同时打开 8 个标签页；关闭仅剩的最后一个标签时会自动补一个空白标签。
- 系统返回键/返回手势优先执行"网页后退"，只有当前网页无法再后退时才恢复正常返回行为。
- 空白起始页就是书签页：收藏过的网页会以"书签"列表形式列在这里，新建标签后可一键打开常用网站。
- 切换"网页主题"深浅色或"电脑模式"后，当前打开的网页会自动重新加载，未提交的网页表单内容可能丢失。
- 浏览历史对话框中的"清空"没有确认提示，点击即全部删除。
- 证书（SSL）无效的网站会被直接拒绝加载，没有"继续访问"选项，表现为页面加载不出来。
- 重新进入浏览器页面时会自动恢复上次浏览的网址（仅恢复一个标签，标签列表不跨会话保存）。

---

## 8. 日期记录

**如何到达**：默认收在「导航」页中：导航 → 点击「日期记录」卡片进入；也可从首页「日期记录」模块直接跳转。可在 设置 → 底部导航 中把它改为底部栏常驻标签。

### 顶栏、悬浮按钮与空状态

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 顶栏标题「日期记录」 | 无交互 | 仅显示页面名称，顶栏无其他按钮。 |
| 右下角悬浮「+」按钮 | 单击 | 打开「添加日期」对话框，新建一条日期记录。 |
| 「添加日期」按钮（空状态） | 单击 | 尚无记录时显示「还没有日期记录」「添加纪念日、目标日或其他重要日期」，点按钮同样打开「添加日期」对话框。 |

### 记录列表卡片

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 记录卡片整体 | 单击 | 打开「编辑日期」对话框。卡片显示：Emoji 图标、名称、格式化日期，以及倒计时文案——未来"还有 N 天到 X"、过去"距离 X 已经过去 N 天"、当天"今天就是 X"；日期解析失败显示「日期格式无效」。 |
| 铅笔图标（编辑） | 单击 | 同样打开「编辑日期」对话框。 |
| 垃圾桶图标（删除） | 单击 | 弹出确认对话框「删除日期？」（此操作无法撤销）；点「删除」永久删除，点「取消」或对话框外部放弃。 |

### 添加/编辑日期对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「名称」输入框 | 输入文字 | 占位提示「例如：旅行出发」，单行，最多 256 字符。名称为空时「保存」置灰不可点。 |
| 「图标（可直接输入 Emoji）」输入框 | 输入/粘贴 Emoji | 最多 4 个字符码点（约 1~2 个 Emoji）。留空保存时自动使用默认图标 🎯。 |
| 常用图标行（🎯🎂❤️✈️🎓🏠💼🎉⭐📅） | 左右滑动浏览，单击选择 | 点击某个 Emoji 立即填入图标框，选中项高亮显示。 |
| 日期按钮（日历图标 + 当前所选日期） | 单击 | 打开系统日期选择器对话框（支持日历/键盘输入两种模式切换），点「确定」应用所选日期，「取消」放弃。新建时默认今天。 |
| 倒计时预览文字 | 无交互（自动更新） | 对话框底部实时预览保存后的倒计时文案，名称为空时用「这个日期」代替。 |
| 「保存」/「取消」按钮 | 单击 | 「保存」写入记录并关闭（名称必填）；「取消」放弃修改。操作失败时页面底部弹出「操作失败: <原因>」提示条。 |

### 小技巧

- 日期记录卡片**整卡可点**，不必精确点铅笔图标即可进入编辑。
- 日期图标框可直接输入或粘贴任意 Emoji（最多约 2 个），不限于下方的 10 个常用图标；留空则自动使用 🎯。
- 系统日期选择器右上角有模式切换按钮，可从日历点选切换为手动键盘输入日期。

---

## 9. 诗词本

**如何到达**：默认收在「导航」页中：导航 → 点击「诗词本」卡片进入。可在 设置 → 底部导航 中改为底部栏常驻标签。每日诗词的收藏入口在主页「每日诗词」模块，诗词本负责汇总保存。

### 顶栏、分类筛选、悬浮按钮与空状态

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 分类文件夹（右上角） | 单击 | 打开「诗词分类」：可添加分类，或点已有分类修改名称、颜色、删除。删除时会显示两个明确动作：只删除分类并把诗词归入「未分类」，或把分类及其全部诗词一起删除。 |
| 「排序」按钮（右上角） | 单击 | 开启排序模式；每条诗词收成一行，并把能识别到的《题目》放在正文开头。右侧出现四点拖动手柄，按住拖动即可调整顺序，包括调整第一条或把任意条目移到第一条；点「完成」退出排序模式。 |
| 设置（右上角齿轮） | 单击 | 打开 设置 → 子页面设置 → 诗词本，可导入字体并调整诗词卡片排版。 |
| 「全部 / 未分类 / 自定义分类」筛选条 | 横向滑动并单击 | 只显示对应分类的诗词；末尾「分类」按钮也会打开分类管理。当前筛选只影响显示，不改动诗词。 |
| 右下角悬浮「+」按钮 | 单击 | 打开「添加诗词」对话框。 |
| 「添加诗词」按钮（空状态） | 单击 | 打开「添加诗词」对话框。整本为空时还会提示可收藏每日诗词、手动添加或从初高中预设导入；当前分类为空时提示切换分类。 |

### 分类与初高中预设

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 分类管理中的「添加分类」 | 单击 | 选择「新建自定义分类」或「选择初高中古诗文预设」。 |
| 「新建自定义分类」 | 单击 | 输入最多 100 字符的唯一分类名，并从色板选择分类颜色；同名分类不可重复保存。 |
| 已有分类 | 单击 | 编辑名称和颜色，也可点「删除分类」。确认框中的「仅删除分类（诗词变为未分类）」会保留内容；「分类和诗词一起删除」会永久删除该分类下所有诗词；「取消」不改动任何内容。 |
| 「选择初高中古诗文预设」 | 单击 | 显示 11 个离线预设：初中七上、七下、八上、八下、九上、九下，以及高中必修上、必修下、选择性必修上、中、下，共 182 篇古诗文。每行显示篇数，点该行立即导入。 |
| 预设导入结果 | 查看底部提示 | 预设会创建或复用同名分类；标题/作者/正文相同的已有内容自动跳过，并显示新增与跳过数量。教材版本可能调整，实际篇目请以学校教材为准。 |

### 诗词卡片列表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 诗词卡片整体 | **长按** | 打开「诗词操作」界面，显示「编辑」「切换分类」「删除」；普通单击不执行编辑或删除。卡片会用彩色标签显示所属分类。 |
| 「编辑」（长按操作界面） | 单击 | 若这条收藏来自每日诗词的节选，应用会先按当前条目重新读取并匹配完整诗词，再把完整正文放入编辑框；加载期间显示进度。若完整内容读取失败，会保留原收藏并提示，绝不会拿另一首随机诗替换。 |
| 「切换分类」（长按操作界面） | 单击 | 在「未分类」和所有已有分类中选择，选择后立即保存。 |
| 「删除」（长按操作界面） | 单击 | 弹出确认对话框「删除这首诗词？」（删除后无法恢复）并预览诗句前 80 字；「删除」/「取消」。 |

### 添加/编辑诗词对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「诗词正文」多行输入框 | 输入文字 | 占位「输入完整诗词正文」，显示 3~8 行，上限 4000 字符；键盘「下一项」自动跳到出处框。正文为空时「保存」不可点。 |
| 「出处（可选）」输入框 | 输入文字 | 占位「例如：李白《静夜思》」，单行，上限 512 字符；键盘「完成」收起键盘。 |
| 「分类：…」按钮 | 单击 | 选择「未分类」或任一已有分类。若从某个分类筛选页点新增，默认选中该分类；编辑时保持原分类。 |
| 「保存」按钮 | 单击 | 保存期间按钮变为「保存中…」并禁用；此时「取消」按钮和点击对话框外部均无效，须等保存结束。保存失败底部弹出「操作失败: <原因>」。 |
| 「取消」按钮 | 单击 | 放弃编辑并关闭对话框（保存中不可用）。 |

### 小技巧

- 排序模式下四点手柄支持拖动排序；条目以稳定 ID 判断落点，因此列表第一项和移到索引零均可正常保存。TalkBack 可用「上移 / 下移」操作，排序模式结束后恢复完整诗词显示。
- 诗词卡片的编辑、改分类与删除只从长按操作界面进入，避免滚动时误触；卡片的字体与排版由 设置 → 子页面设置 → 诗词本 控制。
- 对话框在「保存中」状态下点击对话框外部或「取消」都无法关闭，需等待保存完成。

---

## 10. RSS 订阅

**如何到达**：默认收在「导航」页中：导航 → 点击「RSS 订阅」卡片进入。可在 设置 → 底部导航 中改为底部栏常驻标签。

### 顶栏、悬浮按钮与空状态

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 顶栏刷新图标（刷新订阅） | 单击 | 重新拉取所有已启用订阅的文章。刷新进行中图标变为小圆形进度圈且不可再点；设置尚未加载完成时也禁用。前提：至少有一个已启用的订阅，否则提示「请先添加一个 RSS 订阅。」或「没有已启用的 RSS 订阅。」 |
| 右下角悬浮「+」按钮 | 单击 | 打开「添加 RSS 订阅」对话框。 |
| 「添加订阅」按钮（空状态） | 单击 | 无任何订阅时显示「还没有 RSS 订阅」「添加一个 HTTPS 订阅地址，在一个页面阅读更新。」；点按钮打开添加对话框。 |

### 「订阅源」横向卡片区

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 订阅源卡片区 | **左右滑动** | 订阅源以横向一行卡片排列，订阅多时可左右滑动查看；区块标题右侧显示「每个订阅最多 N 篇」（N 在 设置 → 子页面设置 → RSS 订阅 中用滑条调整，10~200，默认 20）。 |
| 订阅卡片上的复选框 | 单击勾选/取消 | 启用或停用该订阅。启用后自动触发一次刷新；停用后该源的文章立即从列表移除。正在保存订阅时复选框暂时禁用。 |
| 铅笔图标（编辑订阅） | 单击 | 打开「编辑 RSS 订阅」对话框，可改名称和地址；保存成功后自动刷新。 |
| 垃圾桶图标（删除订阅） | 单击 | 弹出确认对话框「删除 RSS 订阅？」（将删除该订阅及当前加载的文章）；「删除」/「取消」。 |

### 「最新文章」列表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 文章卡片 | 单击 | 对带有有效绝对 HTTPS 链接的文章，进入 DeskCubby 内置多标签浏览器阅读完整原文；可在浏览器菜单中再选择「用外部浏览器打开」。缺少链接或链接不安全/格式不受支持时卡片不可跳转，并给出对应提示。卡片显示标题（最多 3 行）、来源与发布时间、摘要（最多 5 行；是否显示由 设置 → 子页面设置 → RSS 订阅 的「显示摘要」开关控制）。 |
| 「刷新」按钮（文章为空时） | 单击 | 已有订阅但暂无文章时显示「暂无文章」提示；点「刷新」重新拉取。 |

### 添加/编辑订阅对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「名称（可选）」输入框 | 输入文字 | 留空保存时自动以订阅地址的域名作为名称（域名取不到则用「RSS」）。 |
| 「HTTPS 订阅地址」输入框 | 输入网址 | 辅助提示「省略协议时会自动使用 https://」。仅支持 HTTPS：输入 http:// 地址会报错要求改为 https://；重复地址会报「这个 RSS 地址已经添加过了。」；键盘「完成」等同点「保存」。 |
| 「保存」按钮 | 单击 | 地址为空或正在保存时禁用；保存中按钮内显示小进度圈。保存成功后关闭对话框并自动刷新文章列表。 |
| 「取消」按钮 | 单击 | 关闭对话框（保存中不可用）。 |

### 小技巧

- RSS 订阅源卡片区是横向列表，订阅较多时可左右滑动查看，容易被误认为只有一张卡。
- RSS 只接受 HTTPS 地址；输入地址时可省略协议，会自动补全 https://。
- 「每个订阅最多 N 篇」与文章「显示摘要」不在 RSS 页面本身调整，需前往 设置 → 子页面设置 → RSS 订阅。
- 停用某个订阅（取消勾选）会立刻从「最新文章」里移除该源已加载的文章；重新勾选会自动刷新拉回。
- 刷新时若个别订阅源失败，成功源的文章正常更新，失败源保留上次加载的旧文章，并在提示条中按「源名：错误信息」逐行提示。
- 对话框在「保存中」状态下点击外部或「取消」都无法关闭，需等待保存完成。

---

## 11. DeskCubby Agent

**如何到达**：底部导航 →「导航」→「AI 聊天」。页面沿用聊天体验，但 Android 0.14.0 起运行的是真实 Agent 循环：模型可连续请求多个原生工具，DeskCubby 校验并执行后把结果返回模型，直到得到最终答复。页面不会把普通自然语言当作工具指令，也不会展示模型隐藏的 chain-of-thought。

第一次使用前，先到 **设置 → 子页面设置 → AI 设置 → AI 配置** 新建或编辑文字模型。填写接口地址、模型名称和必要的 API Key，并且只在确认该 Provider/模型支持 OpenAI-compatible 原生 tool calling 时勾选「原生工具调用」。旧配置默认不勾选；不支持时 Agent 会明确停用，不会偷偷解析普通回复执行工具。

Agent 回复按 Markdown 渲染：标题、粗体/斜体、列表、引用、代码块与链接会保留排版，文字可长按选择、链接可点击。页面字体大小、回复框最大宽度与 Agent 提示词都在 **设置 → 子页面设置 → AI 设置** 调整：字体大小作用于消息气泡与输入框（12–28 sp），回复框宽度限制气泡最大宽度（280–1200 dp），Agent 提示词作为风格/任务偏好附加在严格内置规则之后，可一键「恢复 Agent 提示词」回到默认文案；每个模型配置自己的「附加模型指令」仍会一起发送。

### 顶栏与模型

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 标题 | 仅显示 | 无活动会话时显示「DeskCubby Agent」和「按需调用工具，不预加载全文」；有会话时显示会话标题和「会话自动保存，可选择云同步」。 |
| Agent Review（清单图标） | 单击 | 打开独立 Review 页面，按 Agent Run / 会话查看真实修改、详细工具记录和 Undo。 |
| 会话历史（时钟图标） | 单击 | 查看本机保存的全部会话，可继续、重命名、删除或新建。 |
| 新会话（加号对话框图标） | 单击 | 清空当前消息、草稿、待发附件和执行状态；发送第一项任务时才真正建立会话。运行中或解析附件时禁用。 |
| AI 设置（齿轮图标） | 单击 | 打开「AI 设置」页（字体大小、回复框宽度、Agent 提示词与 AI 配置入口）。 |
| 当前 Agent 模型 | 单击 | 在文字模型配置间切换。每项明确标注「原生工具调用」或「不支持 Agent 工具」；只有地址/模型有效且已勾选原生工具调用的配置才能运行 Agent。 |

### 左下角四方块菜单

输入框左侧原来的 `+` 已改成四个方块图标。单击后有三项：

| 菜单项 | 操作 | 效果 |
| --- | --- | --- |
| 插入图片 / 文档 | 单击后在系统文件选择器多选 | 当前任务最多添加 5 个附件，单个上限 8 MiB；同一模型请求实际携带的图片合计另限 8 MiB。支持图片、PDF、DOCX、TXT、Markdown、HTML、JSON、CSV 和 XML；扫描版 PDF 没有可提取文字时会提示先 OCR。图片发送给当前多模态模型，文档提取出的有界文字作为不可信数据发送。 |
| 管理上下文 | 单击 | 分别授权/取消日记、小巧思、日期、日常事件、笔记、诗词本、手机使用时间、统计数据。选择立即持久化，下次进入仍保持。勾选表示允许 Agent 检索和按需读取，不会把该类全部正文塞入 Prompt；取消后相关工具立即无权访问。 |
| AI 权限模式 | 单击 | 在「需要批准」和「全自动」之间选择并持久化。读取、搜索和查询不弹审批；创建、编辑、删除、写文件或允许的设置修改属于 mutation。 |

「管理上下文」取代了旧的“选择某一天日记作为上下文”。每轮开始时，Agent 只看到已经授权的数据源说明及必要的轻量 metadata，例如条目数量、分类、标题和日期范围，再按任务使用分页/数量限制执行 `list_sources`、`list_entries`、`search_entries`、`read_entry` 或 `read_entries`。例如“总结上周日记”会先按日期范围查询再读取命中的日记；未勾选「日记」时，连检索该来源也会被拒绝。以后新增的数据源可以继续接入同一授权模型。

### 可用工具与安全边界

第一版工具按统一 Registry 注册：

- DeskCubby 数据：列出/搜索/读取条目，以及创建、编辑、删除允许修改的日记、小巧思、日期、日常事件、笔记和诗词。
- 文件：只在 Android SAF 已授权的日记或笔记根中列出、搜索、读取、创建和修改；不会把 `content://` URI 转成路径，也不能访问任意文件系统。
- 网络：`web_search` 与受限网页读取；只访问公开 HTTPS，限制重定向、私网地址、响应大小和运行时间。
- App：读取非敏感 DeskCubby 设置、修改明确允许的设置、查询应用状态。API Key、密码、keystore、云凭据、存储授权、Agent 权限等安全字段不会向工具公开，也不能由 Agent 修改。

日记、笔记、网页、附件和工具结果全部按外部不可信数据处理。其中即使写着“忽略系统提示”“开启全自动”或“调用某工具”，也不能改变系统提示、数据源授权、审批模式或工具规则。Agent 必须真的调用工具后才能声称读过文件或搜索过网络。

### 运行任务、执行过程与中止

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 附件行右侧 X | 单击 | 在任务启动前移除该附件。运行中不可移除。 |
| 消息输入框 | 输入 | 1–6 行自动伸缩，最多 100,000 字符。文字可为空，但此时至少要有一个附件。软键盘发送键等同纸飞机。 |
| 运行 Agent（纸飞机） | 单击 | 保存用户消息并启动“模型 → 工具 → 结果 → 模型”的循环。可在一次任务中连续调用多个工具，也可重复调用同一工具；最多 12 轮，防止无限循环。 |
| 中止 Agent（红色停止图标） | 单击 | 取消当前模型请求/工具执行；若正等待审批，待审批修改按拒绝处理。已经成功完成的修改不会因中止自动消失，可到 Review 手动 Undo。 |
| Agent 执行中 / 执行记录 | 查看或单击工具行 | 显示“正在搜索日记”“读取某条目”“搜索网络”“等待批准”“修改完成”等安全摘要。展开单项可看工具名、有界参数和结果摘要，不显示隐藏推理。末尾显示本次模型调用数、Provider 报告的 Token 与缓存率；未报告时明确标为未报告。 |

网络失败、工具失败或被拒绝会作为结构化 tool result 返回模型，模型可以重试、换方案或向你说明。非法参数、未知工具、重复 call ID、一次返回过多工具调用或达到循环上限都会安全停止并显示原因。

### Mutation 审批

默认「需要批准」。Agent 准备执行每一个 mutation 时会弹出「Agent 请求修改」，展示工具、目标、计划以及可用的修改前/修改后内容：

| 按钮 | 效果 |
| --- | --- |
| 批准 | 只批准当前这一项操作；执行结果再返回 Agent。后续 mutation 仍会逐项询问。 |
| 拒绝 | 不执行当前修改，并把拒绝作为 tool result 返回 Agent，使它可以继续解释或改用别的方案。点击对话框外也按拒绝处理。 |

「全自动」会直接执行 mutation，但每一项仍独立生成 Review 和可用的 Undo 信息；不会把多个操作包装成一项绕过记录。即使全自动，未经「管理上下文」授权的数据源仍不可访问，敏感设置仍不可修改。

### Agent Review 与真实 Undo

**如何到达**：AI 聊天 → 顶栏「Agent Review」。

- 顶部按运行/会话列出时间、会话标题、任务摘要、状态、模型调用和 Provider Token；点一项切换其记录。
- 「实际修改」按工具、目标、操作类型和时间展示摘要；点「详情」展开修改前/修改后内容。
- 只有真正成功的数据修改才显示「当前可撤回」。点 `Undo` 后再确认，DeskCubby 会先确认目标仍与 Agent 修改后的版本一致，再执行对应恢复操作；文件、日记、结构化数据和允许的设置都会恢复修改前状态。外部又改过或恢复失败时不会谎报成功，也不会只删除日志。
- 「详细工具执行记录」包含读取、查询、网络搜索、批准/拒绝、失败和取消状态，但不包含隐藏推理。Review 主体仍聚焦真正改变用户数据的操作。
- 全自动和需要批准产生相同的 Review 完整度。已撤回、冲突、无安全恢复信息或失败的操作会显示不可撤回。

### 会话、附件与云同步

| 控件 | 操作 | 效果 |
| --- | --- | --- |
| 历史会话条目 | 单击 | 打开并继续；会自动选择该会话绑定的模型，配置已删除时保留历史并改用当前可用配置。 |
| 重命名 | 单击 | 修改本机/同步会话标题，最长 80 字符。 |
| 删除 | 单击并确认 | 会话立即从本机隐藏；启用 Agent 会话同步时，删除 tombstone 也会同步到其他 Android 设备。 |

若在 **设置 → 应用数据 → 云端同步 → 某服务 → 同步内容** 勾选「Agent 会话」，聊天会通过单独的 `agent/v1/chats.json` 双向合并。同步包含会话文字、冻结后的文档文字、图片占位信息、已完成运行状态和 Provider 报告的 Token 用量；不上传本机 `content://` URI、图片字节、审批/Undo 载荷、详细 Review 事件、API Key 或其他秘密。另一设备会看到图片占位而不是原图。新建云配置默认勾选 Agent 会话；旧配置不会在升级时静默开启，需手动编辑保存。

Agent 会话不进入通用 v31 应用 JSON。旧 AI 会话和旧版冻结上下文会在 Room v13 迁移后保留；旧冻结内容只作为「旧版冻结上下文（只读兼容）」和不可信用户数据参与后续任务，不会获得系统指令权限。

### Token 与缓存统计

每次模型响应若带 OpenAI-compatible `usage`，DeskCubby 会保存 input/prompt、output/completion、total、cached-input 和 reasoning Token。进入 **导航 → 统计 → Agent Token** 可查看 Agent 运行数、模型调用数、已报告/未报告调用、输入/输出/总 Token、缓存输入、推理 Token 和缓存率。缓存率按 Provider 报告的缓存输入与输入 Token 计算；服务不返回的字段显示「—」，应用不会按文本长度猜测。完成运行的用量会随 Agent 会话同步，详细 Review/Undo 仍只留在产生修改的设备。

---

## 12. 收藏夹（加密保险库）

**如何到达**：底部导航栏 →「导航」页 → 点击「收藏夹」进入（默认不直接显示在底栏；可在 设置 → 底部导航 中设为底栏可见）。注意：此「收藏夹」是密码保护的加密保险库，与浏览器页面内的「收藏夹」（网页收藏）是两个不同功能。

### 首次设置页（尚未设置过密码时全屏显示）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 密码（输入框） | 单击后键盘输入 | 输入收藏夹密码，默认以圆点隐藏显示。允许 1 个到任意多个 Unicode 码点，不设最大长度；Emoji 等补充平面字符各按 1 个码点计算。 |
| 确认密码（输入框） | 单击后键盘输入 | 再次输入密码；与上一栏不一致时变红并提示「两次输入不一致」。 |
| 显示密码 / 隐藏密码（眼睛图标） | 单击 | 切换密码明文/圆点显示；两个密码框共用同一开关，点一次同时切换。 |
| 设置密码并启用（按钮） | 单击 | 创建收藏夹密码并进入解锁状态。前提：密码至少 1 个 Unicode 码点且两次输入一致，否则按钮置灰；不设最大长度。页面明确提示：内容用密码派生密钥在本机加密，**密码一旦丢失无法找回，数据也无法解密**。 |

### 锁定页（已设密码但未解锁时，标题「收藏夹已锁定」）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 密码（输入框） | 单击后键盘输入 | 输入解锁密码；重新输入时自动清除上次的错误提示。密码错误提示「密码错误」，异常提示「操作失败」。 |
| 显示密码 / 隐藏密码（眼睛图标） | 单击 | 切换密码明文/圆点显示。 |
| 解锁（按钮，带开锁图标） | 单击 | 校验密码并解锁进入条目列表。前提：密码框非空，否则按钮置灰。 |

### 解锁后 · 顶栏与列表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 修改密码（钥匙图标） | 单击 | 打开「修改密码」对话框。 |
| 锁定（锁图标） | 单击 | 立即锁定收藏夹并清除内存中的解锁密钥，回到锁定页；再次查看需重新输入密码。 |
| 普通文字条目卡片 | 单击 | 复制完整内容，并在底部提示「已复制」。卡片不显示标题和日期；正文最多预览 4 行，有备注时在原日期位置显示最多 2 行备注，没有备注时整行省略以节省高度。 |
| 安全链接条目卡片 | 单击 | 当完整内容是可安全解析的 `http://` 或 `https://` 链接时，正文以主题色和下划线标识，单击调用系统浏览器打开。含空白、控制字符、用户信息或无主机名的可疑文本仍按普通文字处理，只能复制。 |
| 条目卡片 | 长按 | 打开条目界面，可复制、编辑保存或删除；复制入口不再占用卡片空间。 |
| 四点拖动手柄（卡片右侧） | 按住后上下拖动 | 调整收藏条目顺序并持久保存；只有一个条目时手柄置灰。TalkBack 可使用「上移 / 下移」。 |
| 新增条目（空状态按钮） | 单击 | 列表为空时显示「收藏夹还是空的」及说明，点按钮打开新增条目对话框。 |
| 列表区域 | 上下滑动 | 滚动浏览条目列表。 |
| 新增条目（右下角 + 悬浮按钮） | 单击 | 打开「新增条目」对话框。 |

### 新增条目 / 编辑条目 对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 内容（输入框） | 单击后键盘输入 | 必填的多行正文（最少 4 行高，超长可在框内滚动）。可以填写普通文字或完整链接。 |
| 备注（可选）（输入框） | 单击后键盘输入 | 给条目增加可选备注；备注会与正文一起加密。有内容时自动显示在卡片底部，没有备注时不保留空白行。 |
| 保存（文字按钮） | 单击 | 加密保存新条目或修改。前提：内容不是空白，否则按钮置灰；保存失败时页面底部弹出「操作失败」提示条。 |
| 复制（仅编辑已有条目时出现） | 单击 | 复制完整正文并保持对话框打开。 |
| 删除（红色文字按钮，仅编辑已有条目时出现） | 单击 | **立即删除该条目并关闭对话框。注意：没有二次确认，点击即删。** |
| 取消（文字按钮） | 单击 | 放弃修改并关闭对话框；点击对话框外部区域同样关闭且不保存。 |

### 修改密码 对话框

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 旧密码（输入框） | 单击后键盘输入 | 输入当前密码；输错提示「旧密码错误」，重新输入自动清除错误。 |
| 新密码（输入框） | 单击后键盘输入 | 输入新密码；至少 1 个 Unicode 码点，没有最大长度。 |
| 确认新密码（输入框） | 单击后键盘输入 | 再次输入新密码，不一致时提示「两次输入不一致」。 |
| 显示密码 / 隐藏密码（眼睛图标） | 单击 | 三个密码框共用同一开关，点一次同时切换明文/圆点。 |
| 确认修改（文字按钮） | 单击 | 校验旧密码并将所有条目用新密码重新加密。前提：旧密码非空、新密码至少 1 个 Unicode 码点且两次一致。对话框内明示：新密码丢失后同样无法找回。 |
| 取消（文字按钮） | 单击 | 关闭对话框，不改密码；点击对话框外部同样关闭。 |

### 小技巧

- **密码不可找回**：设置页与修改密码对话框均明确提示，密码丢失后加密数据无法解密，请务必牢记或另行妥善备份密码。
- 密码下限按 Unicode 码点计数（1 个），不是 UTF-16 代码单元；没有最大长度。安全强度仍取决于密码本身，重要内容建议使用足够长、难猜的密码。
- 删除条目没有二次确认——编辑对话框里点红色「删除」会立即删除，请谨慎操作。
- 眼睛图标是页面级开关：点任意一个密码框旁的眼睛，会同时切换该页面所有密码框的明文/隐藏状态。
- 解锁密钥只保存在内存中：应用进程被杀或重启后，收藏夹会自动回到锁定状态；但离开页面本身不会自动锁定，建议离开前手动点顶栏锁图标。
- 编辑对话框点外部空白处会直接关闭且不保存，未点「保存」的修改会丢失。
- 条目卡片单击负责复制或打开安全链接，长按进入复制/编辑/删除界面；四点手柄单独负责拖动排序。

---

## 13. 小游戏

**如何到达**：底部导航栏「导航」页 → 点击「小游戏」进入（默认收纳在「导航」页中；可在 设置 → 底部导航 设为直接显示在底栏）。主页「小游戏」模块可直达用户在 **设置 → 子页面设置 → 主页 → 小游戏快捷入口** 中勾选的游戏。共有八个独立入口：2048 · 4×4、2048 · 5×5、2048 · 6×6、贪吃蛇、俄罗斯方块、自定义扫雷、一花色蜘蛛纸牌和本地双人围棋。页面顶部安全间距与其他主页面一致；进入任一游戏后底栏自动隐藏。游戏列表和各游戏中未单独设色的文字都会使用当前主题前景色，在亮/暗模式下自动变黑/白。

### 游戏列表页（标题「小游戏」）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 页面列表 | 上下滑动 | 浏览八张游戏卡片：三种 2048，以及「贪吃蛇」「俄罗斯方块」「扫雷」「蜘蛛纸牌」「围棋」。每个入口都有独立存档、最高分或最高提子，以及「累计游玩」时长；旧版本的 2048 存档继续属于 4×4。时长不足一分钟时显示「不足 1 分钟」。各游戏的移动、消除、胜负或围棋提子等特色统计请到「导航 → 统计 → 小游戏战绩」查看。 |
| 开始 | 单击 | 仅当该游戏没有存档时显示；从零开始一局新游戏并进入对应游戏页。 |
| 继续 | 单击 | 仅当该游戏有存档时显示（上次暂停或中途退出时自动保存）；读取存档、恢复棋盘和分数接着玩。若存档数据损坏无法解析，自动降级为开新局。 |
| 新游戏 | 单击 | 有存档时与「继续」并排显示；忽略存档、从零开一局。注意：旧存档不会立即删除，但新对局中一旦暂停或退出，新进度会覆盖旧存档；任意一局打完（游戏结束）也会清空存档。 |

### 贪吃蛇与俄罗斯方块 · 顶栏与分数区

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回（左上角箭头图标） | 单击 | 退出到游戏列表页；若本局尚未结束，退出前自动保存进度（之后列表卡片会出现「继续」按钮）。2048 也有同等作用的「返回」文字按钮，但使用自己独立的页面样式。 |
| 系统返回键 / 返回手势 | 按键或侧滑 | 与左上角「返回」完全等效：先自动存档（对局未结束时）再回到游戏列表，不会直接退出应用。 |
| 暂停 / 继续（右上角图标） | 单击 | 在暂停与继续之间切换；仅在游戏已加载且未结束时显示。点「暂停」的瞬间自动保存进度，棋盘上叠加显示「已暂停，进度已保存」浮层；暂停期间所有滑动和操作按钮均无效。再点一次恢复游戏。 |
| 自动暂停 | 切到后台、锁屏、打开系统界面、切换到其他 DeskCubby 页面或游戏页离开组合树 | 未结束的对局会立即进入暂停状态并排队保存；返回游戏时不会自行继续，需点右上角「继续」。重复的暂停/结束/读取操作按顺序持久化，避免旧存档覆盖新结果。 |
| 「分数」卡片 | 仅展示 | 实时显示本局得分。 |
| 「最高分」卡片 | 仅展示 | 显示历史最高分与当前分数中的较大值，即本局刷新纪录时会实时上涨。最高分在暂停存档和游戏结束时写入本地数据库。 |

### 2048 游戏页

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 顶部「返回」 | 单击 | 保存尚未结束的本局并返回游戏列表。2048 没有暂停状态；切到后台或离开页面仍会自动保存，重新进入可直接继续。 |
| 右下角回转箭头按钮（无限撤回） | 单击 | 恢复最近一次**有效移动之前**的棋盘与分数，并移除该步生成的新方块。每个有效移动都会压入历史栈，因此可以连续点击直到本局开局状态；无效滑动不会增加或清空历史。完整历史会随退出/后台存档，重新“继续”后仍可连续撤回。 |
| 「白天 / 黑夜」按钮 | 单击 | 手动覆盖当前 2048 页面自己的白天/黑夜配色，不改变应用其他页面主题。尚未手动覆盖时，2048 默认色板自动跟随应用当前亮/暗模式；手动选择会在当前游戏页面状态中保留，离开后重新打开时再次以应用主题为默认。 |
| 「动画开 / 关」按钮 | 单击 | 开关方块生成、移动、合并和得分飘字动画；关闭后每次操作直接显示最终棋盘。 |
| 「慢速 / 标准 / 快速」文字按钮 | 单击 | 在三档动画速度间循环切换：约 500 / 300 / 150 毫秒。选择立即保存并同时用于 4×4、5×5、6×6 三个 2048 入口；关闭动画时速度不会产生视觉效果。 |
| 「2048」标题与「分数」框 | 查看 | 页面只显示本局当前分数，不显示最高分或 2048.org 文案；每次合并的 `+分数` 在动画开启时向上飘出。 |
| 「新游戏」 | 单击 | 立即清空当前棋盘和撤回历史，从新局开始；历史最高分保留。 |
| 4×4 / 5×5 / 6×6 方形棋盘 | 在**页面任意位置**向上/下/左/右滑动（拖动距离需超过约 42dp，抬手时按主导方向判定，一次拖动只算一次方向操作） | 整盘方块向该方向滑动；相邻相同数字合并为其两倍，每次有效移动后随机生成一个新方块。棋盘无变化的滑动不生成新块，但仍计入“总操作次数”；“有效移动”只在棋盘确实变化时增加。五位数及更长数字会根据位数和格宽缩小字号并固定单行，不会折成两行。填满且无法合并时显示游戏结束浮层。 |

### 贪吃蛇游戏页

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 16×16 棋盘 | 向上/下/左/右滑动（约 42dp 阈值，抬手判定） | 改变蛇的前进方向。蛇每 220 毫秒自动前进一格；吃到食物 +10 分并变长，撞到墙壁或自己的身体则游戏结束。不能直接 180 度掉头：与当前实际前进方向相反的输入会被忽略。暂停时蛇停止移动，滑动无效。 |
| 「上」「左」「下」「右」方向键（棋盘下方四个圆形按钮） | 单击 | 与滑动等效，改变蛇的前进方向；同样忽略直接反向的输入。暂停或游戏结束时按钮置灰不可用。 |

### 俄罗斯方块游戏页

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 10×20 棋盘 | 仅观看（无滑动手势，操作全靠底部按钮） | 方块自动下落：基础每格 600 毫秒，每升一级加快 40 毫秒，最快 120 毫秒。方块落定后满行消除；新方块出生位置被占时游戏结束。 |
| 「下一块」预览面板（右侧） | 仅展示 | 预览下一个将出现的方块形状。 |
| 「行数」/「等级」面板（右侧） | 仅展示 | 显示累计消除行数与当前等级；每消 10 行升一级，等级越高下落越快。消行得分：一次消 1/2/3/4 行分别 +100/300/500/800 分。 |
| 左移 / 右移 | 单击 | 当前方块向左/右移动一格，被墙或已有方块挡住时不动。 |
| 旋转 | 单击 | 当前方块顺时针旋转 90 度；旋转后会碰撞时旋转直接失败（经典规则，无踢墙补偿），方块保持原样。 |
| 加速下落 | 单击 | 方块立即下落一格（软降）；已到底时会直接锁定。 |
| 硬降 | 单击 | 方块瞬间落到底部并立即锁定，随后结算消行。 |

### 自定义扫雷

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 初级 / 中级 / 高级 | 单击 | 快速填入 9×9·10 雷、16×16·40 雷或 30×16·99 雷。 |
| 列数 / 行数 / 雷数滑条 | 拖动 | 自定义 6–30 列、6–30 行及 1 到「格数减 1」颗雷；雷数会随棋盘缩小自动收敛到合法范围。 |
| 「开始」 | 单击 | 创建新棋盘并清除上一份扫雷暂停存档。第一次翻开的格子保证不是雷；空间足够时，首格周围八格也不会放雷。 |
| 棋盘格 | 单击 | 翻开格子；空白区域会自动展开相连的无雷格。踩雷后显示失败对话框，翻开全部安全格后显示完成对话框并自动标出所有雷。 |
| 已翻开的数字格 | **双击** | 一次翻开该数字周围所有**没有插旗**的未开格，不要求旗帜数与数字相同。旗帜位置若标错，双击可能直接翻到未标记的雷并判负；这是便于确认周边的快捷操作，不会替你判断旗帜是否正确。 |
| 棋盘格 | 长按 | 插旗或取消旗帜；旗帜数量不会超过本局雷数。超大棋盘可分别横向和纵向滑动查看。 |
| 右上角刷新 | 单击 | 再次打开自定义对话框。完成对话框中的「新游戏」也进入此对话框；「返回」回游戏列表。 |

### 蜘蛛纸牌（一花色，横屏）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 自动横屏 | 进入页面 | 页面锁定为可感应方向的横屏，离开后恢复跟随系统；底栏同时隐藏。 |
| 明牌或连续牌组 | 单击 | 第一次单击选中该牌到列底的同花降序牌组；再点另一列，把牌组移动到空列或大一阶的牌下。不可移动时会改选当前可移动牌，背面牌不能选。 |
| 「发牌 n」 | 单击 | 向 10 列各发一张明牌并减少剩余发牌次数；任一列为空时按钮置灰，需先把牌移回空列。 |
| 撤回 | 单击 | 恢复上一步移动或发牌；最多保留最近 100 步，撤回也恢复分数、完成组数和翻牌状态。 |
| 刷新 / 重开 | 单击 | 先弹出「重开蜘蛛纸牌？」确认框，说明当前进度确认后无法恢复。点「取消」保留当前牌局；点「确认重开」才创建新牌并清除暂停存档。只有旧牌局已经发生过移动、发牌或撤回时，确认换牌才记为一次失败；刚打开但没有操作的牌局不会被误记。 |
| K 到 A 完整同花序列 | 自动 | 形成后自动收走；若移牌后在来源列或目标列连续露出多组完整序列，会依次收走。完成 8 组即胜利。 |

蜘蛛纸牌初始 500 分，每次移动或发牌扣 1 分，每收走一组加 100 分；只有完成整局的得分才记为历史最高分。长牌列会自动压缩牌间距，保证横屏内仍能点到列底。

### 围棋（本地双人）

围棋不联网、不匹配对手，也不提供 AI；同一台设备上的两位玩家从黑方开始轮流落子。当前实现覆盖提子、禁自杀与简单劫，适合按双方约定的规则完成一局，但不会自动计算地域、贴目或胜负。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 9×9 / 13×13 / 19×19 | 单击 | 选择棋盘路数。新进入默认 9×9；选择不同尺寸，或已经落子后再次选择当前尺寸，会先弹出重开确认，确认后清空当前棋局。 |
| 棋盘交叉点附近 | 单击 | 当前一方落子。触控会吸附到整块棋盘中最近的交叉点，不必精确点中很小的圆形命中区；已有棋子、棋盘外、自杀棋和会立即还原上一局面的简单劫都不会落盘，页面显示对应错误。合法落子会自动移除无气的对方棋串并累计提子数；0.12.0 在每次合法落子后发布新的界面状态，所以棋子、最后一手、轮次和手数会立即刷新，可继续交替落子。 |
| 黑方落子 / 白方落子与“第 n 手” | 查看 | 显示当前轮到谁、当前手数，以及“黑提 / 白提”累计值；最后一手用棋子中心小点标出。 |
| 停一手 | 单击 | 当前方放弃这一手并换方；连续两方都停着时结束棋局。上一方已停着时，状态区会提示再次停着将结束。 |
| 清空重开 | 单击 | 弹出确认框；确认后以当前棋盘尺寸开始新局，未确认不会清除进度。 |
| 棋局结束对话框 | 查看/单击 | 显示黑、白双方提子数。“再来一局”用当前尺寸清空重开，“返回”回到游戏列表。DeskCubby 不自动按数子或数目规则判断胜负，请双方按采用的规则自行结算。 |

围棋中途返回或应用退到后台会保存本机进度，列表卡片可继续；最高值按本局双方提子总数记录为“最高提子”。落子次数、提子总数、停着次数和完成棋局数进入本机 Room 特色统计。围棋存档、最高提子、特色统计、累计游玩时长和主页围棋快捷入口都属于 Android 本机数据；其中围棋相关项目明确从 v31 投影排除，不随 JSON、云端应用 JSON 或 Windows 导入迁移。

### 小游戏特色统计

**如何到达**：底部导航「导航」→「统计」→「小游戏战绩」。统计按游戏 ID 独立累计，三种棋盘尺寸的 2048 不会混在一起。

| 游戏 | 累计指标 |
| --- | --- |
| 2048 · 4×4 / 5×5 / 6×6 | 总操作次数、有效移动、方块合并、历史最高方块和达到 2048 的获胜次数。“总操作”包含游戏接受但没有令棋盘变化的方向输入；“有效移动”只统计棋盘变化。0.10.0 不再显示失败次数或胜率，撤回也不会倒扣已经发生的累计动作。旧 Room/备份里的失败字段仍可兼容读取和往返，但不会继续增加。 |
| 贪吃蛇 | 吃到食物总数、历史最长蛇身、撞墙/撞到自身的失败次数。 |
| 俄罗斯方块 | 落定方块总数、消除行数、一次四消次数、出生区被占的失败次数。 |
| 扫雷 | 揭开格数、累计排雷、手动插旗次数、获胜次数、失败次数和胜率。“累计排雷”只在成功清空一局时增加该棋盘的雷数；取消旗帜不会倒扣插旗次数，胜利后自动补旗不算手动插旗。 |
| 蜘蛛纸牌 | 成功移牌次数、发牌次数、撤回次数、获胜次数、主动放弃已操作牌局的失败次数和胜率。无效点击不会增加移动次数。 |
| 围棋 | 合法落子次数、提子总数、停着次数和双方连续停着完成的棋局数。应用不自动统计地域胜负，因此不显示胜率。 |

特色统计写入 Room v13，操作增量与游戏存档/最高分按同一持久化队列依序提交，避免快速退出时旧状态越过新状态。计数达到 `Long` 上限时饱和，不会溢出变成负数。

### 游戏结束

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 2048 浮层中的「撤回」 | 单击 | 恢复最后一次有效移动并继续本局；仍可继续使用页面右下角的回转箭头撤回按钮。历史最高分不会回退。 |
| 2048 浮层中的「再试一次」 | 单击 | 立即重开一局，分数和撤回历史清零。 |
| 贪吃蛇/俄罗斯方块对话框「再来一局」 | 单击 | 立即重开一局全新游戏（分数清零）。扫雷、蜘蛛纸牌和围棋使用各自的新游戏入口。 |
| 贪吃蛇/俄罗斯方块对话框「返回」或外部空白 | 单击 | 关闭对话框并回到游戏列表。扫雷、蜘蛛纸牌与围棋的完成对话框需明确点按钮。 |

### 小技巧

- 存档规则：贪吃蛇/俄罗斯方块点「暂停」或中途退出时保存；2048 每次离开或进入后台时直接保存棋盘、分数和完整撤回栈。列表页随后出现「继续」+「新游戏」。
- 不必先手动点暂停：切到桌面、锁屏、打开系统设置或离开游戏页都会保存。贪吃蛇和俄罗斯方块返回时保持暂停，2048 没有计时器或暂停层，可直接继续滑动。
- 2048 使用独立的经典数字方块色板，但默认白天/黑夜会跟随应用亮/暗模式；在当前游戏页手动切换后暂时使用用户覆盖，重新进入时再跟随主题。其他游戏继续使用当前主题色，列表、分数区、按钮和游戏内普通文字也会随主题前景色切换，避免暗色主题出现黑字或亮色主题出现白字。
- 游戏结束的瞬间最高分即已记录，即使随后直接点对话框外退出也不会丢分。
- 滑动判定细节：2048 的页面任意位置和贪吃蛇棋盘上的滑动需拖动超过约 42dp 才生效，且在手指抬起时才结算方向；一次拖动只算一次操作，长按拖着不放不会连续触发。2048 达到五位数后仍保持单行，字号会继续随位数缩小。
- 2048 动画按方块移动、生成、合并和得分飘字分别处理；右上角关闭动画后直接显示最终状态，也会尊重系统的「移除动画 / 减少动态效果」设置。
- 2048 撤回恢复棋盘和分数，但不会回退随机数生成器；撤回后重新做相同方向的移动，新生成方块的位置或数值可能不同。
- 贪吃蛇不能直接 180 度掉头——想回头需先转 90 度再转一次。
- 俄罗斯方块没有滑动手势，只能用底部五个按钮操作；2048 页面任意位置滑动即可，贪吃蛇则棋盘滑动即可（贪吃蛇额外提供方向键按钮）。
- 2048 没有游戏暂停按钮也没有时间压力；右上角的暂停/播放图标只控制动画，离开页面或进入后台会自动保存。贪吃蛇和俄罗斯方块的暂停会真正停住自动移动/下落。
- 「新游戏」不会当场删除旧存档，但新对局中第一次暂停或退出就会用新进度覆盖它——想保住旧存档就只用「继续」进入。
- 存档与最高分/最高提子全部保存在本机数据库，无需网络、无需任何账号或 AI 配置。围棋是同设备双人，不会匹配联网玩家或调用模型。
- 每个游戏入口只在引擎已加载、对局实际进行且页面位于前台时累计时长；暂停、配置棋盘、结算画面和旋转重建都不计入。每 30 秒检查点保存一次，离开页面、切到后台或锁屏时再提交余下时间；快速暂停→恢复也不会让旧的异步结束覆盖新会话。累计值使用单调时钟，写入应用私有 `engagement/engagement-times-v1.json`，采用备份恢复与回读校验。全部累计游玩时长都不进入 v31 应用 JSON、Android 云备份或设备迁移；既有七个游戏/变体的 Room 特色统计继续进入 v31 并按指标取较大值，围棋特色统计则留在 Android 本机并从 v31 排除。

---

## 阅读页面（Android）

**完整路径**：底栏「导航」→「阅读」。阅读默认收纳在导航页；也可前往 **设置 → 底部导航**，打开「阅读」条目的「底栏」开关，或把它设为默认启动页。

### 书架与导入

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 右下角「+」 | 单击 | 打开 Android 文件选择器，仅接受 TXT 与 PDF。DeskCubby 取得该文档的持久只读授权，原文件仍留在用户选择的位置，不复制、不改名也不删除。TXT 最大 32 MiB，自动识别 UTF-8/UTF-16 BOM，并在严格 UTF-8 失败时兼容 GB18030；PDF 最多 20,000 页。 |
| 右上角「书架设置」（齿轮） | 单击 | 打开书架设置；选择「列表」或「两列封面」，并开关「显示封面下方书名」与「显示阅读进度百分比」。书名开关只在两列模式出现；关闭后隐藏封面下方的重复标题，但没有图片的 TXT 默认封面仍直接显示书名。点对话框「保存」后立即应用。列表模式继续显示文件类型、书名与累计阅读时间；进度开关开启时追加百分比。 |
| 书籍卡片/封面 | 单击 | 打开上次阅读位置。最近打开的书排在前面；两列模式显示可选的封面下方书名、可选进度和封面。封面按卡片实测宽度限制解码、像素和缓存；PDF 优先复用已验证缓存或 Android 文档提供方的安全缩略图，取不到时显示 PDF 类型占位。没有手动图片的 TXT 会把书名居中绘制在默认封面上，而不是只显示通用类型图标；内存分配失败也只让该封面回到安全默认，不应令书架退出。 |
| 两列封面右上角「修改封面」（图片图标） | 单击 | PDF/TXT 都可选择一张 SAF 图片作为手动封面；选择只保存持久只读 URI，不复制或改写图片。已有覆盖时，PDF 可点「恢复自动封面」回到安全缩略图或占位，TXT 可点「移除封面」。系统缩略图不保证每个文档提供方都支持，也不承诺一定是第一页。 |
| 「正在打开」左上角返回 / 系统返回 | 单击或返回手势 | 取消仍在解析的大文件并回到书架；已取消的旧加载任务不会稍后把阅读页重新打开。 |
| 书籍卡片右侧删除 | 单击后确认 | 只从书架移除记录并释放不再使用的文档授权，不删除原 TXT/PDF；已经产生的累计阅读时长及其最后书名继续保存在本机阅读统计中。 |
| 空书架「导入小说」 | 单击 | 与右下角「+」相同。损坏、过大、无法随机读取的 PDF 或失去授权的文档会安全失败，不会留下半条书架记录。 |

### TXT 与 PDF 阅读

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| TXT 正文 | 上下滑动 | 文件会按约 1,800 字符划成稳定逻辑页，每页末尾显示页码；屏幕尺寸、字号或方向变化不会改变逻辑页编号。首个可见页与页内位置会向下取整到 5% 一档（0/5/…/95），稳定约 600 毫秒后保存；例如停在第 13 页的 65% 处会保存为 13.65，快速滑动不会为每一行排队写磁盘。 |
| TXT 正文文字 | 长按后拖动选择柄 | 选择任意文字并使用 Android 系统浮动菜单复制；选择只作用于显示内容，不修改原 TXT。 |
| PDF 正文 | 上下滑动 | PDFium 通过 SAF 文件描述符直接打开原文件，不复制、不改写；整份 PDF 以连续页面从上到下按需渲染，当前位置按真实页和向下取整的 5% 页内检查点保存，增强与兼容视图都会恢复到页内位置。打开失败、页数异常、首个恢复/可见页渲染失败、native 链接或内存失败、首屏超过 30 秒时，提示后自动切换到系统 `PdfRenderer` 连续兼容视图，无需重新导入。字体大小、行距和段距只影响 TXT。 |
| PDFium 增强视图（Android 8+） | 双指缩放 / 单指拖动 | 增强视图加载成功后可用双指放大或缩小连续 PDF：手势期间内容围绕捏合中心实时跟随手指（矩阵变换，不重新渲染页面），**松手重新渲染后页面大小与位置和松手时保持一致**（按捏合中心锚定滚动位置）；单指拖动是自由二维平移，每次拖动同时移动水平和垂直方向（可以画圈），不再把一次动作判定为横向或纵向；阅读设置中的 50%–300% 是每次打开时使用的持久基准比例，滑杆按 1% 步进。增强视图提供真实页码、双色显示、内嵌文字搜索计数/跳页和文本目录扫描；不承诺搜索高亮，也不提供长按文本选择覆盖层。 |
| “增强 PDF 视图未能加载”提示条 | 单击“重试增强视图”或关闭 | 自动回退后可在当前阅读页重试一次增强视图；不重试也可继续用兼容视图阅读。一次瞬时失败只影响当前打开过程，不会永久禁用之后重新进入的增强尝试。反复失败时继续使用兼容视图更安全。 |
| PDF 兼容视图 | 放大后拖动 | 兼容视图同样支持自由二维平移：单指拖动同时移动水平和垂直方向，全部页面共用同一个横向偏移，不会出现只移动当前页、下一页又跳回原位。 |
| PDF 内嵌文字 | 搜索或扫描目录 | PDFium 能从含内嵌文字的 PDF 读取文本，用于统计匹配并跳到结果页、自动生成目录；当前不绘制匹配高亮，也不提供长按选择复制。纯图片扫描件没有可提取文字时保持无结果，DeskCubby 不进行 OCR。 |
| 左上角返回 / 系统返回 | 单击或返回手势 | 保存当前位置、结算本次前台阅读时间并回到书架；离开阅读正文后恢复底部导航。 |
| 顶部目录按钮（书本图标） | 单击 | 从左侧打开「目录」侧栏。TXT 会扫描整本书，并把首页连续目录中与后文同名的条目合并到后文正文位置；可识别带普通/全角空格的“第 1 章”、中文数字、中英文卷/回/幕、Markdown 标题等，也可叠加自定义整行正则。受支持的 PDF 增强视图会在首屏成功后逐页扫描内嵌文本层自动生成目录；扫描中显示进度提示，兼容视图和纯图片 PDF 不伪造目录。 |
| 顶部搜索按钮（放大镜） | 单击 | 展开整书搜索栏。输入文字后显示「当前项 / 总项数」；上一项、下一项循环跳转，关闭会清空查询。TXT 跳到包含结果的逻辑页并高亮当前页匹配；PDFium 增强视图统计内嵌文字匹配并跳到对应页，但当前不在页面上绘制高亮。兼容视图没有文本层，不显示 PDF 搜索按钮。 |
| 顶部页码按钮（带编号列表图标） | 单击 | 打开「跳转页数 / 进度」：可输入 1 到总页数的页码，也可拖动进度滑块；确认后滚动到目标页。TXT 对没有原始页码的文件也有效，PDF 使用真实页数。 |
| 右上角设置 | 单击 | 打开本地阅读设置，并显示当前书累计阅读时间。 |
| 屏幕中央（已开启全屏纯净模式） | 单击 | 唤醒已隐藏的书名、工具栏、页码与系统栏；再次点中央会隐藏组件。控件悬浮覆盖页面顶部，正文平面不会重排或缩放，因此屏幕中央文字保持原位；顶部少量内容可能暂时被控件遮盖。正文两侧的普通滚动/缩放仍按原手势工作。 |

### TXT/PDF 目录侧栏、搜索与跳转对话框

| 控件 | 操作 | 效果 |
| --- | --- | --- |
| 目录章节条目 | 单击 | 关闭侧栏并跳到章节开头；当前页所在章节条目会高亮。TXT 与 PDF 目录都从原书文本临时派生，不会改写或复制原书。 |
| PDF「正在扫描文本目录」 | 无（自动） | PDFium 增强视图首屏加载成功后，应用按页读取内嵌文字并逐步补全目录；扫描时仍可阅读。系统 `PdfRenderer` 兼容视图没有文本层所以不扫描，纯图片扫描件会提示没有可用于自动目录的文字。 |
| 搜索输入框 | 输入文字 | 搜索整本 TXT，或受支持 PDF 增强视图的文本层；查询最多 128 个字符，结果有安全数量上限。大小写字母按不区分大小写匹配。 |
| 上一项 / 下一项 | 单击 | 在搜索结果中循环移动并滚动到对应逻辑页或 PDF 位置；没有匹配时按钮不可用。 |
| 「按页数或进度跳转」 | 单击 | 关闭目录侧栏并打开统一跳转对话框。 |
| 页码输入框 | 输入数字 | 只接受范围内的整数；空白、0 或超过总页数时显示错误且「跳转」置灰。 |
| 阅读进度滑块 | 拖动 | 按 0–100% 选择目标逻辑页/真实 PDF 页，旁边实时显示百分比并同步页码输入框。 |
| 「跳转 / 取消」 | 单击 | 「跳转」前往目标页；「取消」保留当前位置。 |

### 阅读设置与横竖屏

| 设置 | 范围与效果 |
| --- | --- |
| 背景颜色 | 白色、纸张、棕褐、护眼绿、夜间五种方案，另有带调色板图标的「自定义」色块。点自定义后可用 HSV 蜂窝色盘、滑杆或 Hex 选择任意不透明颜色。该背景同时作用于 TXT 和 PDF 的页面显示，不改写原文件。 |
| 字体 / PDF 前景色 | 「自动」会按当前背景选择可读深/浅色；点右侧色块可用同一取色器设置任意字体色。TXT 直接使用该文字颜色；PDF 把页面显示映射成所选前景/背景双色，选择夜间背景与浅色前景即可得到黑底白字，原 PDF 内容和格式不变。 |
| 全屏纯净模式 | 开启后隐藏顶部书名/工具栏、底部页码和 Android 系统栏，让正文占用更多空间。回到正文后点屏幕中央可显示或再次隐藏组件；显隐只改变覆盖层，不移动正文，显示时允许顶部内容被短暂遮盖。 |
| 字体大小 | 12–38 sp，仅影响 TXT。 |
| 行间距 | 1.0×–2.4×，仅影响 TXT。 |
| 段间距 | 0–36 dp，仅影响 TXT。 |
| PDF 缩放 | 50%–300%（按 1% 步进），用于连续 PDF 打开时的基准比例；Android 8+ 的 PDFium 增强视图加载成功后仍可在正文中双指临时缩放，兼容视图按保存比例渲染。 |
| 屏幕方向 | 跟随系统、竖屏、横屏。设置在阅读正文中立即生效；退出阅读页后恢复跟随系统，不会永久锁住应用方向。 |
| 智能章节 | 选择「仅智能」「仅自定义」或「智能 + 自定义」。仅自定义适合章节格式固定但内置规则不认识的小说；组合模式会合并两者的匹配结果。规则变化后当前 TXT 会立即重新解析目录。 |
| 自定义整行正则 | 最多 1,024 字符，只在“仅自定义”或“智能 + 自定义”时显示。正则匹配完整标题行，例如 `^(第.+章|Chapter\s+.+)$`；格式无效时会标红且不能保存，留空表示不追加规则。 |
| 章节标题最长字符数 | 20–240，默认 160。超过上限的长段落不会被误当章节；标题本来较长时可适当调大。 |

设置保存在应用私有 `reader/reader-state-v1.json`（内部 schema v7），其中保存书架不透明 `content://` URI、标题/类型、可选封面 URI、逻辑页/段落位置、0/5/…/95 页内偏移、两列封面下方书名开关、其他显示偏好、PDF 基准缩放、章节规则和有界进度账本，没有小说正文、搜索词或派生目录。schema v7 继续读取 v1–v6，旧状态的页内偏移默认为 0；初始化、指纹计算、整书搜索和大文件解析都在有界后台 I/O 中完成。正式状态损坏、超限或提交失败时会保留原文件、在书架显示保护提示并停止后续写入，缺少正式文件时只从已验证的 pending 副本恢复。

跨设备进度有两条明确路径：

1. **设置 → 应用数据 → 导出 JSON / 自动备份**：v31 携带最多 500 条 URI-free 进度记录。另一台 Android 设备导入 v31 后，再通过书架「+」导入内容完全相同的 TXT/PDF，即可按文件字节与类型的 SHA-256 指纹跳转；先导入同文件再恢复备份也可以。Windows 0.7.0 仍只支持 v29，不能导入 v31。
2. **设置 → 应用数据 → 云端同步 → 新增/编辑配置 → 同步内容 → 阅读进度**：勾选后使用独立的 `reading/v1/progress.json` 自动按更新时间合并，不必恢复整份应用 JSON。新建配置默认勾选；旧配置升级后不会被静默开启，需要手动编辑并保存。

对外进度记录只包含 64 位十六进制文件指纹、TXT/PDF 类型、页/段位置、总页数和更新时间，不含 Android 私有的 5% 页内偏移、书名、`content://` URI、封面或正文；跨设备恢复因此仍从记录页的页首开始。文件哈希仍可能被拿来识别一个已知文件，云对象也没有端到端加密，请只同步到可信服务。书架/封面/显示偏好和阅读累计时长不进入 v31；同内容原书仍需在新设备重新通过 SAF 导入。阅读时间与小游戏时间继续写入 `engagement/engagement-times-v1.json`，只统计前台并每 30 秒回读校验保存。0.13.2 把该文件内部 schema 升至 v2：每本产生阅读时长的书会同时保存最后已知书名；旧 schema-v1 的时长原值无损读取，并会在仍有书架条目时补齐书名。此文件仍不进入 v31 或 Android 系统备份。

---

## 统计中心（Android）

**如何到达**：底部导航「导航」→「统计」。新安装默认把“统计”收在导航页；也可前往 **设置 → 底部导航**，打开“统计”的「底栏」开关或设为默认启动页。原来的“手机使用时间”和“健康”仍是可配置的独立页面，但新安装默认从统计中心进入它们。

### 统计总览

总览以自适应卡片网格展示所有已经有统计口径的内容。卡片都可单击进入详情；没有授权或没有可信记录时显示“—”或说明文字，不会把未知日期补成 0。

| 卡片 | 总览内容 | 单击后 |
| --- | --- | --- |
| 日记 | 总字数、日记篇数、当前连续记录天数，以及最近月份迷你图。 | 进入日记统计：日记总数、总字数、当前/最长连续天数，以及截至当前月的最近 24 个月字数柱状图；缺日记的自然月显示 0，点柱形查看具体月份和值。 |
| 手机使用时间 | 最近 7 个自然日中**实际已记录**的总时长、记录天数和今日值。 | 打开完整手机使用时间页面，可继续选择设备、应用、范围与图表。统计关闭时保留历史，并明确显示“统计已关闭”。 |
| 健康 | 今日步数、最近 7 天已记录天数和已知天数总步数。 | 打开完整健康页面，可切换步数、距离、活动热量和范围；未授权/无聚合值仍保持未知。 |
| 阅读时长 | 全部书籍前台阅读累计时长、有阅读记录的书籍数与书籍对比迷你图。 | 进入阅读统计，按累计前台阅读时长降序比较书籍；书籍从书架移除后，已记录的最后书名和历史时长仍会保留。只有 0.13.2 升级前已经移除、当时已失去书名映射的旧记录会显示“旧版未记录书名”。 |
| 小游戏战绩 | 全部游戏累计前台游玩时长、已有记录的游戏数与时长对比。 | 进入小游戏战绩：先看八款游戏/变体的时长图，再按游戏查看最高分或最高提子、特色累计指标和适用游戏的胜率。围棋没有自动胜负口径，因此只显示落子、提子、停着和完成棋局。普通离开页面不算失败。 |
| Agent Token | Provider 报告的总 Token、Agent 运行数、缓存率，以及输入/输出/缓存/推理迷你图。 | 进入 Agent 用量：查看运行数、模型调用数、已报告/未报告调用、输入/输出/总 Token、缓存输入、推理 Token 和缓存率。只累计响应里的 `usage`，不估算缺失值。 |

统计中心只汇总已有本机数据，不开启新的采集权限。手机使用时间/健康是否采集仍分别由 **设置 → 子页面设置 → 手机使用时间 / 健康** 控制；阅读和游戏时长仍留在私有 engagement JSON，小游戏特色指标和 Agent 用量留在 Room v13。统计页本身不复制日记正文、书籍正文、媒体或系统事件原始流。

---

## 14. 手机使用时间

**如何到达**：默认路径为底部导航「导航」→「统计」→「手机使用时间」。也可在 设置 → 底部导航 把“手机使用时间”直接放到底栏或单独收进导航页。统计默认关闭；首次使用先到 **设置 → 子页面设置 → 手机使用时间** 开启并保存，再回到本页点「打开系统设置」，在 Android 的「使用情况访问权限」页面允许 DeskCubby。开启且已授权后，每天第一次打开 DeskCubby 也会自动尝试采集一次；当天后续打开不会重复采集。

### 顶栏与授权状态

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 刷新（右上角） | 单击 | 读取 Android 仍允许访问的应用进入/退出前台、熄屏和锁屏事件，拼接实际前台区间并按本地午夜切分。从最早的**事件流完整自然日**开始重建过去日期，今天替换为最新实时值。系统通常只保留有限历史，DeskCubby 最多回看约 10 年；最老事件若从一天中途开始，该截断日及更早日期保持原状，不会被误写为完整日结。统计关闭或正在刷新时按钮置灰。进入页面或从系统授权页返回时也会自动刷新。 |
| 加载按钮 | 无（加载中） | 页面首次检查开关、系统授权和历史时显示带进度环的「加载中」按钮；检查结束后才显示对应内容，不会短暂闪现「统计已关闭」。 |
| 「统计已关闭」提示中的「打开使用时间设置」 | 单击 | 跳转到 设置 → 子页面设置 → 手机使用时间。关闭统计不会删除已有历史。 |
| 「需要使用情况访问权限」提示中的「打开系统设置」 | 单击 | 打开 Android 特殊授权页面；它不是普通运行时权限弹窗，需要在系统列表中找到 DeskCubby 并手动允许，再返回本页。 |
| 读取失败提示中的「重试」 | 单击 | 再次读取。失败日期不会写成 0，也不会标记为已经日结。 |
| 应用每天首次打开 | 自动执行一次 | 在手机使用时间开关已开启且有 Usage Access 权限时，应用启动会检查本地日期；当天尚未成功采集才刷新一次。权限不足或读取失败不会写入成功标记，下次启动仍会重试。 |

### 设备历史与云同步

Android 0.14.0 不显示下载图标或「导出给 Windows」卡片。页面使用「设备历史」卡片：每次安装会生成一个稳定的随机设备 ID，默认设备名来自手机型号，可自行修改。该 ID 不读取 IMEI、序列号或广告 ID，也不会被普通 v31 导入覆盖。

| 按钮/状态 | 操作 | 效果 |
| --- | --- | --- |
| 「设备历史」中的设备按钮 | 单击 | 从底部打开设备列表。可选「所有设备」、本机或任一已导入/同步设备；每项显示设备名、8 位短 ID、平台和记录天数。 |
| 「所有设备」 | 单击 | 按日期和应用把所有设备时长相加，适合总览 A/B 手机；各设备的原始记录不会被改写。 |
| 任一设备 | 单击 | 只看这台设备的总览、应用选择、图表和每日明细。 |
| 设备名右侧编辑图标 | 单击 | 修改本机设备名。名称会随“多设备使用时间”同步；随机设备 ID 保持不变。 |

要让 A、B 手机互相看到记录：两台手机配置同一 WebDAV/S3 服务，进入 **设置 → 应用数据 → 云端同步 → 编辑配置 → 同步内容**，勾选「多设备使用时间」，选择「双向」并保存，然后各自执行一次「立即同步」。每台设备只采集自己；远端使用 `usage/v1/{deviceId}.json` 独立对象，因此不会争抢同一个总文件。同一设备/日期冲突时，已日结的 FINAL 记录优先于实时 OPEN；状态相同时保留采集时间较新的记录。

使用时间对象包含应用包名、日期、时区与前台时长，云端没有端到端加密，请只使用可信服务。v31 完整应用 JSON 也包含所有已知设备历史；选择「应用 JSON」同步时同样会上传这些内容，但远端应用 JSON 仍需用户确认恢复，不会由后台直接覆盖整套应用数据。

### 总览、筛选与图表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 总览数据卡 | 无（只读） | 不再显示“总览”标题。显示能成功读取到的最早本地日期、已记录日期数、当前统计对象的总使用时长、日均使用时长、单日最高与过去 7 天平均；时长统一用紧凑的 `H`/`M`（如 `1H 23M`）表示。 |
| 「统计对象」选择器 | 单击 | 从底部弹出应用选择列表，在「全部应用总时长」与历史中出现过的具体应用之间切换。优先显示应用图标和系统应用名称，仅在系统无法解析名称时回退包名；应用按当前日期范围内的使用时长从高到低排列。选具体应用后总览、图表和每日明细都切换到该应用。 |
| 统计图 | 单击图中数据 | 统计图显示在范围与图表类型按钮上方。柱状图按数值从低到高改变颜色；柱状图和曲线把最高值与最低值（0）叠加标在图内左上角和左下角，不额外侵占图表宽度。 |
| 范围：7 天 / 30 天 / 90 天 / 全部 | 单击 | 位于统计图下方，改变图表显示的本地自然日范围，不删除范围外历史。 |
| 一行内的直方图 / 曲线 / 格子图图标 | 单击 | 位于范围按钮下方；三个切换项只显示图标，不显示文字解释，选择柱状、折线或 GitHub 风格日历热力图，无数据日期显示为空。 |
| 图表中的柱、点或日期格 | 单击 | 在所点柱/折线点/方块上方悬浮显示日期和当天使用时间，不弹出新页面或对话框；选择具体应用后显示该应用当天的时长。再点其他日期即可移动提示。 |
| 「每日明细」行 | 无（只读） | 按日期倒序显示时长；当天标记「实时，可刷新」。 |

### 统计口径与隐私

- DeskCubby 读取 Android UsageStatsManager 的应用前台/后台、熄屏与锁屏事件，把每段真实前台区间按当前时区的本地午夜切分；统计口径可能与手机厂商的“数字健康/屏幕时间”不同。
- 第一次成功采集会尽量回填系统仍可访问且事件流完整的全部历史，最早成功日期成为「开始统计」日期。当天数据为可替换的实时状态；完整过去日期可由事件流重新日结。最老的截断日或某个读取失败区间不会写入假 0，也不会误标完成。
- 0.3.6 及更早版本曾按日期查询厂商的日汇总；部分系统会对多个查询重复返回同一个汇总桶，再被旧版标成不同日期，表现为微信等应用连续多天都是完全相同的高值（例如每天 `2H 20M`）。0.3.7 首次刷新会自动重建 Android 仍保留事件的可信日期并覆盖这些旧日结值；系统已经清理事件的更早日期无法可靠推算，因此会保留而不是猜测或清零。当天事件不可用时只对当天使用单日汇总回退。
- Room（使用时间表在 v11 引入，当前数据库为 v13）是本机和其他设备使用时间的唯一运行时权威。首次升级会把 `usage-statistics.json`、旧根目录/恢复副本和 `usage-device-histories/` 中的有效缓存逐文件、事务化、幂等迁入 Room；损坏文件原样保留且不会挡住后面的有效文件。迁移成功后旧 JSON 不再参与运行时读取，JSON codec 只用于旧数据迁移、v31、导入导出和云同步，外部格式不变。关闭开关会停止本机后续采集，但不会删除 Room 历史。
- 应用名称和图标优先从 Android 已安装应用及启动器信息解析，例如不会再把抖音只显示成 `aweme`；系统确实无法提供标签时才使用包名回退。
- 授权并成功读取后页面直接显示统计内容，不再额外显示「本机私有统计」说明卡。
- 使用时间历史会进入 v31 应用 JSON，并可通过用户明确勾选的 WebDAV/S3「多设备使用时间」同步；系统 Usage Access 授权永远不迁移。导入后统计开关仍强制关闭，需在新设备自行开启和授权；导入的历史按来源设备 ID 保留。
- 开启手机使用时间后，应用启动任务每天只在首次打开时尝试刷新一次；只有刷新成功才记住当天已处理，权限不足或临时读取失败会在下一次启动重试。
- 开启手机使用或步数任一统计后，应用会注册约每 6 小时运行一次的后台补采任务；Android 仍可能根据电量和系统策略延后实际执行时间。

---

## 15. 健康

**如何到达**：默认路径为底部导航「导航」→「统计」→「健康」；也可在 设置 → 底部导航 把“健康”直接放到底栏或单独收进导航页。首次使用先到 **设置 → 子页面设置 → 健康** 开启并保存，再回到本页。0.8.0 只使用已授权的 Health Connect 读取步数、距离和活动热量，不申请“活动识别”权限，也不使用手机系统计步传感器。

### 顶栏、数据源与授权

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 刷新（右上角） | 单击 | 从已授权的 Health Connect 按本地自然日聚合每日步数、距离和活动热量。统计关闭或刷新中按钮置灰，进入页面或从权限页返回时自动刷新。 |
| 「统计已关闭」提示中的「打开健康设置」 | 单击 | 跳转到 设置 → 子页面设置 → 健康。关闭统计保留已有本机历史。 |
| 「需要健康读取权限」中的「授权读取健康数据」 | 单击 | 打开 Health Connect 权限界面，请求只读步数、距离和活动热量；系统支持时还会一并请求后台读取，以便约每 6 小时补充日结。拒绝后不会生成假数据。 |
| 页面最下方 Health Connect 状态卡片中的操作 | 单击 | 说明/授权模块位于总览、图表、每日明细或空状态之后。可用时打开管理页面；Android 13 及以下需要安装/更新时优先打开 Google Play，商店无法处理时回退到安全 HTTPS 页面；Android 14+ 需要提供方更新时打开系统更新。完全不支持的设备会明确提示且不显示无效按钮。若所有系统入口都无法打开，页面会显示可重试的中英错误提示。 |
| 读取失败提示中的「重试」 | 单击 | 重新读取；失败日期保持未完成，可稍后安全重试。 |

### 总览与可视化

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 指标按钮：步数 / 距离 / 热量 | 单击 | 切换本页总览、三种图表和每日明细所展示的指标；步数显示整数，距离以 m 或 km 显示，活动热量以 kcal 显示。切换只改变展示，不重新读取或删除数据。 |
| 总览数据卡 | 无（只读） | 显示当前指标首次有可信值的日期、已记录日期数、总计和有数据日期的日均值。 |
| 范围：7 天 / 30 天 / 90 天 / 全部 | 单击 | 切换图表日期范围，不删除其他日期。 |
| 一行内的直方图 / 曲线 / 格子图图标 | 单击 | 三个切换项只显示图标；切换柱状、折线或 GitHub 风格日历格子视图。 |
| 图表中的柱、点或日期格 | 单击 | 在所点数据上方悬浮显示日期和当天的当前指标值，不打开新页面；无可信值时显示「无记录」。 |
| 「每日明细」行 | 无（只读） | 按日期倒序显示当前指标；当天标记「实时，可刷新」。Health Connect 无聚合值时显示「—」，不会把缺失值当作 0。 |

### 数据与隐私

- DeskCubby 不修改 Health Connect 原始健康记录，只读取系统聚合的步数、距离和活动热量。没有 Health Connect、没有授权或没有聚合值时保持无记录，不改用其他数据源，也不会从步数推算距离或热量。
- 当天记录可刷新；Health Connect 已完整读取成功的过去日期标记日结并保持不变。没有数据源、未授权或读取失败时不伪造结果。
- Health Connect 每日历史保存在 Room（健康表在 v11 引入，当前数据库为 v13），Room 是唯一运行时权威。升级时会把旧 `statistics/step-statistics.json`（继续兼容 v1/v2/v3）事务化、幂等迁入 Room；损坏源文件保持原样并可在修复后重试。旧 JSON 只保留为迁移源/格式兼容，不再参与运行时读取。关闭开关停止后续读取但保留 Room 历史。
- 步数、距离、活动热量历史和 Health Connect 授权都不进入 DeskCubby 应用 JSON、云同步、Android 云备份或设备迁移。v31 应用 JSON 导入后健康开关会强制关闭，必须在新设备重新授权。
- Health Connect 权限页可打开 DeskCubby 的「健康数据与隐私」说明，其中同样说明数据只用于本机总览和统计图。

---

## 16. 导航页与底部导航栏

**如何到达**：底栏「导航」标签（默认显示在底部导航栏）→ 进入导航聚合页。首次进入时，页面教学蒙版会说明卡片入口与排序方式。

### 顶栏

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 标题「导航」+ 副标题「快捷入口」 | 无（纯展示） | 标明当前处于导航聚合页；文字过长时省略号截断。 |
| 管理导航页（右上角调节图标按钮） | 单击 | 跳转到 设置 → 子页面设置 → 导航页，可切换一列/两列/三列布局、总开关描述、选择收纳页面，并编辑每个模块的名称、按钮底色、模块整体底色与描述；按返回可回到导航页。页面改名、换图标与底栏显示仍在 设置 → 底部导航。 |
| 完成布局更改（右上角对勾按钮） | 单击 | 仅在布局更改模式显示。点击后所有卡片上的四点手柄消失，返回普通浏览模式。 |

### 页面卡片网格

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 页面卡片（图标圆底 + 页面名称 + 可选简介） | 单击整卡 | 打开对应页面（重复点击不会叠开多份）；打开后底栏的「导航」标签保持高亮。卡片名称与图标跟随 设置 → 底部导航，简介来自 设置 → 子页面设置 → 导航页；描述总开关关闭或某页描述留空时只显示图标和名称。图标底色与卡片底色可在导航页设置中逐模块自定义，未自定义时按 3 色循环着色仅为装饰，无功能含义。 |
| 页面卡片 | 长按 | 进入布局更改模式：所有卡片右上角出现四点手柄，顶部出现提示条，右上角按钮变为对勾。再单击对勾退出。 |
| 卡片右上角四点拖动手柄（仅布局更改模式） | 按住后拖动 | 直接调整导航页卡片顺序；拖动命中同时考虑左右列位置和卡片真实边界，松手保存顺序。TalkBack 提供可用位置对应的「上移 / 下移」操作。平时卡片不显示手柄。 |
| 一列/两列/三列瀑布流 | 上下滑动 | 列数在 设置 → 子页面设置 → 导航页 中选择；每列按各自卡片的真实高度连续排列，短卡片下方不会为另一列的长卡片留出整行空白。列间与卡片间保留正常视觉间距。 |
| 默认收纳的卡片：笔记、浏览器、日期记录、诗词本、RSS 订阅、AI 聊天、收藏夹、阅读、小游戏、统计、小卡片 | 单击 | 进入各页面。卡片列表由 设置 → 子页面设置 → 导航页 中的收纳开关决定，且「首页」「导航」「设置」永远不会出现为卡片。手机使用时间和健康的新安装默认入口改为“统计”，两者仍可单独收纳；其采集开关默认关闭，详情会引导前往对应设置与系统授权。 |

### 空状态

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 提示卡片「还没有收纳的页面」 | 无（纯展示） | 当没有任何页面被收进导航页时显示。 |
| 管理导航页（文字按钮） | 单击 | 跳转到 设置 → 子页面设置 → 导航页，选择要收纳的页面。 |

### 底部导航栏（全局）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 底栏标签（默认：首页、日记、小巧思、导航、设置；「设置」标签始终显示、无法隐藏） | 单击（无长按功能） | 切换到对应主页面。选中项只用主题色区分图标/文字，不再绘制 Material 默认椭圆底色，因此底栏音乐频谱不会被一块椭圆遮住。切换标签会重置其他标签内部打开的子页；标签是否显示文字由设置里「底部导航」的显示标签开关控制，关闭后底栏变为纯图标窄条。 |
| 底栏自动隐藏行为 | 无（自动触发） | 以下情况底栏自动隐藏：键盘弹出时；进入非顶级页面时（如日记编辑器、笔记编辑器、吃历、热量估算进度、小巧思回收站、日常记录、底部导航/AI 设置子页）；在设置页打开某个子页时；打开一本 TXT/PDF 或进入任一小游戏对局时。 |

### 逐页软件教学蒙版（全局）

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 深色全屏蒙版 + 当前页说明卡 | 首次进入某个逻辑页面时自动显示 | 教学模式默认开启。主页面、日记编辑/吃历等嵌套路由、每个设置子页、书架/TXT/PDF、游戏列表与八个具体游戏/变体分别拥有稳定页面 ID，因此每页只需确认一次。 |
| 点击蒙版外部 / 系统返回 | 单击或返回手势 | 不会关闭，防止第一次说明被误触跳过。 |
| 「知道了」 | 单击 | 记录当前页面已确认并关闭蒙版；记录只在当前设备 DataStore 中保存，不进入 v31 JSON、Android 系统备份或设备迁移。 |
| 设置 → 关于 →「软件教学模式」 | 关闭 / 开启 | 关闭后所有页面不再弹教学，但已有逐页确认不会丢失；重新开启只显示尚未确认的页面。 |
| 设置 → 关于 →「重新显示全部页面教学」 | 单击 | 清除当前设备的逐页确认；教学模式开启时，随后访问每个页面会再次显示。按钮在尚无确认记录时置灰。 |

### 小技巧

- 每个教学蒙版都必须点「知道了」；要临时关闭或全部重播，请走完整路径 **设置 → 关于 → 页面教学**。
- 导航页卡片平时只响应单击；长按任意卡片进入布局更改模式后，所有卡片出现四点手柄可拖动排序，点右上角对勾退出。改名/换图标去 设置 → 底部导航；列数、模块名称、按钮底色、模块整体底色、描述与收纳去 设置 → 子页面设置 → 导航页（右上角调节图标直达）。
- 从导航页打开某个收纳页面后，底栏的「导航」标签会保持高亮，表示该页面归属导航聚合页。
- 切换底栏标签会重置其他标签内已打开的子页面（不保留浏览位置），编辑中请先保存再切换。
- 键盘弹出时底栏会整体消失，收起键盘即恢复，不是故障。
- 「设置」标签无法从底栏隐藏；「首页」「导航」「设置」三者永远不会作为卡片出现在导航页中。
- 把某页面在底部导航设置里设为隐藏，再到导航页设置开启收纳后，它就从底栏移到导航页卡片中；若全部取消收纳，导航页会显示空状态。
- 自定义描述最长 160 个 Unicode 字符；留空只隐藏该页描述，关闭总开关则一次隐藏所有描述。不同描述长度会自然形成各列不同高度的瀑布流。
- 模块名称最长 32 个字符；按钮底色与模块整体底色用取色器选择，可随时点「默认」恢复主题配色，设置随 v31 应用备份迁移。

---

## 桌面小卡片与同步小组件（Android）

**如何到达**：信息卡设计页位于底栏「导航」→「小卡片」，也可在 **设置 → 底部导航** 把“小卡片”放到底栏。桌面组件统一从 Android 系统的 **小组件 / 窗口小工具** 面板添加；当前应能看到可配置信息卡、“立即同步”和“强制上传/下载”三类 DeskCubby provider。

### 卡片列表

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 默认「今天 / Today」卡片 | 查看/编辑 | 首次升级会得到一张今天日期卡片，可直接编辑。 |
| 右下角「+」 | 单击 | 新建一份独立卡片草稿（默认 2×1），名称默认为「新小卡片 / New card」，背景色沿用当前主题主色。 |
| 编辑（铅笔） | 单击 | 打开设计器。草稿有修改时返回会询问「放弃 / 继续编辑」；右上角保存成功后才持久化，并按模板 ID 立即更新所有仍绑定这份模板的桌面实例。 |
| 删除（垃圾桶） | 单击并确认 | 删除这份可复用设计。已经放到桌面的实例保留各自最后有效快照，不会切成默认卡或消失；要换模板或移除实例，请在桌面长按该实例重新配置或移除。 |

> 0.16.0 起列表不再显示卡片预览，也不再提供「添加到桌面」按钮：请直接在系统桌面长按空白处 → **小组件 / 窗口小工具 → DeskCubby** 放置实例，然后长按实例选择「重新配置」套用某张设计。

### 设计器

| 设置 | 操作 | 效果 |
| --- | --- | --- |
| 卡片名称 | 输入 | 最多 80 个 Unicode 字符；不能为空。是否把名称显示在桌面卡片上由下方开关决定，关闭显示不等于删除设计名称。 |
| 桌面尺寸 | 不可编辑 | 0.16.0 起设计器不再提供大小设置；最终格数完全由 Android 启动器决定，添加后可在桌面长按卡片自由拉伸，卡片会按实际空间自适应内容。 |
| 「背景颜色」「文字颜色」 | 单击 | 打开与应用其他页面一致的 HSV/蜂窝取色器；保存为不透明 ARGB。 |
| 「选择/更换背景图片」 | 单击 | 通过系统文件选择器选图并持久保留只读授权。桌面渲染会有界采样，避免把超大原图直接送入 RemoteViews；「移除背景图片」只移除设计引用，不删除原图。 |
| 「显示卡片名称」 | 开关 | 控制桌面卡片顶部是否显示设计名称；关闭后内容获得更多空间，设计列表中的名称仍保留。 |
| 「空间足够时显示图标」 | 开关 | 控制主页模块或应用启动内容的图标是否可显示；实际空间不足时渲染器仍会自动隐藏图标。 |
| 「背景透明度」 | 拖动 | 0%–100%，每 5% 一档；同时作用于背景色、背景图片及其遮罩。0% 可做完全透明背景，但桌面最终合成效果仍取决于启动器。 |
| 「文字对齐」 | 选择 | 左侧、居中或右侧对齐名称和主要文字。 |
| 「文字大小」 | 拖动 | 75%–150%，每 5% 一档；用于在紧凑卡片和大尺寸信息卡之间调整层级。启动器仍可能因实际边界裁切过长文本。 |
| 内容「主页模块」 | 单击后选择 | 弹出选择器，分为「主页模块」与「应用模块」两组。主页模块包含日历、天气、每日诗词、今天、日期记录、连续记录、本月日记、日记总字数、最近日记、最近小巧思、快速输入、日常记录、饮食图片、随机旧日记、年度进度、网站快捷入口、笔记、小游戏快捷入口、记录概览、立即同步、强制上传/下载。应用模块包含 2048、贪吃蛇、俄罗斯方块、扫雷、蜘蛛纸牌、围棋（均在桌面直接游玩）、音乐可视化、阅读、使用时间总览/图表/应用排行（可选 3/7/30 天范围）与合并的云端同步。Android `RemoteViews` 无法直接承载编辑框、相机和系统选择器时，由非导出的私有 Activity 接续真实交互。 |
| 内容「应用启动」 | 单击后选应用 | 从系统可启动应用列表中按名称/包名搜索并选择；优先读取目标启动 Activity 或 launcher alias 图标，失败才回退 application icon，以 48dp 正常桌面图标大小放在卡片正中央。仍遵守名称/图标开关；空间不足或图标读取失败时居中显示应用名，不留空白。 |
| 保存（右上角） | 单击 | 名称有效，且应用按钮已选择目标应用时可用。DataStore 写入成功后退出编辑器，只更新所有仍绑定这份模板的实例；绑定其他模板或经桌面重新配置的实例保持不变。 |

### 从系统桌面添加与数据边界

长按 Android 桌面空白处 → **小组件 / 窗口小工具 → DeskCubby** → 拖到桌面；信息卡随后必须在配置页为这个实例选择已有设计。每个 App Widget ID 独立保存绑定模板与快照，不再回退到列表第一张。渲染时同 ID 模板仍存在就使用最新模板并回写实例快照；模板已删除就继续显示最后快照。只有删除桌面实例才移除该 App Widget ID 的绑定。桌面占用格数由启动器决定，拉伸后卡片按启动器报告的实际 dp 重新选择信息密度；通常可长按实例寻找“配置/编辑”来切换模板。

### 信息卡中的响应式直接操作

| 主页模块 | 启动器报告的实际空间 | 桌面操作 |
| --- | --- | --- |
| 每日诗词 | 宽≥170dp 且高≥56dp，或宽≥110dp 且高≥150dp | 同时显示诗词正文与“刷新 / 加入诗词本”；4×2 也完整显示。刷新与收藏通过私有广播进入唯一 WorkManager，完成后刷新卡片；诗词正文仍可整卡进入应用。 |
| 快速输入 | 宽≥160dp 且高≥56dp，或宽≥110dp 且高≥120dp | 显示仿输入框与发送按钮；4×1 即满足。Android `RemoteViews` 不能承载可编辑 `EditText`，点仿输入框打开 DeskCubby 私有真实输入对话框；点「发送」先弹出「发送到分类」（无分类或某个小巧思分类），选定后输入并发送，只有小巧思落库成功才关闭并刷新。 |
| 饮食图片 | 宽≥180dp 且高≥56dp；或宽≥150dp 且高≥120dp；或宽≥110dp 且高≥180dp | 3×1/4×1 即显示六个横排按钮（早餐/午餐/下午茶/晚餐/水果/夜宵），点餐别**直接打开相机拍摄**（相机不可用时回退相册），沿用应用内 SAF 写入链路；图片和今日日记真正落盘后才提示成功并刷新。 |
| 完整月历 | 宽≥200dp 且高≥220dp | 复刻主页本月完整星期行和日期网格，今天使用主题色高亮。 |
| 日期记录 | 宽≥170dp 且高≥70dp 显示最近一条；宽≥170dp 且高≥130dp 显示两条；宽≥160dp 且高≥200dp 显示四条 | 3×1/4×1 显示最近一条记录，3×2/4×2 显示最近两条，更大的显示四条；每项可直达日期记录，底部保留“添加”与“查看全部”。 |
| 最近日记 / 最近小巧思 | 宽≥180dp 且高≥240dp | 显示多项真实内容；最近日记逐篇打开对应日记，最近小巧思逐条进入对应内容，每项保持独立操作。 |
| 日常记录 | 宽≥180dp 且高≥240dp | 最多复刻四项实际录入入口及“管理”；点某项由私有 Activity 打开真实输入，只有今日日记写入成功后才刷新。 |
| 随机旧日记 | 空间足够显示具体内容 | 显示本次选中的具体日记并直接打开该篇，不先进入日记列表。 |
| 小游戏快捷入口 | 宽≥180dp 且高≥240dp | 按主页设置显示多项游戏，每项直接打开对应 2048 变体、贪吃蛇、俄罗斯方块、扫雷、蜘蛛纸牌或围棋。 |
| 立即同步 | 宽≥120dp 且高≥90dp 显示操作；宽≥180dp 且高≥180dp 显示完整状态 | 显示运行状态、进度、上次结果、错误和待确认应用 JSON 数量，并按总开关、启用来源及运行状态禁用按钮；可用时进入共用串行队列。 |
| 强制上传/下载 | 宽≥180dp 且高≥90dp 显示操作；宽≥180dp 且高≥180dp 显示完整状态 | 同样显示运行/进度/上次结果/错误/待确认 JSON；上传、下载按配置分别禁用，下载仍要求恰好一个启用来源。点击后先打开私有确认界面，确认后才排队。 |
| 任一主页模块空间不足，尤其 1×1、1×2 | 不满足该模块阈值 | 不挤入难以点击或会误导的控件，整张卡只保留跳转到对应应用页面。 |

0.11.0 为三个 provider 显式声明启用状态、图标、名称和 provider 元数据，并为 Android 12+ 补齐描述/预览，改善系统检测不到 DeskCubby 小组件的情况。若升级后面板仍没出现，请先回到桌面的小组件面板重新搜索 DeskCubby，必要时重启当前启动器或设备以清除其 provider 缓存；同时检查系统是否限制桌面放置或后台活动。应用可以补发更新并通过 WorkManager 保底，但不能绕过启动器明确禁止小组件或系统冻结后台的策略。

可复用卡片设计随 v32 JSON 备份，包含名称、颜色、内容类型、模块 ID、应用包名/标签、背景图片 URI、使用时间范围，以及名称显隐、背景透明度、图标显隐、文字对齐和字号。背景图片文件不嵌入；跨设备导入时，若本机没有该 `content://` 图片的持久读取授权，背景图会被清除而保留其他设计。每个 App Widget ID 的完整实例快照仍是设备瞬时状态，不进 JSON、Android 云备份或设备迁移；恢复设计后需要在新设备重新添加各实例。

### 云同步快捷组件

| 系统小组件 | 操作 | 效果 |
| --- | --- | --- |
| 「立即同步」 | 单击左侧文字区 | 把一次普通安全同步加入有联网约束的 WorkManager 队列；排队或运行期间，首页、通用卡片和两个独立系统组件会共同显示忙碌状态并禁用再次触发，不会再加入第二个动作。完成状态会显示本次上传/下载/冲突数量。 |
| 「撤回一次」 | 单击右侧按钮 | 撤销最近一次同步对本机日记的改动：被下载覆盖的文件恢复原内容，本次新建的文件移入日记回收站；无快照或同步进行中时按钮禁用。 |
| 「强制上传 / 强制下载」二合一组件左侧 | 单击“强制上传” | 这是用户主动添加到系统桌面的显式快捷动作，点击后直接排队。远端条件版本仍必须匹配，远端独有项目不会被删除。 |
| 同一二合一组件右侧 | 单击“强制下载” | 这是显式快捷动作，点击后直接排队。仅允许一个已启用云端来源；单一来源下本机必须仍匹配扫描快照，并发本机修改会保留为冲突副本，本机独有项目不会被删除。 |

两个同步组件都只显示无敏感信息的通用状态，如“已排队、正在同步/上传/下载、已完成、同步失败、请检查同步设置”，不会显示服务地址、路径、文件名、正文或凭据。它们要求已经在 **设置 → 应用数据 → 云端同步** 保存并开启至少一个可用配置。独立的系统强制组件是用户显式放置的快捷动作，通过非导出的私有 Receiver 直接排队；若希望每次操作前查看风险并确认，请使用通用小卡片中的“强制上传/下载”主页模块，或应用首页的强制同步模块。

如果只是想在 DeskCubby 首页查看同步状态和操作，请使用 **设置 → 子页面设置 → 主页 → 立即同步 / 强制上传/下载** 两个模块；它们属于应用首页，强制卡会在操作前显示确认，不需要把任何系统组件放到 Android 桌面。

---

## 17. 设置

**如何到达**：底部导航栏（或导航页）点「设置」进入设置主页。「外观与语言 / 应用数据 / 底部导航 / 关于」直接从主页进入；「主页 / 日记与媒体 / 浏览器 / 小巧思 / 诗词本 / RSS 订阅 / AI 配置 / 收藏夹 / 手机使用时间 / 健康 / 导航页」需先进入「子页面设置」。云端同步位于“应用数据”页面中的入口。诗词本、RSS、AI、收藏夹、底部导航、导航页及两种统计设置也支持从相应功能页面直接跳入，此时返回/保存后直接退出设置而非回到设置主页。

### 17.1 设置主页与通用操作

所有设置子页面的顶栏通用行为：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 返回箭头（左上角） | 单击 | 返回上级页面。若当前页有未保存修改，先弹出「设置尚未保存」对话框。设置主页不显示返回箭头。系统返回键/手势效果相同。 |
| 重置本页所有设置（右上角回转箭头） | 单击 | 把当前可编辑设置页的全部草稿恢复为应用默认值；这是一次草稿修改，仍需点「保存」才真正生效。误点后可返回并选择“放弃”恢复进入页面前的值。设置主页、只读/即时操作页不显示此按钮。 |
| 保存（右上角文字按钮，带保存图标） | 单击 | 保存当前子页面的全部修改并自动返回上级页面。仅在「当前页有修改且输入校验通过」时可点，否则置灰；无需保存机制的页面（设置主页、子页面设置、备份、AI 列表、关于）不显示此按钮。 |
| 「设置尚未保存」对话框 | 单击 | 提示「返回会丢失刚才的修改。」三个选项：「保存」（校验不通过时置灰）保存并返回；「继续编辑」留在当前页；「放弃」丢弃修改并返回。 |

设置主页本身：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「搜索设置」输入框 | 输入文字 | 按标题、描述和内置关键词（中英文均可，如「压缩」「热量」「webdav」）实时过滤出可跳转的设置分区；无结果时显示「没有匹配的设置」。 |
| 清除搜索（输入框右侧 × 图标） | 单击 | 清空搜索词，恢复完整菜单。仅在有输入内容时出现。 |
| 搜索结果条目 | 单击 | 自动清空搜索词并直接跳转到对应设置页面。 |
| 「外观与语言」菜单项 | 单击 | 进入外观与语言页（风格、多色主题、全局背景图片、字号、明暗模式和语言）。 |
| 「子页面设置」菜单项 | 单击 | 进入子页面设置列表（主页、日记、浏览器、小巧思、诗词本、RSS、AI 设置、收藏夹、手机使用时间、健康与导航页）。 |
| 「应用数据」菜单项 | 单击 | 进入应用数据页面，可管理自动保存、JSON 导入导出/整体预览，并从页内的「云端同步」卡片进入 WebDAV/S3 设置。副标题会根据自动备份与云同步状态变化。 |
| 「底部导航」菜单项 | 单击 | 进入底部导航设置（显示方式、默认页、排序、名称与图标）。 |
| 「关于」菜单项 | 单击 | 进入关于页（版本、页面教学、检查更新、桌面名称与软件图标）。 |

### 17.2 外观与语言

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「界面风格」分段按钮：原生 / 玻璃 / 有机未来 / 自定义 | 单击 | 切换全局视觉风格，下方同步显示说明。选择「自定义」后展开受控主题编辑器；需点顶栏「保存」才生效。自定义只映射到 Compose 主题角色，不加载 CSS、脚本、网络资源、任意选择器，也不改变页面结构。 |
| 自定义主题 →「基础渲染」：原生 / 玻璃 / 有机 | 单击 | 选择 Custom 主题沿用哪一种现有面板/布局渲染方式；颜色和视觉参数仍使用下方自定义值。 |
| 「浅色模式颜色」「深色模式颜色」中的 8 个角色 | 点颜色圆钮或输入 Hex | 分别设置页面背景、背景文字、基础表面、正文文字、卡片表面、次级表面、次要文字和边框，格式必须为 `#RRGGBB`。可通过取色器或输入框修改；文字/边框对比度不足时页面会提示，保存时自动调整到可读值。 |
| 「全局圆角」 | 拖动 | 0–40 dp，统一影响受控 Compose 形状。 |
| 「面板边框 / 面板阴影」 | 拖动 | 边框 0–4 dp（0.25dp 步进），阴影 0–16 dp。 |
| 「面板不透明度 / 面板内容间距」 | 拖动 | 不透明度 65%–100%；内容间距 75%–135%。 |
| 「页面切换动效」 | 拖动 | 0%–200%；0% 关闭应用页面切换动画，Android 系统“移除动画”设置优先级更高。 |
| 「明暗模式」分段按钮：跟随 / 浅色 / 深色 | 单击 | 选择深色模式策略，保存后生效。 |
| 「软件语言」选择：简体中文 / 繁體中文 / English / 한국어 / 日本語 | 单击 | 切换应用界面语言，保存后立即生效，覆盖全部页面与桌面小组件文案；首次打开应用也会先弹出语言选择页。 |
| 主颜色预设色块（6 个圆形色块） | 单击 | 把主颜色设为该预设色，Hex 输入框同步更新。 |
| 自定义主颜色（调色板图标圆钮） | 单击 | 打开「选择颜色」取色器对话框：原有实时预览、HSV 和 Hex 输入都保留。色相/饱和度/明度标签与各自滑杆压缩在同一行；新增蜂窝色盘，可直接点选某个六边形颜色，再用滑杆或 Hex 微调。「确定」应用、「取消」放弃。 |
| 「主颜色 Hex」输入框 | 输入文字 | 直接输入 #RRGGBB（例如 #42664D），格式错误时标红且无法保存。 |
| 副颜色「添加」按钮 | 单击 | 追加一个副颜色（自动挑选未用过的预设色）。最多 5 个，满 5 个后置灰。副颜色须 2–5 个、格式正确且互不重复才能保存。 |
| 副颜色圆形色块 | 单击 | 打开取色器对话框修改该副颜色。 |
| 「副颜色 N」Hex 输入框 | 输入文字 | 直接编辑该副颜色的 #RRGGBB 值。 |
| 删除副颜色（行尾 × 图标） | 单击 | 删除该副颜色。仅剩 2 个时置灰不可删。 |
| 「背景图片」预览 | 查看 | 未选择时显示占位；选择后按当前草稿的可见度和模糊实时预览。原图片留在系统文档提供方中，不会复制进应用。 |
| 「选择图片 / 更换图片」 | 单击 | 打开系统文档选择器并只接受图片。应用验证持久只读授权、128 MiB 文件上限和最大 32,768×32,768 像素边界；验证失败会提示且不替换草稿。 |
| 「移除」 | 单击 | 只把背景 URI 从本页草稿清除，不删除原图片；仍需点右上角「保存」。 |
| 「图片可见度」滑条 | 拖动 | 0–100%，每 5% 一档；0% 只显示主题底色，100% 显示完整图片 alpha。没有选择图片时置灰。 |
| 「背景模糊」滑条 | 拖动 | 0–40 dp，每 1 dp 一档；使用轻微超裁避免模糊边缘露白。没有选择图片时置灰。 |
| 「全局字号」滑条 | 拖动 | 在 80%–130% 之间以 5% 一档调整全局字体大小，右上角实时显示百分比。 |
| 「紧凑显示」开关 | 单击 | 开启后缩小列表与卡片间距，一屏显示更多内容。 |

### 17.3 子页面设置

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「主页」菜单项 | 单击 | 进入主页设置（问候语、模块、标题、排序与饮食按钮）。 |
| 「日记与媒体」菜单项 | 单击 | 进入日记与媒体设置。已配置日记目录时副标题显示「日记目录已配置」。 |
| 「浏览器」菜单项 | 单击 | 进入浏览器设置（默认主页、主题和电脑模式）。 |
| 「小巧思」菜单项 | 单击 | 进入小巧思设置（打开位置、内容显示与行高）。 |
| 「诗词本」菜单项 | 单击 | 进入诗词本显示设置（导入字体、字号、行距、对齐、出处、引号装饰和七言自动换行）。 |
| 「RSS 订阅」菜单项 | 单击 | 进入 RSS 设置（每个订阅的文章数量与摘要显示）。 |
| 「AI 设置」菜单项 | 单击 | 进入 AI 设置页（页面字体大小、回复框宽度、Agent 提示词与「AI 配置」子页入口）。 |
| 「收藏夹」菜单项 | 单击 | 进入收藏夹显示设置，可调整收藏夹卡片的最小行高；收藏内容仍需从 导航 → 收藏夹 页面解锁。 |
| 「手机使用时间」菜单项 | 单击 | 进入使用时间统计开关页；开启后仍需从手机使用时间主页面进入 Android「使用情况访问权限」授权。 |
| 「健康」菜单项 | 单击 | 进入健康统计开关页；开启后仍需从健康主页面最下方完成 Health Connect 授权。 |
| 「导航页」菜单项 | 单击 | 进入导航页设置（一列/两列/三列布局、收纳页面、模块名称、按钮底色、整体底色与描述）。 |

### 17.4 手机使用时间设置

**完整路径**：设置 → 子页面设置 → 手机使用时间。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「统计已关闭 / 统计已开启」开关 | 单击 | 修改是否允许 DeskCubby 后续采集手机使用时间；必须点右上角「保存」才生效。默认关闭。关闭后停止后续读取，但保留 Room 中已有历史。 |
| 权限与隐私说明 | 无（只读） | 提醒统计运行时保存在本机 Room；使用时间可按 v31/按设备 JSON 显式备份或同步，但不会靠 Android 系统备份迁移数据库。开启开关本身不会自动授予 Usage Access。保存后进入「导航 → 统计 → 手机使用时间」，点「打开系统设置」完成特殊授权。 |

系统授权与统计页面操作详见第 14 章。功能开关是普通 v31 设置字段，但导入任何应用 JSON 后都会被强制关闭；Usage Access 授权始终由 Android 管理，不能备份或迁移。使用时间历史本身按设备进入 v31。

### 17.5 健康设置

**完整路径**：设置 → 子页面设置 → 健康。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「统计已关闭 / 统计已开启」开关 | 单击 | 修改是否允许 DeskCubby 后续只从 Health Connect 读取步数、距离和活动热量；必须点右上角「保存」才生效，默认关闭。关闭后保留 Room 历史。 |
| 权限与隐私说明 | 无（只读） | 提醒应用只读 Health Connect 的三种指标，不使用其他计步数据源。每日历史只写入本机 Room，不进入 v31、云同步或 Android 系统备份。保存后进入「导航 → 统计 → 健康」，滚动到页面最下方按提示授权；拒绝、撤销权限或无数据源时不会伪造 0。 |

Health Connect、后台采样和图表用法详见第 15 章。功能开关虽出现在 v31 设置结构中，导入时仍会强制关闭；Health Connect 权限与三种指标历史不迁移。

### 17.6 主页设置

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「主页问候」菜单项 | 单击 | 进入独立的主页问候设置页，管理用户名和每日问候语。若当前主页模块设置尚未保存，会先出现未保存确认。 |
| 「显示模块边框」开关 | 单击 | 关闭后主页模块会更自然地连成一体。 |
| 主页模块的四点拖动手柄 | **按住垂直拖动** | 重新排序主页模块：被拖模块半透明跟手移动，目标插入位置显示主题色横线；松手落位，无需长按即可开始拖动。无障碍模式下提供「上移/下移」操作。 |
| 模块行「移除」文字按钮 | 单击 | 把该模块从主页移除（同时移除其标题显示设置），之后可在下方「添加模块」找回。 |
| 模块行「显示标题」开关 | 单击 | 单独控制该模块在主页是否显示标题文字。 |
| 「添加模块」区的模块按钮 | 单击 | 把该模块添加到主页模块列表末尾（默认显示标题）。共 21 种模块：日历、天气缓存、每日诗词、今天日期、日期记录、连续记录天数、本月日记数量、日记总字数、最近日记、最近小巧思、快速输入、日常记录、饮食图片、随机旧日记、年度进度、网站快捷入口、笔记入口、小游戏快捷入口、记录概览、立即同步、强制上传/下载；全部添加后显示「所有模块都已添加」。 |
| 「小游戏快捷入口」中的八个复选框 | 单击 | 分别控制主页「小游戏」模块内是否显示 2048 · 4×4、2048 · 5×5、2048 · 6×6、贪吃蛇、俄罗斯方块、扫雷、蜘蛛纸牌和围棋。默认勾选 2048 · 4×4、贪吃蛇、扫雷；允许全部取消，此时主页模块显示前往本设置页的提示。只有右上角保存后才生效。既有七个入口随 v31 恢复，围棋入口只保存于 Android 本机并从 v31 投影排除。 |
| 「饮食按钮」分段按钮：文字 / 图标 | 单击 | 选择主页饮食按钮显示文字标签还是自定义图标。 |
| 六个「xx图标」输入框（早餐/午餐/下午茶/晚餐/水果/夜宵） | 输入文字 | 可输入 emoji 或简短符号（最多 16 个字符）。任一图标为空时标红且无法保存。 |

#### 主页问候设置

**完整路径**：设置 → 子页面设置 → 主页 → 主页问候。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「用户名」输入框 | 输入文字 | 最多 32 个 Unicode 字符。模板中的 `{name}` 会替换为这里的名称；留空时使用简短的「你 / you」，不再代入「朋友 / Friend」。 |
| 「复制 {name}」按钮 | 单击 | 把字面占位符 `{name}` 复制到系统剪贴板，便于粘贴到下方中英文问候模板。 |
| 「今日预览」 | 无（只读） | 按今天的日期、当前语言、用户名和草稿列表实时显示主页问候效果。 |
| 每条问候的「中文」「English」输入框 | 输入文字 | 修改同一条问候的中英文版本；单个语言最多 40 个 Unicode 字符。至少填写其中一种语言，另一种为空时会回退使用已有语言。支持 `{name}` 占位符。 |
| 删除（每条右上角垃圾桶） | 单击 | 删除该条问候。允许全部删除；此时主页显示「今日概览 / Today's overview」。 |
| 「增加问候语」按钮 | 单击 | 在列表末尾增加一条空白问候，最多 100 条。空白条目需填写中英文至少一种后才能保存。 |
| 保存（右上角） | 单击 | 保存用户名与完整问候列表并返回主页设置；有未保存修改时返回会弹出确认框。 |

### 17.7 日记与媒体

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「日记目录」按钮 | 单击 | 打开系统文件夹选择器（SAF）选择日记 Markdown 存放目录，按钮下方显示当前目录名或「尚未选择」。 |
| 「媒体目录」按钮 | 单击 | 打开系统文件夹选择器选择图片等媒体的存放目录。 |
| 「今日日记文件名格式」输入框 | 输入文字 | 设置日记文件名日期格式（提示示例 yyyy-MM-dd），不能为空。 |
| 「图片命名格式」输入框 | 输入文字 | 设置图片文件命名规则（提示示例 {date}_{category}_{seq}），不能为空。 |
| 「默认 Markdown 模板」多行输入框 | 输入文字 | 新建日记的默认模板，支持 {title} 与 {date} 占位符。 |
| 「Markdown 阅读预览」中的 H1–H6 六个滑条 | 拖动 | 分别把一级到六级标题调整为 12–48 sp，旁边用对应标题实时预览。只影响日记与笔记的阅读预览，不改写 Markdown 源码；需点右上角「保存」才持久化。 |
| 「图片最大宽度 dp」「图片最大高度 dp」输入框 | 输入数字 | 只接受数字；留空或非数字时提示「图片宽度和高度必须填写数字。」且无法保存。 |
| 「自动压缩饮食图片」开关 | 单击 | 开启后对主页拍照、选图和日记中的餐别图片自动压缩。 |
| 「压缩质量」滑条 | 拖动 | 30%–95%、5% 一档；仅在压缩开关开启时可用。数值越低文件越小；压缩时最长边同时限制为 2560 像素。 |
| 「保存原图到系统相册」开关 | 单击 | 导入图片时把未压缩的原图另存到系统相册的 DeskCubby 相簿。 |
| 「记录照片拍摄地点」开关 | 单击 | 从照片 EXIF 读取拍摄地点，与热量一起写入媒体目录的 `dc-media.json`。在 Android 10+ 上开启时会弹出「媒体位置」权限请求；拒绝权限时相机拍摄的照片仍可记录位置，相册照片则可能读不到。已有地点会同时显示在日记阅读预览的照片下方与吃历放大查看器；旧名 `deskcubby-media.json` 仍可读取。 |
| 吃历「图片高度上限」滑条 | 拖动 | 80–320 dp、8 dp 一档，控制吃历中图片显示高度，右侧实时显示数值。 |
| 「显示餐别文字」开关 | 单击 | 关闭后吃历只显示图片，仍按早餐、午餐、晚餐的顺序排列。 |
| 「图片自动换行」开关 | 单击 | 关闭时单行横向滑动，开启后按每行数量换行显示，并展开「每行图片数量」选项。 |
| 「每行图片数量」分段按钮：2 / 3 / 2+3 自动 | 单击 | 仅在自动换行开启时显示。「2+3 自动」混合每行 2 或 3 张，让最后一行不留空位（如 4=2+2、5=3+2、7=3+2+2）。 |
| 「上传饮食图片后自动估算」开关（AI 热量估算） | 单击 | 前提：需先在 AI 配置中添加至少一个文字模型和一个图片模型，否则开关置灰并提示「需要先在 AI 配置中添加文字模型和图片模型。」开启后结果写入媒体目录的 `dc-media.json` 并显示在吃历，不再改写 Markdown 图片说明；开启时还必须选好下面两个模型才能保存。 |
| 「热量计算文字模型」「食物图片识别模型」下拉按钮 | 单击 | 弹出下拉菜单，从已有 AI 配置中分别选择文字模型与图片模型；无可选配置时按钮置灰，显示「请选择配置」。 |
| 「图片识别提示词」「热量计算提示词」多行输入框 | 输入文字 | 自定义热量估算两阶段的提示词，各最多 20000 字符。0.9.2 的内置文字提示词面向同一天多图统一计算；旧内置默认值会自动迁移，自定义值保持原样，但应用仍会附加必须按 `photoIndex` 返回全部图片的安全输出契约。 |

如果日记目录尚未配置，也可直接回到底栏 **日记**，在空状态点 **一键设置默认目录**；系统选择器确认本机 Documents 后会创建并绑定 `deskcubby/diary` 与 `deskcubby/media`。这个快捷流程仍是 SAF 授权，不会把 `content://` 转为文件路径；要使用已有目录或把两类文件放在不同位置，请继续使用本页的两个手动选择按钮。

### 17.8 浏览器设置

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「网址」输入框（默认主页） | 输入文字 | 设置浏览器页的默认主页地址；为空时无法保存。 |
| 「浏览器主题」分段按钮：跟随 / 浅色 / 深色 | 单击 | 设置内置浏览器网页的明暗主题。 |
| 「电脑模式」开关 | 单击 | 开启后优先请求网页的桌面版布局。 |

### 17.9 小巧思设置

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「重新打开」分段按钮：上次停留 / 全部页面 | 单击 | 记住关闭前所在的分类页，或每次都从「全部」开始。 |
| 「内容显示」分段按钮：一行 / 完整 | 单击 | 列表条目内容显示为单行截断或完整多行。 |
| 「小巧思列表」行高滑条 | 拖动 | 48–120 dp 连续调节每行高度，右侧实时显示数值。 |
| 「重点背景颜色」圆形色块 | 单击 | 打开取色器对话框修改重点标记的背景色。说明文字提示：在小巧思页面长按条目可标记为重点。 |
| 「输入框最大高度」滑条 | 拖动 | 调节小巧思输入框的最大高度；超过上限后输入框内部滚动。 |

### 17.10 诗词本设置

**完整路径**：设置 → 子页面设置 → 诗词本；也可从 导航 → 诗词本 → 右上角齿轮 直接进入。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「导入字体」按钮 | 单击 | 打开 Android 系统文件选择器，选择可读取的 TTF/OTF 字体。应用验证字体并持久保留该文档的只读授权；无效文件会提示导入失败。 |
| 「恢复默认」按钮 | 单击 | 把诗词本草稿切回应用默认字体；点右上角保存后生效，不会删除手机上的原字体文件。 |
| 「字号」滑条 | 拖动 | 在 14–36 sp 之间调整诗词正文大小，预览即时变化。 |
| 「行距」滑条 | 拖动 | 在 1.00–2.00 倍之间调整诗词正文行距，预览即时变化。 |
| 「左对齐 / 居中」分段按钮 | 单击 | 调整诗词正文与出处的横向对齐。 |
| 「显示出处」开关 | 单击 | 控制卡片是否显示「—— 出处」；只影响展示，不删除已保存的出处。 |
| 「显示引号装饰」开关 | 单击 | 控制卡片左侧的大引号装饰。 |
| 「七言诗自动换行」开关 | 单击 | 开启后，只有检测到至少两句七言句式（每句 7 个汉字/Unicode 码点）的诗词才会按每七个正文字符连同标点自动分行；词、散文或其他非七言内容保持原样。手工换行仍保留，只改变展示，不改写数据库里的原文。 |
| 「预览」卡片 | 无（只读） | 使用当前草稿字体、字号、行距、对齐、装饰和七言换行开关实时展示示例。 |
| 保存（右上角） | 单击 | 保存全部诗词显示设置；有未保存修改时返回会显示保存/继续编辑/放弃确认框。 |

这些设置属于 v31 应用备份。字体 URI 只有在当前设备仍持有对应 SAF 读取授权时才会启用；跨设备导入备份后通常需要重新点「导入字体」选择字体文件。导入 v17 及更早备份时七言自动换行默认关闭。

### 17.12.1 收藏夹设置

**完整路径**：设置 → 子页面设置 → 收藏夹；也可从 导航 → 收藏夹 → 设置入口直接进入。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「每行高度」滑条 | 拖动 | 在 48–120 dp 之间调整收藏夹卡片的最小高度，实时显示当前 dp 数值；48 dp 会同步压缩卡片内边距、圆角和列表间距，单行无备注条目可与最紧凑的小巧思一样高，内容或备注较多时仍会自然增高。点右上角「保存」后生效。 |

收藏夹行高是普通设置，会随 v31 JSON 备份导入导出；收藏夹正文、备注和密码仍按 Android Vault 的加密边界处理。

### 17.11 RSS 订阅设置

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「每个订阅最多显示」滑条 | 拖动 | 10–200 篇、10 一档，限制每个订阅源显示的文章数量。 |
| 「显示摘要」开关 | 单击 | 关闭后列表只保留标题、订阅名和时间。 |
| 页底提示文字 | 无（只读） | 「订阅地址请在 RSS 页面右上角添加和管理。」——本页不管理订阅源本身。 |

### 17.12 AI 设置与 AI 配置

**完整路径**：设置 → 子页面设置 → AI 设置。

「AI 设置」页：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「AI 配置」入口卡 | 单击 | 进入「AI 配置」子页管理文字/图片模型（见下）。 |
| 「字体大小」滑条 | 拖动 | 12–28 sp 整数步进；作用于 AI 聊天页消息气泡与输入框文字。 |
| 「回复框宽度」滑条 | 拖动 | 280–1200 dp、10 dp 步进；限制 AI 聊天页消息气泡的最大宽度，窄屏会自动收窄。 |
| 「Agent 提示词」多行输入框 | 输入文字 | 最多 20000 字符；作为风格/任务偏好附加在严格内置规则之后，与每个模型配置的「附加模型指令」合并后随每次 Agent 运行发送，不能扩大权限。 |
| 「恢复 Agent 提示词」按钮 | 单击 | 把 Agent 提示词恢复为默认文案（只改草稿，仍需右上角保存）。 |
| 右上角「保存」 | 单击 | 持久化本页所有改动。 |
| 右上角「恢复本页默认值」 | 单击 | 把字号、回复框宽度与 Agent 提示词恢复默认（只改草稿）。 |

「AI 配置」子页（列表）：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| AI 配置卡片 | 单击 | 进入该配置的详情编辑页。卡片左侧圆形徽标区分类型（「文」= 文字模型，图片图标 = 图片模型）。 |
| AI 配置卡片 | **长按** | 弹出操作对话框「选择要对这项配置执行的操作。」：「复制配置」立即复制一份；「删除配置」进入二次确认对话框（将删除该配置及其 API Key）→「删除」/「取消」。这是列表页唯一的复制/删除入口。 |
| 「添加配置」按钮 | 单击 | 新建一个空白配置（默认类型为文字模型、默认端点为 OpenAI chat/completions 地址）并进入详情页填写。 |

详情页：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「配置类型」分段按钮：文字模型 / 图片模型 | 单击 | 切换配置类型；「系统提示词」输入框仅在文字模型下显示。 |
| 「配置名称」「API 地址」「模型名称」输入框 | 输入文字 | 三项均为保存前提：名称和模型名称非空、API 地址必须是带主机名的 https 地址（勾选「允许 HTTP」后才接受 http），否则地址标红且保存按钮置灰。 |
| 「API Key」输入框 | 输入文字 | 填写密钥；提示「明文显示并随配置保存，也会包含在设置备份中。」本地服务可留空。 |
| 「系统提示词」多行输入框 | 输入文字 | 仅文字模型显示，最多 20000 字符。 |
| 「原生工具调用」复选框 | 单击 | 仅在确认 Provider/模型原生支持 OpenAI-compatible `tools` / `tool_calls` 时开启。DeskCubby Agent 只运行勾选了此能力的文字配置；旧配置和新建配置默认关闭，普通聊天文字不会被解析成工具。 |
| 「允许 HTTP」复选框 | 单击 | 允许使用 http 接口地址，仅用于可信局域网接口。 |
| 「温度」滑条 | 拖动 | 0.0–2.0、0.1 步进，右侧实时显示数值。 |
| 「预览请求 JSON」按钮 | 单击 | 弹出「请求 JSON 预览」对话框，显示按当前配置生成的请求体（等宽字体、可滚动、文本可长按选择复制）；占位内容会在实际调用时替换，API Key 在请求头中、不会显示在预览里。「关闭」退出。 |

### 17.13 应用数据

**完整路径**：设置 → 应用数据。

页面从上到下依次为「存储占用」「云端同步」「自动保存文件夹」「JSON 导入与导出」「备份内容」「保存状态」。进入页面时会自动计算一次；统计用户选择的 SAF 目录可能需要数秒。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「存储占用」 | 查看 | 首次进入自动计算，显示安装包、Room 结构化数据库（含手机使用时间和健康每日统计）、设置与偏好、书架/阅读进度、阅读/小游戏时间 JSON、旧统计迁移文件、其他私有文件、缓存和应用专属外部文件；顶部给出应用及私有数据合计。 |
| 日记目录 / 媒体目录占用 | 查看 | 作为“用户目录”单独列出，不计入应用私有数据合计。若文档提供方超过 50,000 项、统计超过约 8 秒或部分内容不可读，数值前显示「≥」，表示只统计了已读取部分。 |
| 「重新统计」 | 单击 | 重新扫描当前类别；统计期间按钮置灰并显示加载状态。应用只读取大小元数据，不修改日记、媒体或小说原文件。 |
| 「云端同步」卡片 | 单击 | 进入云端同步子页，管理 WebDAV/S3 服务、同步内容与立即同步。主设置页不再另列一个云端同步入口。 |
| 「选择文件夹 / 更换文件夹」按钮 | 单击 | 打开系统文件夹选择器指定自动保存目录；选定后每次应用内容更改都会自动保存到 `dc.json`。若所选文件夹里已有 `dc.json` 或旧版 `DeskCubby.json`，会弹出「发现已有备份」对话框（显示核心记录、Vault 密文、游戏、特色统计和使用时间数量）：「导入已有备份」「用当前数据覆盖」「取消」三选一。 |
| 「立即保存」按钮 | 单击 | 立刻把当前数据写入自动保存文件夹。仅在已选择文件夹后显示；处理中置灰。 |
| 「停止自动保存」按钮 | 单击 | 关闭自动保存并解除文件夹绑定。仅在已选择文件夹后显示。 |
| 「导出 JSON」按钮 | 单击 | 打开系统「新建文件」界面，默认文件名 `DC-yyyy-MM-dd.json`。注意：**v31 JSON 整体未加密，包含明文 AI API Key、Agent 数据源/审批配置、自定义主题、结构化记录、收藏网址、多设备使用时间、既有七个游戏/变体的存档与特色统计、URI-free 阅读进度、桌面卡片设计及外观选项，以及 Vault 密文/校验元数据，勿存入公开或共享目录。Agent 会话/Review 与围棋数据不进入 v31。** |
| 「导入 JSON」按钮 | 单击 | 打开系统文件选择器选取备份文件，然后弹出「导入应用数据？」确认对话框。Android 0.15.0 可导入 v1–v31；v31 会恢复普通设置、Agent 数据源授权/权限模式/模型工具能力、AI 页面字号/回复框宽度/Agent 提示词、导航页列数与模块按钮/卡片底色、受控 Custom 主题、小卡片设计及外观、Markdown/主页配置和 Android Vault 密文（随后保持锁定），合并游戏、各设备使用时间和阅读进度。v1–v30 没有 Agent 与 v31 字段时使用“未授权任何数据源、需要批准、工具调用关闭”的安全默认。导入后云同步、手机使用时间和健康统计都会强制关闭，系统权限与本机设备 ID 不会迁移。 |
| 「查看整体 JSON」按钮 | 单击 | 当场生成与手动导出相同的**完整 v31 应用备份快照**并打开「整体 JSON」对话框；内容用等宽字体分段滚动显示，支持长按选择/复制。AI API Key 完整明文显示，Vault 显示密文/盐/校验值；Agent 会话、Review、附件与工具记录不显示。点「关闭」退出。 |
| 「保存状态」区 | 无（只读） | 显示「正在处理…」、上次自动保存时间、自动保存错误、操作结果消息；未配置时显示「尚未开启自动保存」，刚配置显示「等待首次自动保存」。 |

当前格式为 v31，单文件上限 64 MiB，并继续支持导入 v1–v30。除原有设置、AI 配置/明文 API Key、小巧思/分类、浏览器收藏、日期记录、诗词/分类和云同步非秘密元数据外，当前内容包括：

- v22 新增桌面小卡片设计：名称、1–6 格期望尺寸、颜色、背景图片 URI、主页模块或应用启动信息。背景图片文件和桌面实例绑定不进入 JSON；跨设备没有持久 URI 授权时只清除背景图。
- v23 新增底栏音乐可视化开关/样式和 2048 动画速度，并允许扫雷、蜘蛛纸牌的存档与最高分进入游戏状态区。音频权限仍由 Android 管理，不会写入 JSON。
- v24 新增音乐可视化的自适应/手动频率模式、最低/最高 Hz，以及七款小游戏的特色统计。导入统计时每个 `(gameId, metricKey)` 取本机与备份中的较大值，因此重复导入不会把计数翻倍；v1–v23 没有特色统计所有权，导入时保留本机现有统计。旧备份没有频率字段时使用安全默认的自适应模式。
- v25 新增全局背景图片的 `content://` URI、0–100% 可见度、0–40dp 模糊，以及软件教学模式总开关。图片文件本身不嵌入 JSON；恢复设备没有该 URI 的持久读取授权时会丢弃 URI。每个页面是否点过「知道了」是设备瞬时状态，不进入备份。
- v26 新增笔记库的 `content://` URI 引用、H1–H6 Markdown 标题字号，以及“笔记 / 小游戏 / 记录概览”三项主页模块及标题配置。笔记正文、目录结构和媒体文件不嵌入 JSON；恢复设备没有原笔记库授权时会丢弃该 URI，需重新选择仓库。
- v27 新增主页小游戏快捷入口选择；可保存七个游戏/变体中的任意组合。v1–v26 没有该字段时，恢复为 2048 · 4×4、贪吃蛇和扫雷三个安全默认入口。
- v28 新增受控 Custom Compose 主题的基础渲染、浅/深色八类颜色角色、圆角/边框/阴影/不透明度/间距/页面动效参数；同时新增最多 500 条书籍指纹进度。进度只含指纹、类型、TXT/PDF 位置、总页数和更新时间，按最新记录合并；v1–v27 缺少该区时保留本机进度并使用默认自定义主题。
- v29 为桌面小卡片设计增加 `showName`、0%–100% `backgroundOpacityPercent`、`showIcon`、左/中/右 `textAlignment` 和 75%–150% `textScalePercent`。导入 v1–v28 时分别回退到显示名称、100% 背景、显示图标、左侧对齐和 100% 字号。每个桌面实例的设备本机快照仍不进入备份。
- v31 新增 `agentEnabledSources`、`agentPermissionMode`、每个 AI 模型的 `supportsToolCalling`、AI 页面字号/回复框宽度/Agent 提示词，以及导航页列数与每个模块的按钮/卡片底色。这些只恢复配置，不携带聊天正文、附件、Agent Run、Review 或 Undo 载荷；v1–v30 导入使用安全默认。
- Android 0.15.0 导出 v31，并继续维持既有七游戏白名单：设置、`gameStates` 与 `gameStatistics` 投影会排除围棋快捷入口、围棋存档/最高提子和围棋特色统计。Windows 0.7.0 的实现未修改，仍只支持 Android v1–v29，不能导入 v31；Android 设备间可直接使用 v31，Agent 会话则走独立同步对象。
- 诗词本的持久排序、收藏夹卡片行高和每个 WebDAV/S3 配置的 User-Agent。
- Android Vault 的 AES-GCM 密文、IV、盐、PBKDF2 迭代次数、加密密码校验值和换密 generation；不含密码、明文或派生密钥。换设备导入后输入原密码即可解锁。
- 4×4/5×5/6×6 2048（包括完整撤回栈）、贪吃蛇、俄罗斯方块、自定义扫雷和蜘蛛纸牌的暂停存档与最高分，以及按随机稳定设备 ID 分组的手机使用时间，包括设备名、平台、日期、时区、应用包名和前台时长。

笔记/日记正文、媒体文件、阅读书架/书名/URI/封面/显示偏好、PDF 缩放、阅读与小游戏累计时长 JSON、全局/卡片背景图片文件、逐页教学确认、Agent 会话/附件/运行/Review/Undo、旧 AI 冻结上下文、WebDAV 密码、S3 Access Key ID/Secret Key/Session Token、健康历史和任何系统授权仍不在 JSON 中。本机随机设备 ID 不会被导入文件替换。旧版本缺少的字段继续使用安全默认值；v28–v31 的 URI-free 阅读位置会跨设备合并，v1–v27 导入不会清空本机已有阅读进度。

### 17.14 云端同步

**完整路径**：设置 → 应用数据 → 云端同步。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「开启云端同步」开关 | 单击 | 开启后后台任务会在联网时同步已启用的服务。需点顶栏「保存」才生效；且开启保存的前提是至少存在一个「已启用」的服务配置，否则保存按钮置灰。WebDAV 密码经 Android Keystore 加密；S3 用户名和 Key 明文保存在应用私有 DataStore。两者都不写入 JSON 备份或日志。 |
| 「立即同步」按钮 | 单击 | 手动触发一次普通同步。前提：云端同步已开启（已保存）且当前没有同步在进行；同步中按钮显示「正在同步」并置灰。状态区持久显示「上次同步时间：日期时间」（从未完成时显示「尚未同步 / Never」），并显示「正在处理 x/y 项」、消息/错误、以及「上次：上传 x，下载 y，冲突副本 z」。失败文字以 `[错误代码]` 开头；S3 HTTP 失败还会带状态码和安全提取的服务代码，便于排查而不显示响应正文或凭据。 |
| 「强制上传 / 强制下载」二合一按钮 | 单击左侧或右侧 | 与「立即同步」同高，左右各占一半。左侧先打开“确认强制上传？”对话框，右侧先打开“确认强制下载？”对话框；确认后才执行。两侧都要求总开关已保存开启且当前无同步；右侧还要求恰好启用一个云端来源，多来源时按钮置灰，服务层也会以 `[SYNC_FORCE_DOWNLOAD_SOURCE_COUNT]` 在任何端点读取前失败关闭。按钮下方说明会提醒：强制操作不传播删除，WebDAV 仍严格执行条件版本，S3 条件头按服务能力最佳努力发送，应用 JSON 下载仍只暂存待确认。 |
| 系统桌面的「立即同步」与「强制上传/下载」组件 | 单击 | 可从 Android 系统小组件面板加入桌面，使用同一安全同步实现和串行队列。桌面强制组件是显式快捷动作，点击后直接排队，不显示本页确认框；通用状态和配置要求详见“桌面小卡片与同步小组件（Android）”。 |
| 服务条目的「编辑」图标按钮 | 单击 | 打开该服务的同步配置详情页。 |
| 服务条目的「复制」图标按钮 | 单击 | 复制一份该服务配置。 |
| 服务条目的「删除」图标按钮 | 单击 | 弹出「删除同步配置？」确认对话框：将删除该配置及本机加密凭据，不会删除云端文件。「删除」/「取消」。 |
| 「新增同步服务」按钮 | 单击 | 新建一个空白同步配置并进入详情页；可分别配置多个 WebDAV 或 S3 兼容端点。 |
| 「导入待处理备份」按钮 | 单击 | 仅当云端拉回了新应用 JSON 时出现（远端应用 JSON 不会自动覆盖本机数据）。v31 确认导入会恢复 Agent 配置、Custom 主题、Markdown/主页/小卡片配置与 Android Vault 密文，合并游戏、各设备使用时间和 URI-free 阅读进度；真实书籍、笔记、日记、媒体、封面和背景图片不会被替换。独立的“多设备使用时间”“阅读进度”和“Agent 会话”对象在同步时自动合并，不需要点此按钮。 |

强制模式的精确边界：

- **强制上传**：本机新增项目会上传；同路径内容不同时，只在远端仍匹配本轮扫描到的版本时用本机内容覆盖。远端独有项目保留不删；扫描后远端再次修改会令条件写失败，而不是覆盖新修改。
- **强制下载**：只允许一个已启用云端来源，以免多个端点按顺序把前一来源静默覆盖；多来源会在读取本地同步内容或访问网络前失败关闭。单一来源下，云端新增项目会下载；同路径内容不同时，只在本机仍匹配本轮扫描快照时采用远端内容。本机独有项目保留不删；扫描后本机发生并发修改时保留本机内容，并按既有流程产生冲突副本。
- 两种模式都遵守对象数量、单对象/总流量、总时长、HTTPS/可信内网、SAF 和凭据边界。云端 `json/dc.json` 即使被“强制下载”选中也只进入待处理区，必须再点“导入待处理备份”并确认，后台不会自动覆盖设置或 Room。

同步配置详情页：

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「启用此配置」开关 | 单击 | 启用/停用该服务；全局同步开启时才会自动运行。 |
| 服务类型分段按钮：WebDAV / S3 兼容 | 单击 | 切换服务类型，下方凭据区随之切换为「WebDAV 凭据」或「S3 配置与凭据」。 |
| 「配置名称」「服务地址」「远端目录」输入框 | 输入文字 | 名称与服务地址必填（否则标红、无法保存）；远端目录默认 DeskCubby，不能包含 `.` 或 `..`。WebDAV 地址应填写完整协议；S3 可只填接入点，例如 `obss3.cstcloud.cn`，客户端会按 SSL/TLS 开关补协议。 |
| WebDAV「允许 HTTP」开关 | 单击 | 允许不加密的 `http://` 连接，仅用于可信局域网服务。 |
| S3「SSL/TLS」开关 | 单击 | 默认开启：没有协议的接入点自动加 `https://`。关闭时自动使用 `http://`，只能用于明确可信的内网端点；切换开关时，已有的 `http://` / `https://` 前缀也会随之调整。 |
| 「同步内容」复选框：日记 / 媒体 / 应用 JSON / 多设备使用时间 / 阅读进度 / Agent 会话 | 单击 | 至少勾选一项才能保存。「应用 JSON」包含明文 AI API Key、Agent 配置、Vault 密文、结构化数据和 v31 指纹进度；「多设备使用时间」使用 `usage/v1/{deviceId}.json`；「阅读进度」使用 `reading/v1/progress.json`；「Agent 会话」使用 `agent/v1/chats.json`。新建配置默认勾选阅读进度和 Agent 会话；旧配置不会被升级静默开启，需主动勾选并保存。Agent 对象不含本机 URI、图片字节、Review/Undo 载荷或秘密。远端对象都没有端到端加密，页面会显示敏感/隐私提示。 |
| 同步方向分段按钮：双向 / 仅上传 | 单击 | 双向同步遇到双方都修改时会保留冲突副本，不会无条件覆盖。 |
| WebDAV「用户名」「密码」输入框 | 输入文字 | 密码隐藏显示。已保存过凭据时留空即保留原值（提示「已在本机加密保存；留空可保留」）。 |
| S3「Bucket」「Region」输入框 | 输入文字 | 两项必填。Region 参与 AWS Signature V4 签名，须与服务商要求一致。 |
| S3「路径寻址（Path-Style）」开关 | 单击 | 默认开启，请求形如 `https://接入点/Bucket/目录`。若服务要求 Bucket 子域名，关闭后改为 `https://Bucket.接入点/目录`；部分 CSTCloud 接入点只接受后一种方式。 |
| S3「Access Key ID」「Secret Access Key」「会话令牌（可选）」输入框 | 输入文字 | 均为普通明文输入框。保存后写入应用私有 DataStore，再次编辑会完整显示；不会进入日志或 v31 JSON 备份。Access Key ID 与 Secret Access Key 是启用配置的必填项；可选的会话令牌可以清空。 |
| 「User-Agent」输入框 | 输入文字 | 为当前 WebDAV/S3 配置指定请求 User-Agent，最多 512 个字符且不能包含控制字符；留空会恢复默认值。保存后持久化，并用于该服务的后续同步请求。 |
| WebDAV「清除本机已保存凭据」复选框 | 单击 | 仅在已有 WebDAV 加密凭据时出现；勾选并保存会删除本机用户名/密码。S3 凭据直接在普通输入框中编辑或清空后保存。 |

应用 JSON 远端对象名为 `json/dc.json`，旧 `json/DeskCubby.json` 仍可被识别并暂存。多设备使用时间位于 `usage/v1/`；阅读进度固定为 `reading/v1/progress.json`；Agent 会话固定为 `agent/v1/chats.json`。Agent 会话用稳定同步 ID、更新时间与删除 tombstone 合并；同步文档的冻结文字和完成运行的 Provider 用量，但详细 Review/Undo 只留在执行设备。双向阅读同步下载远端记录后按 `updatedAt` 合并；时间相同时选择更靠后的合法位置。

WebDAV 仍严格要求普通响应或有界 `PROPFIND Depth: 0` 提供单个合法强 ETag，再用 `If-Match` 条件确认。Android 0.12.0 的 S3 通道则整体删除条件 GET 执行探针：不再故意发送错误的 `If-Match` 并要求服务以 409/412 拒绝，也不因未加引号、弱、多个、缺失 ETag 或服务忽略条件 GET 而产生探针型 `[SYNC_REMOTE_VALIDATION]`。实际读取/写入仍尽力发送 `If-Match` 或 `If-None-Match`，服务明确返回 409/412 时仍按冲突处理；manifest 和 payload SHA-256、内容寻址的不可变载荷，以及 PUT 没有可信 ETag 时按实际对象大小执行的同字节回读仍保留。这里的 SHA 校验确认“传到远端的字节是否正确”，不能在兼容服务忽略条件请求时替代服务端原子并发保护。Windows 0.7.0 继续使用自己的受限 S3 实现，不受本次 Android 修改影响。兼容端点若失败，请核对接入点、Region、SSL/TLS 与 Path-Style。

### 17.15 底部导航

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「显示文字」开关 | 单击 | 关闭后底栏仅显示图标，导航栏占用高度更低。 |
| 「音乐可视化」开关 | 单击 | 首次开启会请求 Android 音频权限；授权后，手机播放音乐时底栏背景会读取系统输出的实时频谱/波形并随节奏变化。可视化层只匹配底栏自身高度，不会把底栏撑满页面；退到后台、权限缺失或系统关闭动画时停止捕获，静音时不绘制。采样只在内存中使用，不写文件、不进日志。 |
| 「显示方式」：频谱直方图 / 实时波形 / 频谱曲线 | 单击整行或单选钮 | 选择底栏的实时绘制方式；只在可视化已开启且已授权时显示。直方图与曲线会显示下面的频率范围设置；实时波形直接使用时域样本，因此隐藏频率范围。 |
| 「自适应频率」 | 单击整行或单选钮 | 仅用于频谱直方图/曲线。根据当前音频中有效能量的低、高边界平滑调整分析范围，再按对数频带重采样，让画面更均匀铺满底栏而不是都聚在左侧；音乐内容变化时边界会逐步跟随，避免突然跳动。 |
| 「手动范围」 | 单击整行或单选钮 | 使用下方双端滑条自行设定最低与最高频率，范围为 20 Hz–20 kHz。两端按听觉更自然的对数比例移动；例如只想看鼓点可收窄到低频，想避开低频堆积可提高最低频率。设备实际频谱之外的范围保持空白，不会把边缘频点错误复制进去。 |
| 频率范围双端滑条 | 分别拖动两端 | 左侧为最低频率、右侧为最高频率，标签会用 Hz/kHz 显示当前值；最高值始终大于最低值。设置需点右上角「保存」，并随 v31 JSON 恢复。Android 音频权限本身仍不备份。 |
| 「默认启动页面」下拉框 | 单击展开后选择 | 选择应用启动时打开的页面；只列出当前可见的导航项（设置始终可选）。若把默认页对应的导航项隐藏，默认页会自动切换到第一个可见项。 |
| 导航项的图标按钮 | 单击 | 弹出图标网格菜单（包含手机使用时间与步数图标，5 个一行），点选即更换该导航项的图标。 |
| 导航项名称输入框 | 输入文字 | 自定义该页面在底栏/导航页显示的名称，最多 8 个字符；输入框标签显示默认名称。 |
| 导航项的四点拖动手柄 | **按住垂直拖动** | 调整导航项顺序：被拖行半透明跟手，插入位置显示主题色横线，松手落位；无障碍模式提供「上移/下移」。 |
| 「底栏」开关 | 单击 | 控制该页面是否显示在底部导航栏。「设置」项固定开启、不可关闭。这里不再显示「导航页」勾选项，避免与导航页设置冲突；页面收纳统一前往 设置 → 子页面设置 → 导航页。 |

### 17.16 导航页设置

**完整路径**：设置 → 子页面设置 → 导航页；也可从底栏「导航」页右上角调节按钮直达。

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 页面布局：一列 / 两列 / 三列 | 单击分段按钮 | 选择导航页模块按一列、两列或三列瀑布流显示；每列按卡片真实高度连续排列。改动需点右上角「保存」。 |
| 「显示页面描述」开关 | 单击 | 开启时卡片显示各自描述；关闭后所有导航卡片只显示图标和名称。 |
| 每个页面右侧开关 | 单击 | 控制该页面是否收进导航聚合页。主页、导航页本身和设置不会列在此处，也不能作为聚合卡片。收纳只在本页设置，不再与底部导航页重复显示。 |
| 「模块名称」输入框 | 输入文字 | 自定义该模块在导航页显示的名称，最多 32 个字符；英文界面下未自定义的名称显示内置英文名。 |
| 「按钮底色」行 | 单击「自定义颜色」/ 色块 | 打开 HSV 取色器为该模块的图标圆底设置颜色；设置后点「默认」恢复主题配色。 |
| 「模块整体底色」行 | 单击「自定义颜色」/ 色块 | 打开 HSV 取色器为该模块整张卡片设置底色；设置后点「默认」恢复主题配色。 |
| 「描述」输入框 | 输入文字 | 自定义该页面的导航卡片简介，最多 160 个 Unicode 字符、最多编辑 3 行；留空会单独隐藏这张卡片的描述。中文默认描述在英文界面会显示对应内置英文描述，真正自定义的文字则原样显示。 |
| 页面说明 | 无（只读） | 提醒导航页按所选列数排列，每列按卡片实际高度连续排列；描述长短不同不会按整行高度强制留白。 |

本页管理导航聚合页的“布局、收纳、名称与颜色”；导航页卡片顺序在导航页长按任一模块进入布局更改模式后，用每张卡片右上角出现的四点手柄拖动。页面图标、底栏可见性、默认启动页和底栏文字仍在 设置 → 底部导航 修改。

### 17.17 关于

| 按钮/控件 | 操作 | 效果 |
| --- | --- | --- |
| 「GitHub 仓库」按钮 | 单击 | 用外部浏览器打开项目 GitHub 仓库（github.com/vexpaer/DeskCubby）。 |
| 「应用教学」按钮 | 单击 | 用外部浏览器打开教学文档（仓库中的 TUTORIAL.md，也就是本文档）。 |
| 「第三方许可」按钮 | 单击 | 在应用内打开可选择、滚动的 `pdfium_NOTICES.txt`，其中保留 PdfiumAndroid 封装的 Apache-2.0 条款，以及 PDFium 引擎的 BSD 风格版权、许可和免责声明。DeskCubby 自身仍按 MIT License 发布。 |
| 「软件教学模式」开关 | 单击 | 默认开启；关闭后不再显示任何首次进入蒙版，立即生效且无需顶栏保存。重新开启时，只对尚未确认的逻辑页面继续显示。 |
| 「重新显示全部页面教学」按钮 | 单击 | 清除当前设备全部逐页确认；不改变总开关。若教学模式保持开启，当前 About 页也会重新出现说明蒙版。没有任何确认时按钮置灰。 |
| 「检查更新」按钮 | 单击 | 从 GitHub Release 获取最新版本信息；检查中按钮显示「正在检查…」并置灰。结果三种：「已是最新版本（x）」；失败显示红色错误信息；发现新版本时显示版本号、发布名、更新说明与「下载并安装」按钮。 |
| 「下载并安装」按钮 | 单击 | 下载该 Release 中与 DeskCubby 精确匹配的 APK，并显示字节进度。APK 只写入应用私有缓存，下载完成后会校验包名、版本必须更高且与发布版本一致、签名必须与当前应用一致；校验失败会删除文件且不会启动安装。首次侧载时 Android 可能要求允许「安装未知应用」，授权后返回会继续打开系统安装器。安装界面被关闭后可点「重新打开安装界面」，失败时可重试。 |
| 「应用显示名称」单选行：Desk Cubby / 桌洞 | 单击（整行或单选钮均可） | 立即切换桌面图标下显示的应用名称，无需点保存；部分启动器需要片刻才会刷新。页面顶部的应用名也随之变化。 |
| 「软件图标」单选行：经典图标 / 魔法书图标 / 桌洞图标 | 单击（整行或单选钮均可） | 立即切换 Android 桌面启动图标，无需点保存；第三项使用用户提供的白底黑色桌洞图。行内会显示预览，可与「Desk Cubby / 桌洞」桌面名称自由组合。部分启动器会缓存图标，需要片刻、回到桌面或重启启动器后才更新。 |

### 小技巧

- AI 配置子页列表没有可见的编辑/删除图标：单击卡片是编辑，**长按卡片**才会弹出「复制配置 / 删除配置」菜单。
- 主页模块、底部导航项和导航页卡片的排序都使用「四点拖动手柄」；按住手柄直接拖动即可，导航页会按左右两列的真实位置判断落点。
- 顶栏「保存」按钮只在当前页有改动且校验通过时可点；带保存机制的页面点返回时若有未保存修改，会弹出「设置尚未保存」对话框（保存 / 继续编辑 / 放弃），不会静默丢失。
- 外观页的主颜色/副颜色圆形色块、以及小巧思页的「重点背景颜色」色块本身都是可点击的——点色块直接打开取色器。
- 设置主页搜索支持中英文关键词（含「压缩」「热量」「webdav」「底栏」等隐藏关键词），点击结果会自动清空搜索词并跳转。
- WebDAV 密码和 AI Key 输入框留空表示保留已保存的值；WebDAV 可用「清除本机已保存凭据」删除。S3 Key 会在编辑页完整明文显示，可直接修改；可选会话令牌可清空，启用配置所需的 Access Key ID 与 Secret Access Key 不能为空。
- 开启「云端同步」总开关的保存前提：至少有一个已启用的同步服务配置，否则右上角保存按钮一直置灰。
- 「AI 热量估算」开关的前提：必须先在 AI 配置里各添加一个文字模型和一个图片模型，且开启后必须在本页选好两个模型才能保存。
- 为自动备份选择文件夹时，若文件夹里已有 `dc.json` 或旧版 `DeskCubby.json`，会强制三选一：导入已有备份 / 用当前数据覆盖 / 取消——不会静默覆盖。
- 「应用显示名称」和「软件图标」都是关于页里即点即生效的设置，不走顶栏保存；部分启动器刷新会有延迟。
- 诗词本、RSS、AI、底部导航、导航页、手机使用时间和健康设置可从对应功能页面直接跳入；此时点返回或保存会直接退出设置界面，而不是回到设置主页。
- 「查看整体 JSON」展示 v31 完整快照，因此会包含 Agent 来源/权限/模型能力配置、AI 页面字号/回复框宽度/Agent 提示词、导航页列数与模块按钮/卡片底色、Custom 主题、Markdown/主页/小卡片设置及卡片外观选项、Android Vault 密文、既有七个游戏/变体的存档与特色统计、各设备使用时间和 URI-free 书籍指纹进度；Agent 会话/附件/Review/Undo、围棋存档/特色统计/主页快捷入口、每个 App Widget ID 的实例快照、逐页教学确认、阅读书架/书名/URI/封面/偏好、累计阅读/游玩时间、笔记/日记正文、媒体/背景图片文件、S3 凭据、健康历史和系统权限仍不会因为“整体”二字被纳入。
- AI 详情页的「预览请求 JSON」对话框内文本支持长按选择复制；API Key 走请求头，不会出现在预览里。

---

## 常见问题

**问：Windows 为什么提示“未知发布者”？**

Windows 0.7.0 允许在没有 Authenticode 代码签名证书时正式发布，因此从项目正式发布页下载的安装包也可能被 SmartScreen 标记为“未知发布者”；这反映的是 Windows 发布者身份/信誉，不代表 Tauri updater 签名缺失。正式发布仍强制使用 Tauri updater 私钥生成并验证安装包 `.sig`。若未来配置 Authenticode，流程会自动签名并验证证书链和可信时间戳。普通本地构建还缺少生产 updater 公钥与端点，不会自动检查更新。

**问：Windows 可以直接使用 Android 的日记和媒体吗？**

可以使用已经同步或复制到电脑的普通目录，但不能把 Android 备份中的 `content://` URI 当作 Windows 路径。前往 **Windows 设置 → 子页面设置 → 日记与媒体**，分别选择电脑上的日记和媒体目录并点右上角「保存」；应用会原地读写。Windows 0.7.0 的结构化数据交换支持 Android v1–v29 并统一导出 v29，但不会复制 Android Room 数据库、日记/媒体文件或笔记库。

**问：Windows 为什么拒绝导入旧版 JSON？**

Windows 0.7.0 接受 Android v1–v29，并把旧格式在内存中安全升级为 v29（旧版桌面卡片外观字段用默认值补齐，`cloud_sync` 改写为 `cloud_sync_now`）。它会拒绝超过 64 MiB、根结构错误、数量/长度/枚举超限、重复 ID、孤立关联或 v29 专属结构损坏的文件；拒绝发生在预览阶段，不会部分导入。

**问：从 Windows 导出后，Android 的 AI、RSS 或浏览器数据会丢吗？**

正常 v28 导入会先递归清理云凭据、DPAPI 载荷、两端 Vault、usage/health 明细和来源路径，再把其余兼容数据用当前 Windows 用户的 DPAPI 加密保存。Windows 导出覆盖自己管理的设置和结构化记录；只有你在 Windows 修改过云配置后才覆盖非秘密 `cloudSyncConfigs`。浏览器/桌面小卡片与未知字段从兼容影子合并回来；Custom 主题完整参数会在严格校验后安全往返，Windows 当前按 `baseStyle` 显示。RSS/AI 普通配置由 Windows 管理，AI Key 按产品格式保持明文。不要手动删除兼容影子；跨电脑时应导出 JSON，而不是复制影子文件。

**问：在 Obsidian 修改后，DeskCubby 为什么不让我直接保存？**

编辑器打开文件时记录 SHA-256、大小和修改时间。如果磁盘版本随后变化，DeskCubby 会阻止无条件覆盖。选择「重新加载」查看外部版本、「另存冲突副本」保留两份，或在确认当前草稿应覆盖外部修改时选择「确认覆盖」。若文件被外部删除，「重新加载」会接受删除并关闭编辑，「确认覆盖」会重建原文件，「另存冲突副本」仍可保存草稿。

**问：Windows 版为什么找不到 Android 内置浏览器和桌面小卡片？**

这两项明确保持 Android 平台专属。Windows 0.5.0 已提供 RSS、AI/热量估算、小游戏（含本地双人围棋）、统计、手机使用时间和健康；内置浏览器/小卡片字段可在清理私有值后的兼容影子中往返，但 Windows 不展示或执行。网页文章和收藏链接会交给系统默认浏览器打开。

**问：Windows 手机使用时间中的“导入快照”和“链接只读文件”有什么区别？**

快照会把当前兼容历史复制进 Windows 的 DPAPI 私有缓存，之后源文件变化不会影响它；只读链接保存来源，点「刷新」时重新读取，但绝不会写入、改名或删除源文件。两种方式都不采集 Windows 活动，也不进入 v29、恢复点、自动备份或应用 JSON 云同步。专用云 usage 对象也只有下载/合并方向，Windows 不上传。

**问：浏览器、日期记录、诗词本、RSS、AI 聊天、收藏夹、小游戏、手机使用时间或健康在底栏找不到？**

它们默认收纳在底栏的「导航」页里，点「导航」标签即可看到卡片入口。想让某个页面直接显示在底栏：进入 **设置 → 底部导航**，勾选该页面的「底栏」复选框；也可以在同一页面改名、换图标、拖动排序。「设置」标签始终显示、无法隐藏。

**问：热量估算按钮为什么不显示 / 无法开启？**

热量估算相关功能（主页饮食图片自动估算、吃历顶栏「计算未计算的热量」、日期行重算按钮、编辑器餐别图自动估算）都要求：先在 **设置 → 子页面设置 → AI 设置 → AI 配置** 中分别添加至少一个**文字模型**和一个**图片模型**，再到 **设置 → 子页面设置 → 日记与媒体 → AI 热量估算** 打开「上传饮食图片后自动估算」并选好「热量计算文字模型」和「食物图片识别模型」。未完成配置时开关置灰、相关按钮隐藏。另外单张图片不能超过 8 MiB（过大时可开启饮食图片压缩）。

**问：收藏夹（加密保险库）的密码忘了怎么办？**

**无法找回。** 收藏夹内容使用密码派生密钥在本机加密，应用没有任何重置或恢复渠道——设置页和修改密码对话框都有明确提示。请务必牢记密码或另行妥善备份。修改密码需要输入旧密码，同样无法绕过。

新设或修改密码允许 1 个到任意多个 Unicode 码点；Emoji 各按一个码点计算。应用不限制最大长度，但重要内容仍建议使用足够长、难猜的密码。

**问：JSON 备份安全吗？**

**v32 JSON 整体未加密**，其中包含明文 AI API Key、Agent 来源授权/审批模式/模型工具能力、Custom 主题、Markdown/主页/小卡片设置及外观、记录内容、收藏网址、Android Vault 密文/盐/校验值、既有七个游戏/变体的存档与特色统计、按设备使用时间和 URI-free 书籍指纹进度，请勿放到公开或共享目录。Vault 正文仍是 AES-GCM 密文，且 JSON 不含密码或派生密钥；但普通记录、使用习惯和书籍指纹不是额外加密的。WebDAV 密码仍加密保存在本机；S3 Access Key ID、Secret Key 和 Session Token 按产品要求明文保存在应用私有 DataStore 并在编辑时回显，两者都不进入 JSON 或日志。Agent 会话/附件/运行/Review/Undo、围棋存档/最高提子/特色统计/主页入口、桌面实例快照、逐页教学确认、阅读书架/书名/URI/封面/偏好、PDF 缩放、累计阅读/游玩时间、健康历史、系统授权、笔记/日记正文、媒体和背景图片文件仍不进入 v31。

**问：吃历里的照片为什么没有拍摄地点？**

需要先在 **设置 → 子页面设置 → 日记与媒体** 打开「记录照片拍摄地点」开关。在 Android 10 及以上系统，开启时会弹出「媒体位置」权限请求：如果拒绝该权限，相机直接拍摄的照片仍可记录位置，但从相册导入的照片可能读不到 EXIF 位置信息。记录成功后，地点会显示在日记阅读预览的照片下方，以及吃历点开照片后的底部说明条；此前未记录地点的旧照片不会自动补写。

**问：手机使用时间为什么一直提示权限或没有历史？**

先到 **设置 → 子页面设置 → 手机使用时间** 开启并保存，再进入 **导航 → 统计 → 手机使用时间 → 打开系统设置**，在 Android「使用情况访问权限」中允许 DeskCubby。它不是普通权限弹窗。首次成功采集会尽量回填 Android 仍可访问的历史；系统已清理或从首个保留事件之前截断的部分无法恢复。今天可刷新，过去日期只有完整日结后才不再重复计算。

**问：为什么同一个应用连续几天都是完全相同的高值？**

这是 0.3.6 及更早版本在部分厂商系统上的日汇总兼容问题：系统可能把同一个汇总桶重复返回，旧版又把它分别写成多个日期。升级后进入 **导航 → 统计 → 手机使用时间** 并刷新，应用会用前后台事件重建 Android 仍保留事件的完整日期并覆盖错误日结；系统已清理事件的旧日期无法安全还原，会原样保留，避免用猜测值或 0 覆盖。

**问：健康为什么显示「—」或提示 Health Connect 不可用？**

先到 **设置 → 子页面设置 → 健康** 开启并保存，再进入 **导航 → 统计 → 健康**，滚动到页面最下方按提示授权 Health Connect。Android 14+ 通常内置，旧系统可能需要安装/更新，并需要健康或穿戴应用向其写入步数、距离或活动热量。0.8.0 不使用系统计步传感器；没有 Health Connect、没有授权或没有聚合值时显示「—」，不代表 0，也不会被伪造进日均。

**问：勾选给 Agent 的日记或小巧思会一次性全部上传吗？**

不会。**AI 聊天 → 左下角四方块 → 管理上下文** 的勾选只授予该数据源的检索和按需读取权限。每轮只先提供轻量目录信息；Agent 根据任务搜索并读取命中的少量条目，读取结果才会发往当前 AI 模型端点。取消勾选后相关工具立即无法访问。所有正文都按不可信外部数据处理，不能改变系统提示或审批模式。请只授权愿意交给所选服务商按需分析的来源。

**问：吃历长图为什么提示范围过大？**

导出前会限制 Android Bitmap 的安全高度和总像素，避免内存耗尽或生成无法正常保存的图片。缩短开始/结束日期分批导出即可；范围首尾日期都包含，且会沿用当前餐别筛选、滤镜、每行数量和说明开关。

**问：切换软件图标后桌面为什么没有立刻变化？**

设置位于 **设置 → 关于 → 软件图标**，选择后立即切换，无需保存。部分桌面启动器会缓存图标或名称，等待片刻、回到桌面或重启启动器后才会刷新；经典/魔法书/桌洞图标都可分别与「Desk Cubby/桌洞」名称组合。

**问：底部导航栏突然不见了？**

这不是故障。键盘弹出时、进入非顶级页面（如日记编辑器、吃历、回收站、设置子页等）时，底栏会自动隐藏；收起键盘或返回顶级页面即恢复。

**问：误删了日记或小巧思能找回吗？**

可以。日记删除是软删除：文件先复制到日记目录内的 `.DeskCubby Trash` 文件夹，可在日记列表顶栏的「日记回收站」里恢复。小巧思点垃圾桶会直接移入回收站（无确认弹窗），可在小巧思页顶栏的回收站里点「恢复」找回。两处的「永久删除」都不可恢复。注意：诗词本、日期记录、AI 对话、收藏夹条目的删除没有回收站，删除即永久。
