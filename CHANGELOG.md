# DeskCubby 更新日志（Android）

本文件汇总 DeskCubby Android 端全部版本发布说明，按版本倒序排列（最新在前）。
各版本对应的发布说明原文件保留在仓库本地的 `.android-release-notes-v<版本>.md`（不进 Git）。

| 版本 | versionCode | 应用 JSON 备份 | Room | 说明 |
| --- | --- | --- | --- | --- |
| [0.20.1](#android-0201) | 45 | v34 | v13 | 首次启动语言选择器真正显示；记录同步适配器注册与传输提速 |
| [0.20.0](#android-0200) | 44 | v34 | v13 | 手动备份与云同步解耦为记录同步引擎；手动备份不再包含 API Key/URI/云凭据，新增 Agent 对话备份 |
| [0.19.1](#android-0191) | 43 | v33 | v13 | 饮食图片落盘后立即释放拍照状态，不再等待 AI 热量估算与索引刷新 |
| [0.19.0](#android-0190) | 42 | v33 | v13 | 桌面小组件外观一致性：圆角/直角、卡片尺度、固定应用图标大小与照片提示修复 |
| [0.18.4](#android-0184) | 41 | v33 | v13 | 以内容区为抽屉坐标原点：收起不盖 rail，展开紧贴 rail 且无空隙 |
| [0.18.3](#android-0183) | 40 | v33 | v13 | 尝试整体右移抽屉并删除导航栏左上角 C 标识 |
| [0.18.2](#android-0182) | 39 | v33 | v13 | 修复横屏下阅读目录/小巧思分类抽屉停靠时压住左侧导航栏 |
| [0.18.1](#android-0181) | 38 | v33 | v13 | 横屏 Workspace 布局修复：导航栏遮挡、阅读方向冲突、安全区与抽屉重叠 |
| [0.16.3](#android-0163) | 35 | v33 | v13 | PDF 单指二维手势、自动封面与封面文字、2048 存档保护 |
| [0.16.2](#android-0162) | 34 | v33 | v13 | PDF 真正超宽渲染、桌面卡片交互与可视化重写、同步结果持久化 |
| [0.16.1](#android-0161) | 33 | v33 | v13 | PDF 缩放持久化、应用模块接通、移除同步小组件 |
| [0.16.0](#android-0160) | 32 | v32 | v13 | 五语言、桌面应用模块、云同步撤回 |
| [0.15.0](#android-0150) | 31 | v31 | v13 | AI 设置子页、Agent Markdown、导航布局 |
| [0.14.0](#android-0140) | 30 | v30 | v13 | Agent Runtime、Review/Undo |
| [0.13.2](#android-0132) | 29 | v29 | v12 | 阅读书名/时长、纯净模式覆盖 |
| [0.13.1](#android-0131) | 28 | v29 | v12 | 阅读页内 5% 进度 |
| [0.13.0](#android-0130) | 27 | v29 | v12 | PDFium、桌面组件复刻主页模块 |
| [0.12.0](#android-0120) | 26 | v29 | v12 | 首页云同步模块、实例快照 |
| [0.11.0](#android-0110) | 25 | v28 | v12 | 围棋、强制上传/下载、同步组件 |
| [0.10.0](#android-0100) | 24 | v28 | v12 | 纯净模式、Custom 主题、Plugin API |
| [0.9.3](#android-093) | 23 | v27 | v12 | PDF 超时安全回退 |
| [0.9.2](#android-092) | 22 | v27 | v12 | 吃历加载优化、单次统一估算 |
| [0.9.1](#android-091) | 21 | v27 | v12 | PDF 连续滚动、搜索、主题对比度 |
| [0.9.0](#android-090) | 20 | v26 | v12 | 笔记库、Markdown 预览、章节识别 |
| [0.8.0](#android-080) | 19 | v25 | v12 | 页面教学、全局背景、TXT 目录 |
| [0.7.0](#android-070) | 18 | v24 | v12 | 热量估算进度页、统计中心、特色统计 |
| [0.6.6](#android-066) | — | v23 | v11 | 阅读、音乐可视化、吃历热量详情 |
| [0.6.0](#android-060) | — | v23 | — | 阅读、音乐可视化、扫雷/蜘蛛纸牌 |
| [0.5.0](#android-050) | — | v22 | — | 桌面小卡片、诗词轮换、占用统计 |
| [0.4.2](#android-042) | — | — | — | 2048 修复、健康、诗词本 |

---

## Android 0.20.1

- 修复首次启动语言选择页从未真正显示的问题：`FirstLaunchLanguageScreen` 以前只定义未接线，现由 `DeskCubbyRoot` 在设置加载完成后按 `language_selected=false` 门控显示；已有用户与设置页语言切换不受影响。
- 修复记录同步的两个发布前问题：`ReaderPreferencesRecordSyncAdapter` 此前未注册进同步注册表，默认内容选择下每轮同步都会在 `READER_PREFERENCES` 中止，现已接线并在选中内容类型无适配器时先失败关闭；`RecordSyncRemoteStore` 改为每轮每个内容类型只加载一次对象清单并复用，远端清单已证明一致的载荷跳过上传，新记录以 must-not-exist 语义写入，记录下载恢复真实对象身份（存储名/版本），不再构造清单存储必然拒绝的伪造身份。
- Android 应用升至 0.20.1（versionCode 45）；应用 JSON 备份保持 v34，Room 保持 v13，Reader 保持 schema v8。

## Android 0.20.0

- 手动备份/恢复与云同步解耦：云端同步改为按记录同步引擎（RecordSyncEngine）逐内容类型传输（日记、笔记、小巧思、日期记录、诗词、收藏、RSS、游戏、使用统计、阅读进度、Agent 对话等），不再依赖整份应用 JSON 的“待确认导入”；手动导出/导入与自动备份仍走 v34 应用 JSON。
- 应用 JSON 备份升至 v34（继续导入 v1–v33）：新增 URI-free Agent 对话/运行载荷（`agentChats`，最大 64 MiB）随备份导出；手动备份不再包含 AI API Key、全局/卡片背景图片 URI、日记/媒体/笔记目录 URI、诗词字体 URI 与 WebDAV/S3 凭据，导入后这些字段为空、需在当前设备重新配置；`JSON_BACKUP` 内容类型从云同步中移除，旧备份中的该值不再加入运行中的同步配置。
- 设置主页移除各条目的副标题描述，改为更紧凑的图标 + 标题布局。
- Android 应用升至 0.20.0（versionCode 44）；应用 JSON 备份升至 v34，Room 保持 v13，Reader 保持 schema v8。

## Android 0.19.1

- 修复饮食图片从相机写入今日日记后，「正在添加照片…」状态仍停留到 AI 热量估算和索引刷新完成的问题：现在图片与今日日记真实落盘后立即释放拍照状态、恢复餐别按钮，热量估算与索引刷新改为后台进行，耗时或失败都不影响已保存的图片。
- Android 应用升至 0.19.1（versionCode 43）。

## Android 0.19.0

- 修复桌面「饮食图片」从相机成功写入今日日记后，非取消进度提示仍停留的问题：成功路径现在先主动关闭“正在添加照片…”再显示成功提示并结束代理 Activity。
- 桌面小卡片新增圆角/直角样式、70%–100% 卡片边长比例和 50%–150% 应用图标大小；应用按钮以固定 48dp 为 100% 基线，不再随 1×1、2×1、2×2 或更大面积自行放大缩小。
- 所有卡片统一使用 72dp/格的尺寸回退与同一实际边界解析，卡片尺度按较短边等比留白，减少不同宽高比实例之间视觉密度忽大忽小。
- 稳定五语言外观编辑器布局：长按钮单行省略、对齐与边角选项使用等宽单行、开关行固定最小高度，避免翻译长度让相邻 UI 控件自动换行并改变高度。
- 可复用设计和每个桌面实例快照保留新增外观字段；旧 v33 JSON 与旧实例快照缺少字段时回退到圆角、100% 卡片和 100% 图标。应用 JSON 备份仍为 v33，Room 保持 v13，Reader 保持 schema v8。
- Android 应用升至 0.19.0（versionCode 42）。

## Android 0.18.4

- 修复 0.18.0–0.18.3 延续的横屏抽屉坐标错误。根 `Row` 已把 rail 与内容分栏，阅读「目录」和小巧思「分类」抽屉本来就在内容区坐标内；0.18.2/0.18.3 再加 84dp padding/offset 会让展开面板与 rail 之间出现空隙，却无法从根本上保证收起面板不侵入 rail。
- 删除两处重复 rail 偏移，让 `ModalNavigationDrawer` 直接锚定内容区左边缘；同时给 rail 右侧内容列加 `clipToBounds()`，阻止收起/拖动动画的负向像素越界绘制到 sibling rail。收起时屏幕顺序为 rail → 内容；展开时为 rail → 子目录 → 内容，中间没有额外间距。竖屏 COMPACT 行为不变。
- 修复桌面 AI 浮层两处 `MutableInteractionSource` 未通过 `remember` 保持的问题，消除 Compose `RememberInComposition` Lint error；0.18.4 发布前 Debug Lint 已无 error。
- Android 应用升至 0.18.4（versionCode 41）；应用 JSON 备份保持 v33、Room 保持 v13、Reader 保持 schema v8。

## Android 0.18.3

- 彻底修复横屏下阅读「目录」与小巧思「分类」抽屉在**收起状态**压住左侧导航栏的问题：0.18.2 用 `padding(start)` 只把抽屉内部内容右移，导致展开时抽屉与导航栏之间出现空隙、收起时抽屉仍从整屏最左覆盖导航栏；本次改为用 `Modifier.offset` 整体平移整个抽屉面板（含背景），收起/展开都只停靠在内容区、不再触碰导航栏。
- 删除横屏左侧导航栏左上角的字母「C」标识。
- Android 应用升至 0.18.3（versionCode 40）；应用 JSON 备份保持 v33、Room 保持 v13、Reader 保持 schema v8。

## Android 0.18.2

- 修复横屏下阅读「目录」与小巧思「分类」抽屉在**停靠（收起/未完全展开）状态**时从屏幕最左滑出、压住左侧导航栏的问题：这两处 `ModalDrawerSheet` 在非 COMPACT（展开/中等）布局下增加与导航栏等宽的左偏移，使抽屉只出现在内容区、不再覆盖导航栏；展开后行为不变。
- Android 应用升至 0.18.2（versionCode 39）；应用 JSON 备份保持 v33、Room 保持 v13、Reader 保持 schema v8。

## Android 0.18.1

- 修复横屏 Workspace 模式的多处显示不完整：底部/左侧导航在横屏下由「覆盖层」改为真正的 Row 分栏，导航栏占据独立左侧列，各页面内容在导航栏右侧排版，不再被 84dp 覆盖。
- 左侧 Navigation Rail 增加安全区处理，Logo 与导航项避开状态栏/刘海与侧边系统导航条；小巧思分类抽屉在横屏下锚定内容区而非从屏幕最左滑出，不再与导航栏重叠。
- 协调应用级「屏幕方向」锁与阅读页的「阅读方向」偏好：阅读页打开时应用级方向锁自动挂起，由阅读页的每本书方向偏好接管，离开阅读页后恢复应用级偏好，避免两个 `DisposableEffect` 竞写 `requestedOrientation` 造成方向漂移/锁定。
- Android 应用升至 0.18.1（versionCode 38）；应用 JSON 备份保持 v33、Room 保持 v13、Reader 保持 schema v8。本轮经文 Android 模拟器（DeskCubby_API_35）实际安装验证：横屏 ROTATION_90 下应用正常、导航栏独立分栏、竖横屏旋转无崩溃、无 Activity 重建、无 AndroidRuntime 异常。

## Android 0.16.3

- 彻底重写 PDF 手势与页面定位层：增强与兼容视图共用原始指针视口，单指可沿任意方向同时拖动 X/Y，并保留纵向惯性；双指接管二维移动与即时缩放。放大后页面保持真实超宽布局和捏合中心，缩小到视口以内时居中并清除过期横向偏移。
- 修复 PDF 自动封面过度依赖文档提供方缩略图标记的问题：缩略图缺失时会串行、受限地渲染第一页并写入经过回读校验的私有缓存。两列书架的「修改封面」新增封面文字编辑，可自定义、留空隐藏或恢复跟随书名，不会改名或改写原 TXT/PDF。
- 2048 删除中心新局触控区，桌面棋盘只保留上、下、左、右四个透明方向区；渲染器在任何存档写入前拒绝旧启动器可能残留的 `NEW` 事件，升级后误触也不会清空或覆盖已有长局存档。
- Android 应用升至 0.16.3（versionCode 35）；Reader 私有状态升至 schema v8 并继续读取 v1–v7，应用 JSON 备份保持 v33，Room 保持 v13。

正式 APK：`DeskCubby.apk`（Android 8.0+，versionCode 35）。

## Android 0.16.2

- 彻底修复 PDF 缩放：增强与兼容视图按实际内容宽度计算横向溢出，放大到 100% 以上时页面会按目标像素宽度真正重渲染并可双指拖动，不再被父布局压回视口；缩小到视口以内时强制居中并清除过期横向偏移，不再贴左显示。
- 桌面小卡片编辑器把「主页模块 / 应用模块 / 应用按钮」改为单一下拉菜单；主页模块选择器只列主页内容，应用游戏、音乐、阅读、使用时间和云同步不再混入其中。
- 音乐可视化完整重写为无文字、无自身背景、占满整张卡片的实时频谱/波形/曲线；使用卡片实例的精确快照和实际桌面尺寸，修复切换模板后旧音乐配置继续覆盖新卡片的问题。
- 使用时间新增三种无背景、无坐标、无文字图表：纯色块、纯折线和纯柱状排行；范围统一为 7/30/90 天。应用启动采集、手动刷新及周期统计成功后都会刷新桌面卡片。
- 饮食图片按钮改为无底色纯 emoji，并在任意卡片高度中整体垂直居中。快速输入改为桌面草稿框与透明纸飞机：点输入框编辑草稿，点纸飞机直接加入「未分类」，长按编辑器主操作可选择已有分类后添加；草稿按桌面实例隔离且不进入备份。
- 2048 移除可见的上下左右和新游戏按钮；受 Android `RemoteViews` 不提供任意四向滑动回调的限制，棋盘改用透明边缘触控区完成上下左右移动，中间透明区新开一局，游戏仍直接在卡片内运行。
- 首页云端同步模块与设置页会在完成后持久显示上传、下载、冲突文件数，进程重启后仍可见；设置页「立即同步」右半改为「撤回一次」，下方继续保留强制上传/下载。
- Android 应用升至 0.16.2（versionCode 34）；应用 JSON 备份保持 v33，Room 保持 v13。

正式 APK：`DeskCubby.apk`（Android 8.0+，versionCode 34）。

## Android 0.16.1

- 修复 PDF 阅读双指缩放：双指缩放提交的最终比例现在写回保存的基准缩放（50%–300%），重新打开阅读器保持放大，不再回到默认 100%；修复增强视图横向平移失效（横向最大偏移从未计算，页面缩放后固定靠左、无法拖动），现在增强视图与兼容视图一样支持自由二维平移。
- 桌面小卡片新增「**应用模块**」内容类型，与主页模块、应用启动并列：2048 三种棋盘（4×4/5×5/6×6）、贪吃蛇、俄罗斯方块、扫雷、蜘蛛纸牌、围棋全部**直接在桌面运行**（状态保存于本机，无需打开应用）；音乐可视化 2×1–4×1 均支持；阅读显示最近书籍与进度；使用时间提供总览/图表/应用排行三种可视化并可设 3/7/30 天范围；两种云同步合并为一个模块（立即同步、撤回一次、强制上传/下载）。
- 移除独立的「立即同步」「强制上传/下载」**桌面小组件**：同步操作并入信息卡的应用模块与首页；旧配置与 v32 备份中的同步模块自动归一到合并的云端同步。
- 云端同步合并面板显示 **「已完成 ↑上传 ↓下载 冲突数」**（最近一次完成的同步结果），同步完成后桌面卡片自动刷新。
- 既有模块继续压缩空间：饮食图片 3×1/4×1 显示六个按钮且点按**直接拍照**（不再弹拍照/上传选择）；日期记录 3×1/4×1 显示最近一条、3×2/4×2 显示两条；每日诗词 4×2 完整显示诗词与按钮；快速输入 4×1 显示输入框与发送按钮，**长按发送可先选小巧思分类**。
- 配置页：顶部预览合并进「外观」分区；「显示内容」位于「外观」之前；移除大小设置与尺寸角标。
- 设置 → 外观与语言：「软件语言」改为**下拉菜单**（原分段按钮在窄屏换行，且非中文语言都显示为 English），选项显示各语言本名。
- Android 应用 JSON 备份升至 v33（继续兼容导入 v1–v32）。

正式 APK：`DeskCubby.apk`（Android 8.0+，versionCode 33）。

## Android 0.16.0

- 新增**繁体中文、韩语、日语**支持（连同简体中文、英文共五种语言）；**首次打开应用先显示语言选择页**，设置 → 外观与语言 → 软件语言 可随时切换，桌面小卡片文案同步多语言。
- PDF 阅读改进：缩放滑杆按 1% 步进（不再强制 10%）；双指矩阵缩放松手重渲染后，页面大小与位置与松手时保持一致（按捏合中心锚定）；单指拖动改为自由二维平移，不再把一次动作判定为横向或纵向，可以画圈拖动（增强与兼容视图都生效）。
- 桌面小卡片大幅改进：列表不再显示预览和“添加到桌面”按钮（长按桌面实例可重新配置），编辑页去掉大小设置（新建默认 2×1）、显示内容移到外观之前并内嵌背景预览，“应用启动按钮”改名“应用启动”。
- 新增“应用模块”：2048、贪吃蛇、俄罗斯方块、扫雷、蜘蛛纸牌、围棋六个小游戏**直接在桌面运行**（无需打开应用，状态保存于本机）；音乐可视化（2×1–4×1 均可，前台服务绘制实时频谱）；阅读模块显示最近书籍与进度；使用时间提供总览/图表/应用排行三种可视化并可设 3/7/30 天范围；两种云同步合并为一个模块（立即同步、撤回一次、强制上传/下载）。
- 既有模块压缩空间：3×1/4×1 也显示内容；饮食六键直接拍照；日期记录 3×1/4×1 显示最近一条、3×2/4×2 显示两条；每日诗词 4×2 同时显示诗词与按钮（修复只显示按钮的问题）；快速输入 4×1 显示输入框与发送，发送可先选分类。
- 云端同步新增“**撤回一次**”：立即同步按钮旁（首页与桌面小组件）可撤销最近一次同步对本机日记的改动（恢复被覆盖文件、回收新建文件）；桌面同步小组件完成状态显示上传/下载/冲突数量。
- Android 应用 JSON 备份升至 v32（继续兼容导入 v1–v31），新增桌面小卡片使用时间范围字段。

正式 APK：`DeskCubby.apk`（Android 8.0+，versionCode 32）。

## Android 0.15.0

- AI 设置改为独立子页结构：模型配置移到“AI 配置”子页；新增 AI 页面字体大小（12–28 sp）、AI 回复框最大宽度（280–1200 dp）与可一键恢复默认的 Agent 提示词。
- Agent 回复渲染 Markdown（标题、列表、引用、代码、链接，可选中文字）；修复 Agent 第二次对话在成功调用工具后提示“调用失败”且无回复的问题（工具轮次请求 JSON 规范序列化，不再发送 content:null、去掉非标准 name 字段）。
- 导航页新增一列/两列/三列布局选择；平时卡片不显示四点按钮，长按任一模块进入布局更改模式后所有卡片出现四点手柄可拖动排序，点右上角对勾退出；每个模块可设置名称、按钮底色与模块整体底色。
- PDF 双指缩放改为矩阵实时跟随：手势期间内容围绕捏合中心实时缩放、不重新渲染页面（不再出现加载圆圈），手势结束后按最终比例重新渲染。
- Android 应用 JSON 备份升至 v31（继续兼容导入 v1–v30），新增 AI 页面字号/回复框宽度/Agent 提示词与导航页列数、模块颜色字段。

正式 APK：`DeskCubby.apk`（Android 8.0+，versionCode 31）。

## Android 0.14.0

- 将原 AI Chat 升级为真正的多轮 Agent Runtime，支持原生工具调用、连续多工具执行、取消、失败恢复与严格的循环上限。
- 新增按数据源持久授权、图片/文档附件、“需要批准 / 全自动”权限模式，以及 mutation 审批、独立 Review 页面和真实 Undo。
- 基于 Kotlin Plugin API v2 增加 DeskCubby 数据、SAF 文件、网页与应用工具；未授权数据不可读取，外部内容一律按不可信数据处理。
- Agent 会话可通过独立 `agent/v1/chats.json` 云对象同步；统计新增 Provider 实际报告的输入、输出、总计、缓存输入、推理 Token、未报告调用与缓存率。
- Room 通过显式迁移升级至 v13，应用 JSON 升级至 v30 并继续兼容 v1–v29；旧 AI 会话保持可读。

正式 APK：`DeskCubby.apk`（Android 8.0+，versionCode 30）。

## Android 0.13.2

- 日记 Markdown 源码页恢复普通文字的完整编辑宽度；删除与拖动按钮只占用媒体所在行的右侧区域，不再让整篇文本统一变窄。
- 阅读统计会持久保存已阅读书籍的名称与累计前台阅读时长。书籍从书架移除后，统计页仍显示原书名和历史时长。
- 纯净模式下点按屏幕中央显示或隐藏标题栏、搜索栏和页码时，TXT/PDF 正文平面保持不动；控件改为覆盖在内容上方，避免阅读位置产生视觉跳动。
- 阅读/游戏时长私有文件内部升级到 schema v2，并继续兼容 schema v1；旧累计时长不会丢失，仍在书架中的旧记录会补全书名。
- 阅读书名与累计时长继续只保存在 Android 应用私有目录，不进入 v29 JSON 备份、云同步或 Windows 数据。
- Android 版本升级为 0.13.2（versionCode 29）；备份保持 v29，Room 保持 v12，Reader 私有状态保持 schema v7，Windows 保持 0.6.0。

## Android 0.13.1

- Android TXT、PDFium 增强视图与系统 PDF 兼容视图的阅读位置，现在按每页 5% 保存；例如停在一页的 65% 处，重新打开会恢复到该页内对应位置。
- 进度按 0/5/…/95 向下取整，滚动稳定约 600 毫秒后写入，离开阅读器时再保存一次，避免恢复时越过未读内容。
- 修复旋转或配置变化时旧的初始位置可能覆盖当前滚动状态的问题。
- Reader 私有状态升级到 schema v7，并继续读取 v1–v6；旧状态从记录页顶部恢复。
- 更细的 5% 页内偏移只保存在 Android 私有状态。v29 应用备份与 `reading/v1/progress.json` 的版本和字段集合保持不变，不新增书名、URI、封面、正文或页内偏移。
- Android 版本升级为 0.13.1（versionCode 28）；备份保持 v29，Room 保持 v12，Windows 保持 0.6.0。

## Android 0.13.0

**PDF 阅读**

- 增强视图改用 PDFium 1.0.35，通过 SAF 文件描述符直接打开原 PDF，不复制或改写文件；支持连续按需渲染、50%–300% 基准缩放、双指缩放、真实页码、双色显示、内嵌文字搜索跳页和文本目录扫描。
- 系统 `PdfRenderer` 继续作为兼容视图；打开、首个可见页、native 链接或内存失败时会安全回退。PDFium 初始化、渲染、文本读取和关闭在后台串行执行，超时后的迟到资源会自动关闭或回收。
- PdfiumAndroid 与 PDFium 的第三方许可和免责声明随 APK 分发，并可从“设置 → 关于 → 第三方许可”查看。

**桌面小卡片与组件**

- 保存模板后，所有仍绑定该模板的桌面实例会立即更新；不同 App Widget ID 继续保持独立配置，模板删除后保留实例最后快照。
- 应用快捷卡优先读取 launcher Activity/alias 图标，按 48dp 居中显示，并保留名称、图标、透明度、对齐与字号开关。
- 大尺寸卡片逐模块复刻全部 21 个主页模块：完整月历、日期记录、最近日记/小巧思、日常记录、随机日记、小游戏快捷入口、记录概览、两个云同步模块等均显示真实内容或逐项操作；小尺寸仍安全退化为整卡跳转。
- 诗词可直接刷新或加入诗词本，饮食图片提供六个餐别入口；快速输入显示仿输入框和发送按钮，并在私有 Activity 中完成真实输入。日期记录、日常记录、相机/相册和强制同步确认同样通过非导出的私有入口完成。

**首页云同步**

- 原单一云同步模块拆为“立即同步”和“强制上传/下载”两个独立模块，共享排队、进度、上次结果、错误和待确认应用 JSON 状态。
- 旧 `cloud_sync` 会在原位置迁移为两张卡；此前已主动移除的用户不会被重新添加。
- 强制上传、下载仍需确认；强制下载仅在恰好一个来源启用时可用。两种强制模式都不传播删除，应用 JSON 下载仍只暂存待用户确认。

**数据兼容性**：versionCode 27；备份仍为 v29（继续安全导入 v1–v28）；Reader schema v6；Room v12；Windows 0.5.0 暂不能导入完整 v29。

## Android 0.12.0

**围棋与阅读**

- 修复围棋合法落子后棋子、轮次和最后一手未可靠刷新的问题，并把触控统一为最近交叉点连续吸附。
- 两列书架新增“显示封面下方书名”开关；无自定义图片的 TXT 会把书名直接写在默认封面上。
- 修复增强 PDF 被首屏前非致命 AndroidX 请求事件误判失败的问题；文档打开、绑定和 30 秒首屏超时仍安全回退兼容视图。

**云同步**

- 新增真正位于 DeskCubby 应用首页的“云端同步”模块，可查看队列、进度、结果与待确认应用 JSON，并执行普通同步或确认后的强制上传/单来源强制下载。
- Android S3 删除条件 GET 执行语义探针，不再因兼容服务忽略条件请求或返回非标准/缺失 ETag 而阻断；仍保留最佳努力条件头、409/412 冲突处理、内容寻址、manifest/payload SHA-256 及必要的写后同字节回读。
- WebDAV 的严格强 ETag 与条件请求边界不变；强制模式仍不传播删除。

**系统桌面小组件**

- 修复添加多个信息卡后桌面只显示同一张卡的问题：每个 App Widget ID 现在保存独立设计快照，并支持长按重新配置。
- 新增名称显隐、背景透明度、图标显隐、左/中/右文字对齐和 75%–150% 字号。

**数据兼容性**：versionCode 26；备份 v29（继续安全导入 v1–v28）；Reader schema v6（继续读取 v1–v5）；Room v12；Windows 0.5.0 暂不能导入完整 v29。

## Android 0.11.0

**新用户与日记目录**

- 日记尚未配置时，可从空状态一键定位系统 Documents，并在用户确认 SAF 授权后创建或复用 `deskcubby/diary` 与 `deskcubby/media`。
- 两个子目录完成访问验证后才会原子保存；目录冲突、提供方自动改名或权限失败不会覆盖现有设置。

**阅读修复**

- 修复书架切换为两列后因多个 PDF 封面同时高内存渲染、以及仍在绘制的 Bitmap 被提前回收而闪退的问题。
- 封面改为按卡片实际尺寸有界加载；PDF 优先使用安全缓存或系统文档缩略图，无法取得时显示占位，不改写原文件。
- 修复增强 PDF 一次超时后在当前 Activity 内永久锁定兼容模式的问题：文档改为在 PdfView 附着后绑定，监听首帧失败，超时调整为 30 秒，并提供安全回退与重试入口。

**围棋**

- 新增本地双人围棋，支持 9、13、19 路棋盘、提子、禁自杀、简单劫与连续两次停着结束。
- 接入主页快捷入口、存档、特色统计和 TalkBack 棋盘操作。
- 为保持 Windows 0.4.0 对 v28 备份的严格兼容，围棋存档、统计和主页围棋快捷入口当前只保存在 Android 本机，不进入 v28 导出。

**同步与桌面组件**

- 云同步页在“立即同步”下新增同高的“强制上传 / 强制下载”二合一按钮。两种模式都保留条件校验、不传播删除；应用 JSON 下载仍只暂存待用户确认。
- 强制上传可安全作用于多个已启用端点；强制下载仅允许一个启用来源，多来源会在读取凭据、扫描内容或访问网络前失败关闭。
- S3 非标准 ETag 探测改用与验证读取相同的条件 GET；故意不匹配探针固定最多读取 64 KiB，服务忽略条件或响应超限时停止同步，避免无条件覆盖和额外大对象内存占用。
- 新增“立即同步”和“强制上传 / 强制下载”桌面组件；补齐三个组件 provider 的通用 Android 发现元数据。桌面动作使用联网约束的串行队列，只显示不含端点、路径、正文或凭据的通用状态。

**数据兼容性**：versionCode 25；备份仍为 v28；Reader schema v5；Room v12。

## Android 0.10.0

**阅读体验**

- 修复增强 PDF 在部分/全部文档上误判加载失败的问题：预热隔离服务，并同时确认首内容与首个位图；异常时仍安全回退兼容视图。
- 新增全屏纯净模式，隐藏书名、工具栏、页码和系统栏；点屏幕中央可唤醒或再次隐藏，且不会抢占滚动、平移或缩放手势。
- TXT、增强 PDF 与兼容 PDF 均支持自定义前景/背景色，包括 PDF 黑底白字。
- 书架支持原列表与两列封面，可选进度百分比；PDF 自动提取第一页封面，也可通过 SAF 手动更换。
- 修复兼容 PDF 各页横向偏移不一致。
- Reader 私有状态升级为 schema v5；通过完整文件 SHA-256 指纹同步相同 TXT/PDF 的阅读进度。

**同步与兼容**

- Android JSON 备份升级为 v28，新增受控自定义主题和最多 500 条 URI-free 阅读进度；继续兼容导入 v1–v27。
- 云同步新增可选的 `reading/v1/progress.json` 独立对象。它只包含文件指纹、类型、位置、页数和更新时间，不含书名、URI、封面或正文；旧云配置不会被静默启用。
- 修复部分 S3 兼容服务返回未加引号、弱、多个或缺失 ETag 时无法同步的问题。非标准 ETag 必须通过条件请求和内容 SHA-256 证明，服务忽略 `If-Match` 时仍失败关闭，陈旧写入不会覆盖新数据。

**其他改进**

- 2048 五位及更长数字保持单行并自动缩小；新增“总操作次数”，不再新增或展示失败次数/胜率，旧统计仍可兼容往返。
- 新增受控 Custom Compose 主题：可选择 Material、Liquid Glass 或 Organic Future 基础渲染，并配置明暗色板、圆角、边框、阴影、透明度、间距和动效；不执行 CSS 或脚本。
- 改进 OPPO/OnePlus/realme 等 ColorOS 桌面小卡片的首次渲染、即时/周期保底刷新和手动添加提示。
- 增加 Android Kotlin Plugin API 基础层，提供 Plugin 生命周期、PluginContext、PluginManager，以及 Diary/Vault/Media/Sync/AI/UI/Storage API 和现有实现 Adapter。当前业务调用链未迁移，生产插件集合为空，不改变现有页面。

**数据兼容性**：versionCode 24；备份 v28；Reader schema v5；Room v12。

## Android 0.9.3

- 修复部分设备打开 PDF 后一直停在加载状态：增强 PDF 视图的文档打开和首屏内容分别等待最多 8 秒，失败、服务不可用或超时后会自动切换到连续兼容视图，并显示中英文提示。
- 自动降级后仍可连续阅读、按真实页码跳转并使用已保存的 50%–300% 基准缩放，不需要重新导入原 PDF。
- PDF 文本选择、全文搜索和自动目录改为按系统真实能力启用：要求 Android 11+、S SDK Extension 至少为 13，并在增强视图首屏加载成功后才开始文本扫描；不支持的设备不再调用对应接口。
- 加载超时会作为可恢复失败处理，用户离开页面等调用方协程取消仍正常向上传播。

versionCode 23；备份保持 v27、Room 保持 v12；Windows 源码与版本未修改。

## Android 0.9.2

- 优化吃历加载：日记目录与媒体目录各通过一次 SAF 元数据查询建立索引，媒体文件和 `dc-media.json` 的 current/previous/pending/旧名候选复用同一份目录快照，减少文档提供方反复枚举。
- 从吃历滤镜或热量进度页返回时直接复用已加载结果；应用内日记、媒体或热量数据变更后按内容版本自动失效。即使 SAF 提供方没有及时更新修改时间，应用自己改过的日记也会被精准重读；外部编辑仍可用顶栏刷新强制重建。
- 热量估算保持不同日期依次排队，同一天最多 3 张图片并行调用图片模型。全部识别成功后，只调用一次文字模型，结合当天备注与所有图片统一计算，并可把同一餐的重复拍摄角度记为 0 kJ。
- 同一天的全部结果只写入一次 `dc-media.json`；任一图片识别、全日统一计算、目录复核或保存失败时不部分提交。进度页会显示并行数量、完成数量，以及每个图片请求和“全日统一计算”请求的实时详情。
- 内置旧版默认文字提示词会自动迁移到按 `photoIndex` 返回多图结果的新契约，用户自定义提示词保持不变。

versionCode 22；备份保持 v27、Room 保持 v12；Windows 源码与版本未修改。

## Android 0.9.1

- PDF 阅读改为连续纵向滚动，不再强制一页占满一屏。Android 9 及以上支持双指缩放、50%–300% 持久基准比例、文本选择复制、全文搜索高亮和基于 PDF 文本层的自动目录；Android 8 保留连续渲染与保存缩放比例的兼容视图。
- TXT 小说新增整书搜索和正文选择复制；章节识别会处理 BOM、零宽字符、普通/全角空格、中文数字和标题标点，并把首页整合目录条目合并到后文同名正文位置。
- 修复贪吃蛇、俄罗斯方块、扫雷和蜘蛛纸牌中普通文字未随亮/暗主题切换黑白前景色的问题。
- “设置 → 子页面设置 → 主页 → 小游戏快捷入口”可从三种 2048、贪吃蛇、俄罗斯方块、扫雷和蜘蛛纸牌中任意选择主页快捷按钮，也可全部隐藏。
- Android JSON 备份升级到 v27，保存主页小游戏快捷入口并继续兼容导入 v1–v26；阅读私有状态升级到 schema v4 并兼容 v1–v3。

versionCode 21；Room 保持 v12；Windows 源码与版本未修改。

## Android 0.9.0

- 新增「笔记」页面：可通过系统目录选择器打开 Obsidian 等 Markdown 笔记库，按文件夹优先、自然顺序浏览，并新建、重命名、删除文件夹和 `.md` 笔记。编辑器支持源码/预览切换、外部修改冲突处理；每次插入媒体都会重新选择来源和笔记库内的保存位置。
- 日记与笔记共用增强的 Markdown 预览，支持 H1–H6、粗体、斜体、删除线、列表、引用、代码、链接、普通图片和 Wiki 图片嵌入。可在“设置 → 日记与媒体”分别调整六级标题字号。
- TXT 阅读的智能章节识别支持智能规则、自定义正则或二者组合，可调标题最大长度，并扩充中英文卷、章、节、序言、番外等常见格式；设置保存后会重新解析当前书籍。
- 热量估算进行中可点击任务查看实时耗时、模型、阶段、思考与回复，思考区可折叠；失败项统一显示“估算失败”。每日热量详情现可单独重新估算某张图片，同时保留手工总量。
- 修复小游戏大厅及游戏内部部分文字在亮色/暗色主题下对比度错误；2048 默认配色会跟随当前主题。
- 主页新增笔记入口、2048/贪吃蛇/扫雷快捷入口及记录概览模块；小巧思首次打开分类时会定位到最下面的最新内容。
- Android JSON 备份升级到 v26，保存笔记目录设置、Markdown 标题字号及新版主页模块配置，并继续兼容导入 v1–v25。

versionCode 20；Room 保持 v12；Windows 源码与版本未修改。

## Android 0.8.0

- 新增默认开启的逐页软件教学模式：首次进入主页面、嵌套路由、设置子页、阅读状态页和具体小游戏时，会显示一次不可误触关闭的中英文蒙版说明；可在“设置 → 关于 → 页面教学”关闭或重置全部确认。
- “外观与语言”支持通过系统文件选择器设置全局背景图片，并调整 0–100% 可见度和 0–40 dp 模糊。图片保留在用户选择的位置，应用验证持久读取授权、实际文件大小和像素边界，不复制原图。
- TXT 阅读新增自动章节识别与左侧目录：支持“第…章”、Chapter、Part、Book、序章、前言、尾声、后记等标题。无原始页码的 TXT 会按约 1,800 字符生成稳定逻辑页，可输入页码或拖动进度跳转；PDF 同样支持页码/进度跳转。
- 阅读背景在五种预设之外增加任意自定义颜色，正文前景色会按亮度自动选择深色或浅色；旧版段落进度会安全迁移到对应逻辑页。
- 日常事件模板、填写框和实际 Markdown 追加链路完整支持换行，并继续只在真实文件写入成功后清空输入和提示成功。
- 蜘蛛纸牌点击重开后会先确认；取消保留当前牌局，确认后才清除存档并重新发牌，已操作但未完成的旧牌局按原有统计规则记为主动放弃。
- Android JSON 备份升级到 v25，保存全局背景 URI/参数和教学总开关，继续支持导入 v1–v24；背景图片文件和逐页确认保持设备本机状态，不进入备份。

versionCode 19，Room 保持 v12；Windows 源码与版本未修改。

## Android 0.7.0

- 吃历热量估算改为按日期排队、按天保存：单击计算器加入所有未估算照片，长按进入独立进度页，逐项显示日期、当天第几张图片，以及图片模型识别、文字模型估算和保存阶段。离开吃历后任务继续运行，其他日期的重算可排队，单日全部图片完成后只保存一次。
- 底部导航移除选中按钮的椭圆底色，让音乐可视化保持连续；频谱直方图和曲线新增自适应频率，以及 20 Hz–20 kHz 手动最低/最高频率范围，采用对数频带重采样改善能量只聚集在左侧的问题。
- 新增统一“统计”页面，一处查看日记篇数/字数/连续记录、手机使用时间、Health Connect 健康、阅读时长、小游戏时长与特色战绩，并可进入带趋势或对比图的详情页。缺失或未授权数据保持未知，不伪造为 0。
- 七款小游戏新增各自的终身特色统计：2048 记录有效移动、合并、最高方块和胜负；贪吃蛇记录食物、最长蛇身和失败；俄罗斯方块记录落定方块、消行、四消和失败；扫雷记录揭格、累计排雷、插旗和胜负；蜘蛛纸牌记录移牌、发牌、撤回和胜负。
- 扫雷支持双击已翻开的数字，一次打开周围除旗子外的所有未开格；标旗错误时仍可能触雷。蜘蛛纸牌只在明确放弃已经操作的未完成牌局时记录失败，普通离开页面不算失败。
- Room 升级到 v12，新增带联合主键和溢出保护的小游戏统计表；游戏动作统计与存档通过同一有序持久化队列提交，并提供 v11→v12 显式迁移与回归测试。
- Android JSON 备份升级到 v24，新增音乐频率设置和小游戏特色统计；继续支持 v1–v23，旧备份保留本机统计，v24 重复导入按指标取较大值，不会把累计值翻倍。

versionCode 18；Windows 源码与版本未修改。

## Android 0.6.6

- 修复 0.6.0 音乐可视化层参与页面测量、导致底部导航占满页面高度的问题；底栏可选择频谱直方图、波形或曲线，并仅在授权、前台和系统动画开启时捕获实时数据。
- 新增阅读页面：通过 SAF 导入 TXT/PDF 小说，支持横屏、背景色、字号、行距、段距、横竖屏锁定、滑动阅读、进度与每本书累计阅读时间。
- 小游戏新增可自定义扫雷和横屏单花色蜘蛛纸牌；2048 新增慢速/标准/快速动画速度；每个小游戏分别累计实际游玩时间。
- 吃历新增可点击的每日热量详情、可编辑总量、逐项食物分量/kJ 和备注；重新估算会把备注发送给文字模型，默认估算提示词与 `dc-media.json` 安全写入流程同步升级。
- 手机使用时间、其他设备使用时间缓存与健康每日统计安全迁移到 Room v11；Room 成为唯一运行时权威，原 JSON 格式继续用于迁移、备份、导入导出和云同步兼容。
- “设置 → 应用数据”可查看安装包、Room/偏好、阅读与娱乐数据、缓存及 SAF 日记/媒体目录等分类占用；收藏夹最低高度进一步压缩。
- 修复部分 WebDAV 普通响应不返回 ETag 时的同步失败：使用有界 `PROPFIND Depth: 0` 补取目标资源的强 ETag，并继续执行强条件请求；服务仍不提供强验证器时安全停止，不会无条件覆盖。
- Android 手动导出的 JSON 默认命名改为 `DC-yyyy-MM-dd.json`；自动备份与云端固定对象名保持兼容。

Android JSON 备份保持 v23，应用数据库升级到 Room v11；Windows 源码与版本未修改。

## Android 0.6.0

- 新增“阅读”页面：通过 SAF 导入 TXT/PDF 小说，支持横屏、TXT 背景/字号/行距/段距、横竖屏锁定、滑动阅读、阅读进度与每本书累计阅读时间。
- 底部导航新增音乐可视化，可选择频谱直方图、实时波形或频谱曲线；仅在用户开启并授予音频权限、应用位于前台时捕获实时数据，音频不会保存或上传。
- 小游戏新增自定义扫雷和横屏单花色蜘蛛纸牌；2048 新增慢速/标准/快速三档动画速度；每个小游戏分别累计前台游玩时间。
- “设置 → 应用数据”新增安装包、数据库、设置、阅读、娱乐计时、统计、缓存、应用外部目录及 SAF 日记/媒体目录的分类占用统计。
- 收藏夹最低卡片高度压缩到 48 dp，与紧凑小巧思一致。
- 修复部分 WebDAV 服务不提供强 ETag 时无法同步的问题：可使用 Last-Modified + SHA-256、写前校验、`If-Unmodified-Since` 和写后回读验证；没有可靠验证器时仍安全停止。
- Android JSON 备份升级为 v23，加入阅读导航项、音乐可视化设置、2048 动画速度和新增游戏存档，继续支持导入 v1–v22。阅读书架/进度、阅读与游戏累计时长不进入 v23 或 Android 系统备份。

Windows 0.2.0 仍只支持 v18 JSON，不能直接导入 Android 0.6.0 导出的 v23。

## Android 0.5.0

- 新增“导航 → 小卡片”页面：可设计多套 Android 桌面小组件，自定义 1–6 格宽高、常用尺寸、背景与文字颜色、背景图片，并显示主页模块或作为其他应用的启动按钮。
- 优化吃历加载：缓存未变化日记的图片引用，减少反复读取全部 Markdown；手动刷新仍会强制重建，并继续识别外部修改。
- 每日诗词从单一来源扩展为今日诗词、Hitokoto 诗词分类和古诗词·一言轮换；在线来源失败或内容已看过时使用 182 篇内置诗库，不再提示“暂无未看过的新诗词”。
- 健康页恢复为只使用 Health Connect，移除本机计步传感器回退，并将说明与授权状态模块移至页面最下方。
- 云同步状态新增持久化的上次同步时间；应用开屏底色改为黑色。
- Android JSON 备份升级为 v22，安全保存小卡片设计并继续支持导入 v1–v21；卡片背景图片文件和桌面实例绑定不会进入备份。

Windows 0.2.0 仍只支持 v18 JSON，不能直接导入 Android 0.5.0 导出的 v22。

## Android 0.4.2

- 修复 2048 只能左右滑动的问题，现在四个方向均可正常操作，并更换撤回图标。
- 修复诗词本首项排序及移动到首位的问题；删除分类时可选择连同诗词删除，或仅删除分类并将诗词移至“未分类”。
- “步数记录”升级为“健康”，可读取步数、距离和活动热量。
- 修复首页每日诗词刷新，同一天内不再返回重复诗词。
- 日记媒体新增删除与确认操作，可同时删除媒体文件及当前日记中的全部引用。

本版本仅更新 Android 应用。
